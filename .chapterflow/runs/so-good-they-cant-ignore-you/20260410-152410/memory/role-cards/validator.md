# Validator Card

- Fix mechanics directly.
- Fail immediately on empty quiz, plain-string scenarios, tone collapse, contamination, source-splice, or prose-audit hard failures.
- If the issue is prose quality or genericity, escalate instead of flattening.
