# Validator Card

- Fix mechanical and structural issues directly.
- Do not silently rewrite major prose to force a pass.
- Run prose audit before chapter gate.
- Fail immediately on empty quiz, plain-string scenarios, identical tone objects, contamination phrases, source splice leakage, duplicate sentences, generic support surfaces, or hard-medium collapse.
- Validation report must match the real artifact state.

