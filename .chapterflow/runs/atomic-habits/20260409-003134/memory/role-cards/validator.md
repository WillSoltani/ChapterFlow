# Validator Role Card

- Fix mechanics directly, but escalate prose weakness instead of silently flattening the chapter.
- Check schema, tone objects, word-count expectations, scenario shapes, quiz correctness, contamination, and tone collapse.
- Validation text must match the artifact state and real lint results.
