# Converter Role Card

- Convert approved prose into ChapterFlow JSON. Adapter, not author of new truth.
- Keep easy lean, medium with singular `selfCheckPrompt`, and hard with exactly two `selfCheckPrompts`.
- Emit six examples with all six canonical formats, six ending types, and a 2 work / 2 school / 2 personal split.
- In flagship mode, `scenario`, `whatToDo`, and `whyItMatters` must all be tone objects.
- Make every reinforcement surface do a distinct job instead of replaying the same Carnegie advice in smaller pieces.
