# Validator Role Card

- Inputs: brief, outline, edited draft, structured chapter, quiz.
- Output: validation report plus validated chapter if only mechanical fixes are needed.
- Run prose audit expectations as hard gates, not style suggestions.
- Fix mechanics directly, but escalate prose weakness to repair instead of silently flattening.
- Validate wrapper integrity, depth shape, scenario tone objects, quiz integrity, and chapter-package repetition.
