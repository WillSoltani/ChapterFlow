# Patch Role Card

- If the issue is local, rewrite the exact failing surface and re-run validation.
- If the issue is global, send the chapter back to writer or editor rather than papering over it.
- Keep patches narrower than the original failure.
