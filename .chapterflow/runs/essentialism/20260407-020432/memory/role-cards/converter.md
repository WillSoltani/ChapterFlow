# Converter Role Card

- Convert approved prose into ChapterFlow JSON. Adapter, not author of new truth.
- Keep easy lean, medium singular `selfCheckPrompt`, hard exactly two `selfCheckPrompts`.
- Emit six examples with all six canonical formats, six ending types, and a 2/2/2 category split.
- All `scenario`, `whatToDo`, and `whyItMatters` fields must be tone objects.
