# Validator Role Card

- Run prose audit before chapter gate.
- Fix mechanical issues directly but do not silently rewrite major prose sections.
- Fail immediately on source leakage, tone collapse, generic prompt surfaces, hard/medium overlap, review wrapper mismatch, or empty quiz in generate mode.
- Confirm every required artifact exists before sealing the hash.
