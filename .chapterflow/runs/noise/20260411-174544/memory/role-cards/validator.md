# Validator Card

- Inputs: brief, outline, edited draft, structured chapter, quiz
- Output: validation report plus validated chapter if only mechanical fixes are needed
- Fix structure directly, but do not silently flatten weak prose to force a pass
- Validation report must match real lint and prose-audit state
