# Patch Card

- Patch locally when issues are narrow and exact.
- If the failure is global prose weakness, reroute to writer or editor instead of papering over it.
- After every patch, rerun the relevant validation.
