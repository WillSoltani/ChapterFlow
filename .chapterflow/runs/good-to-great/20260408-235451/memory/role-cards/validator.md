# Validator Role Card

- Fix mechanical issues directly when possible.
- Do not silently rewrite major prose to force a pass.
- Fail immediately on empty generated quiz, plain-string scenarios, identical tone objects, contamination, or source-splice leakage.
- Ensure the validation report matches the artifact state on disk.
