# ChapterFlow — Production-Readiness Report

**Audited branch:** `main` @ `e90937368` (the freshly-merged post-UI-overhaul-integration state) · **Date:** 2026-06-14 · **Scope:** web app only — `app/`, `components/`, `lib/`, `infra/`, root config. **Excluded:** content pipeline & books.

> **This report supersedes an earlier draft.** The first pass was run against `ui-overhaul/auth`, which turned out to be **64 commits behind `main`** — that branch was missing the merged UI-overhaul waves (landing/onboarding/gamification/workspace/library/progress/reader) plus a dead-code consolidation. After the integration was merged to `main`, the full audit was **re-run against `main`**, and the two runs were cross-referenced. Most of the earlier *frontend* findings (fabricated landing/library data, onboarding gaps, badge split, duplicate nav, dead components, dev-server CSS break) are **resolved** on `main`. The *backend/security/infra* findings largely persist.

## How this was produced
- **Static audit (×2):** a 45-agent verified sweep (find → independent re-verify) across 22 subsystem slices, run once on `ui-overhaul/auth` and again on `main`. The `main` run produced **200 confirmed findings, 0 refuted**.
- **Reconciliation:** the two runs were diffed. **Two genuine criticals the `main` run missed/under-rated were re-confirmed by hand** (see the ⚠ note below) — a reminder that any single probabilistic audit pass has gaps; the truth is the union.
- **Build health (firsthand on `main`):** `npm install`, `tsc --noEmit`, `eslint`, unit tests, production `next build`, `npm audit`.

---

## 1. Verdict

**Not launch-ready yet — but the gap is a focused set of fixable defects, not rotten foundations.** The security/money *core* is genuinely strong: OAuth/PKCE + AES-GCM encrypted state, correct cookie flags, a unit-tested open-redirect guard, a webhook idempotency + `proSource` invariant state machine, atomic `TransactWrite` ledger/gift/entitlement guards, and consistent `requireActiveBookUser`/`requireAdminUser` authz with **no IDOR or auth holes found across 41 admin routes** and the social/billing backends. The blockers cluster in three places: (1) **client-trusted gaming of the paywall + points economy**, (2) **production config that fails silently to wrong/legacy hosts**, and (3) **data-integrity/compliance drift on user-facing surfaces**.

### Build health on `main` (firsthand)
| Check | Result |
|---|---|
| `tsc --noEmit` | ✅ Clean (after `npm install` — `@playwright/test` is a real devDep, just not in the prior checkout) |
| `next build` (production) | ✅ Compiles, all routes |
| Unit tests | ✅ **98 / 98** pass (was 92 on auth) |
| ESLint | ⚠️ **47 errors + 150 warnings** (down from 65/167; still React-19 hooks-compiler violations) |
| `npm audit` | ⚠️ **8 vulns (6 moderate, 2 high)** — prod-path: `next`→`postcss`, `uuid <11.1.1`, legacy `aws-sdk` v2; the 2 high are dev-only (`esbuild`) |
| `npm run dev` (local) | ✅ Fixed on `main` (`globals.css` now has `@source` scoping) |

---

## 2. Launch blockers — ranked (do these first)

Reconciled across both audit runs + manual verification. ⚠ = re-confirmed by hand because the automated `main` pass missed/under-rated it.

| # | ID | Blocker | Why | Effort |
|---|---|---|---|---|
| 1 | **X1 ⚠** | **Stripe webhook redirected to `/auth/login` in prod** — middleware matcher catches `/app/api/book/billing/webhook`; only `…/email/unsubscribe` is carved out. **Verified present on `main` (`middleware.ts` byte-identical to auth).** | Users pay → get nothing; cancellations/disputes/refunds never process | **trivial** |
| 2 | **X2 ⚠** | **CloudFront rewrites all 403/404 → homepage at HTTP 200** — `errorResponses` still maps both to `responseHttpStatus 200 → "/"`. **Verified present on `main`.** | Every legit API 403/404 becomes HTML+200; auth/lock/not-found gating breaks | **small** |
| 3 | **C1** | **Production canonical/OG/sitemap/robots URL silently defaults to `https://soltani.org`** when `NEXT_PUBLIC_SITE_URL`/`APP_BASE_URL` unset | Poisons SEO/social/JSON-LD across the marketing surface on day one | small |
| 4 | **H4** | **Chapter-unlock gating bypassable via the client-trusted state-sync PATCH** (reader auto-PATCHes localStorage `unlocked/completed`) | Any user unlocks + "completes" the entire paid catalog and harvests IP — defeats the paywall *and* the economy | medium |
| 5 | **H3** | **Quiz answer key (`correctChoiceId`/`correctIndex`) shipped to the client on every fetch** | Free first-attempt/perfect-score IP; corrupts the gamified economy & analytics | medium |
| 6 | **H12** | **`convertEmptyValues:true` rewrites empty Sets→NULL**, breaking entitlement set-initialization | Paying/license/admin users can't unlock their **first book** | small |
| 7 | **H15** | **Reminder/nudge emails default `APP_BASE_URL` to legacy `chapterflow.siliconx.ca`** | All email CTAs + one-click/List-Unsubscribe URLs point at the wrong host → CASL/CAN-SPAM | small |
| 8 | **H14** | **DynamoDB table has no TTL**, though cron+app write `ttl` | welcome-back nudge fires once-per-user forever; rotating dedup keys accumulate unbounded | trivial |
| 9 | **H1** | **Admin "Real MRR/ARR" sums annual subs as monthly (~12×)** | Every revenue number you'd launch on is wrong | small |
| 10 | **H2 / H19 / H20** | **GDPR/CCPA export truncates heavy users (unpaginated) and carries a false "analytics excluded" disclaimer the code contradicts; cookie policy omits `refresh_token`/`cf_acq_*` & misstates durations** | Regulatory/compliance exposure | small–medium |
| 11 | **H22 / H23 / H26 / H27** | **Cross-device data loss** — chapter notes overwritten by server load; reset-progress resurrected by union-merge; reminder time/timezone never reaches cron (everyone fires 20:00 UTC); stale device clobbers newer settings | Silent user-data loss & broken core settings | medium |
| 12 | **H28** | **No document-level Content-Security-Policy** on an auth + payments product | Security baseline missing | medium |
| 13 | **H9 / H18 / H10 / H11** | **Unbounded/abusable endpoints** — segment bulk-notify (5000 sequential sends in a 30s Lambda), public `/api/book-requests` (no rate-limit/captcha), Ask & reflection-feedback rate limits bypassable | Timeouts, partial/double-sends, paid-AI/SES cost abuse | medium |

---

## 3. Systemic themes (from the `main` synthesis)
1. **Client-trusted state defeats the paywall and the points economy** *(critical)* — `H4`, `H3`, `H12`.
2. **Production config fails silently to wrong/legacy hosts; unset prod values fail soft, not loud** *(critical)* — `C1`, `H15`, plus `X1`/`X2` (infra/edge).
3. **Cross-device sync drops or resurrects user data (no last-writer reconciliation / per-field merge)** *(high)* — `H22`, `H23`, `H26`, `H27`.
4. **Fabricated/hardcoded/diverging numbers on user-facing UI** *(high)* — `H5`, `H6`, `H24`, `H29` (progress, profile, event-detail; far fewer than on auth — the merge cleaned most).
5. **Backend-complete features with no working UI entry point** *(high)* — `H7` (reading-partner card), `H8` (referral escalation), `H25` (notebook unreachable).
6. **Synchronous fan-out / unbounded scans under hard Lambda timeouts** *(high)* — `H9`, `H16`, GDPR export.
7. **Published policies/copy contradict actual code behavior** *(high)* — `H19`, `H20`.
8. Plus *(medium/low)*: competing design-token systems, defense-in-depth gaps on public/paid endpoints, missing error+retry on secondary surfaces, onboarding promises dropped between systems, dependency/dead-code hygiene.

---

## 4. Findings catalog

> IDs `X#` = re-confirmed carry-overs (auto-audit missed). `C1`/`H#`/`M#`/`L#`/`P#` = `main` re-audit (verified). One ready-to-dispatch fix prompt per item lives in [docs/fix-prompts/](fix-prompts/) (by category).

### Critical / launch-blockers

#### X1 — Auth middleware redirects the Stripe webhook to /auth/login, breaking webhook processing in production
*prod-readiness · trivial* · **⚠ carried/re-confirmed (auto-audit missed)** — `middleware.ts:28-94, app/app/api/book/billing/webhook/route.ts:66-88`

> RE-CONFIRMED BY HAND on main (middleware.ts is byte-identical to the audited branch; the webhook route still serves under /app/api/book/billing/webhook and the matcher still catches it). The automated main re-audit did NOT surface this — treat as a launch blocker.

**What:** The Next.js routing root is /Users/radinsoltani/ChapterFlow/app (it contains the top-level layout.tsx + page.tsx), so the directory app/app/api/book/billing/webhook/route.ts serves at URL /app/api/book/billing/webhook. middleware.ts config.matcher is ["/app/:path*","/book/:path*","/dashboard/:path*"], so the webhook is matched. The only carve-out before the auth check is /app/api/book/email/unsubscribe (lines 34-36). In production (NODE_ENV=production so isDevAuthBypassEnabled is false and the dev Cognito-missing skip at 52-63 does not apply), the cookie-presence check at line 67 finds no id_token (Stripe sends no cookies) and the block at 72-87 issues a 302 redirect to /auth/login. Stripe treats any non-2xx (including 302) as a failed delivery, retries, then disables the endpoint.

**Impact:** Every Stripe webhook in production is intercepted before reaching the handler: checkout.session.completed never grants Pro (users pay, get nothing), customer.subscription.* never downgrades cancellations, charge.dispute.created never revokes access, invoice.paid/charge.refunded never persist. Money + access-control failure on the core monetization path; admin MRR/reconciliation drift permanently. The webhook's own correctness (signature, idempotency, proSource guard) is irrelevant because the request never arrives.

**Fix:** Add a pass-through at the very top of middleware() before the protectedSurface computation, mirroring the unsubscribe carve-out: `if (pathname.startsWith("/app/api/book/billing/webhook")) return NextResponse.next();`. Stronger and recommended: stop running auth middleware on API routes entirely — every /app/api/book route already enforces requireUser/requireActiveBookUser/requireAdminUser or a signature check at the route level, so the matcher should cover only UI surfaces (/app non-api pages, /book, /dashboard) and explicitly exclude /app/api/*. Validate end-to-end with `stripe trigger checkout.session.completed` against the deployed URL and confirm a 200 (not a 302) before launch. Add a CloudWatch alarm on the existing StripeWebhookFailure metric AND on webhook 3xx/4xx responses so a regression is caught.

#### X2 — CloudFront errorResponses rewrite ALL 403/404 to the homepage at HTTP 200, corrupting every API error response
*correctness · small* · **⚠ carried/re-confirmed (auto-audit missed)** — `infra/lib/chapterflow-frontend-stack.ts:740-753, app/app/api/book/_lib/http.ts:43-63, app/app/api/book/_lib/account-guard.ts:65-67, app/app/api/book/_lib/content-service.ts:62`

> RE-CONFIRMED BY HAND on main (frontend-stack.ts is byte-identical; errorResponses still maps 403/404 -> responseHttpStatus 200 -> "/"). The automated main re-audit only mentioned this as a side-note in a medium finding — treat as a launch blocker.

**What:** The distribution's errorResponses maps both httpStatus 403 and 404 to responsePagePath '/' with responseHttpStatus 200, applied to the default (server-Lambda) behavior. CloudFront custom error responses fire on the ORIGIN status code regardless of path, so every JSON 403/404 the app legitimately returns is replaced with homepage HTML at status 200.

**Impact:** Core authenticated flows break: clients receive HTML+200 instead of structured 403/404 JSON. account_deleted gating, chapter_locked gating, book/version not-found, and all admin 404 paths malfunction; clients calling .json() on HTML crash or silently mis-handle. Every-user blast radius. (401 is NOT in the list, so unauthenticated 401s still pass through.)

**Fix:** Remove the 403 and 404 entries from errorResponses so the server Lambda's own responses pass through (OpenNext renders its own 404 page). If a custom SPA fallback is genuinely wanted for browser navigations, scope it to a behavior that EXCLUDES the server/API paths — but for an OpenNext SSR app the correct fix is simply to delete both entries. Do not rewrite 4xx to 200.

#### C1 — Production canonical/OG/sitemap URL silently defaults to wrong domain (https://soltani.org) with no env guard
*prod-readiness · small* — `app/_lib/site-url.ts:11-13, app/_lib/chapterflow-brand.ts:32-37, app/page.tsx:26-89, app/sitemap.ts:4-13, app/robots.ts:4-14, app/layout.tsx:48-71, app/books/page.tsx:19-35`

**What:** getChapterFlowSiteUrl() -> siteBaseUrl() -> getSiteUrl(). getSiteUrl() returns NEXT_PUBLIC_SITE_URL || APP_BASE_URL, and when neither is set in production it returns the hardcoded literal 'https://soltani.org' (site-url.ts:11-13). Because getSiteUrl() always returns a non-empty string in production, the `|| (NODE_ENV==='production' ? DEFAULT_CHAPTERFLOW_SITE_URL ...)` fallback in chapterflow-brand.ts siteBaseUrl() (lines 33-36) — which would yield 'https://siliconx.ca' — is unreachable dead code. There is no build/runtime assertion that the env var is configured. Every SEO surface depends on getChapterFlowSiteUrl(): metadataBase + alternates.canonical + OG/Twitter url in layout.tsx and page.tsx, the Organization/WebSite/Product JSON-LD in page.tsx, the ItemList JSON-LD in books/page.tsx, every sitemap.ts entry, and the robots.ts sitemap URL. Three domains float through the tree (soltani.org hardcoded, siliconx.ca dead, chapterflow.ca as support).

**Impact:** On go-live, if the URL env var is not perfectly set, search engines and social cards index/scrape https://soltani.org, canonical tags point off-site, the sitemap/robots advertise the wrong host, and link previews break — a silent, hard-to-detect launch failure that poisons SEO and sharing from day one with no error surfaced. The presence of three different domain literals makes the misconfiguration easy.

**Fix:** In app/_lib/site-url.ts, when process.env.NODE_ENV==='production' and neither NEXT_PUBLIC_SITE_URL nor APP_BASE_URL is set, throw to fail the build/boot rather than returning a hardcoded domain. Replace the soltani.org literal with the one true production domain (or require it via env). Either delete the now-dead siliconx.ca fallback in chapterflow-brand.ts siteBaseUrl() or reconcile it to the single canonical default so there is exactly one production default. Confirm the real launch domain with the owner (chapterflow.ca is the support domain; siliconx.ca is the legal entity; neither matches the soltani.org fallback).

### High (29)

| ID | Cat | Issue | Fix (gist) |
|---|---|---|---|
| H1 | data-integrity | Admin "Real MRR/ARR" sums annual subscription amounts as if monthly — inflates MRR ~12x pe… | In billing/route.ts derive a per-entitlement monthly amount before summing: const monthly = e.subscriptionInterval === 'year' ? Math.round((e.subscrip… |
| H2 | data-integrity | GDPR/CCPA data export silently truncates for heavy users — unpaginated DynamoDB Queries | Add a paginate-until-LastEvaluatedKey loop to each list* function used by the export (mirror queryAllItems in account-erasure.ts:52-71). Cleanest: ref… |
| H3 | data-integrity | Quiz answer key (correctChoiceId/correctIndex) leaked to client on every quiz fetch | The author's proposed fix (gate correctChoiceId/correctIndex the same way selectedChoiceId is gated) is INCOMPLETE and would break the product: the li… |
| H4 | data-integrity | Chapter unlock gating bypassable via client-trusted state-sync PATCH | Make this PATCH read-only for gating fields: never raise unlockedThroughChapterNumber or add completedChapters from the request body — derive those ex… |
| H5 | data-integrity | Live progress page renders fabricated daily-quest reward ("+75 IP for all") that is never … | Either (a) drop the bonusIP prop and the '+IP for all'/'+IP earned' copy from DailyQuests.tsx so quests are habit nudges with no currency promise, or … |
| H6 | data-integrity | Live progress page Knowledge-Review and streak data come from localStorage, diverging from… | Point KnowledgeReview at the server FSRS API: replace the spaced-repetition.ts count helpers with GET /app/api/book/me/reviews?mode=stats (and ?mode=a… |
| H7 | dead-code | Reading-partner (pairs) feature has no UI entry point — entirely unreachable | Either (a) render PartnerProgressCard in components/workspace/WorkspacePage.tsx wired to onboarding state (and add the partner-progress/name endpoint … |
| H8 | data-integrity | Referral escalation tier rewards (4,600 IP + exclusive frames/themes/badges) are never awa… | In ensure-book-started.ts, inside the fraud.allowed branch AFTER the awaited markReferralActivationRewarded (so activatedInvites is incremented first)… |
| H9 | prod-readiness | Segment bulk-notify fans out up to 5000 sequential notification sends in one 30s request (… | Enqueue the send rather than doing it inline: write a segment-send job + matched userIds to SQS/Dynamo and process in a background worker Lambda (the … |
| H10 | security | Ask-the-Book daily rate limit is bypassable via a race (parallel requests) and aborted str… | Make the reservation atomic and decoupled from stream success. Before opening the stream, do a single conditional UpdateCommand `UpdateExpression: 'SE… |
| H11 | security | Reflection-feedback endpoint: client-controlled exampleId defeats the rate limit and uncap… | Rate-limit per (user, chapter) instead of per client exampleId, OR validate exampleId against the chapter's real examples (load via content-service ge… |
| H12 | data-integrity | DocumentClient convertEmptyValues:true rewrites empty Sets (and empty strings) to NULL, br… | Two changes that MUST land together (order matters): (1) remove `convertEmptyValues:true` from aws.ts marshallOptions; (2) stop writing empty sets — w… |
| H13 | security | Public Lambda Function URLs, no CloudFront lock, no WAF | Lock the Function URLs to CloudFront: use origins.FunctionUrlOrigin.withOriginAccessControl(serverFnUrl/imageFnUrl) (CDK now supports OAC for Function… |
| H14 | data-integrity | App table never enables TTL though cron+app write ttl | Add `timeToLiveAttribute: 'ttl'` to the ChapterFlowAppTable definition (backend-stack.ts:130). Verify no other entity stores a `ttl` attribute it does… |
| H15 | correctness | Reminder emails hardcode legacy APP_BASE_URL — unsubscribe broken | Pass CHAPTERFLOW_APP_BASE_URL into the backend deploy step env in _deploy-infra.yml AND/OR add an EMAIL_APP_BASE_URL (or reuse CHAPTERFLOW_APP_BASE_UR… |
| H16 | performance | Reminder cron full-table Scan + serial N+1 reads on 5-min timeout | Add a sparse GSI keyed on `entity` (or a dedicated reminder-schedule GSI keyed by reminder hour) so the cron Queries only settings rows; batch per-use… |
| H17 | accessibility | Reduced-motion users get the mobile sticky CTA bar pinned over content from first paint, w… | Compute the visibility predicate once regardless of motion preference and only vary the transition: `const shown = visible && !dismissed && !pricingIn… |
| H18 | security | Public book-request endpoint has no rate limiting or abuse controls (writes DynamoDB + sen… | Add a per-IP throttle before persist() (an in-memory token bucket is weak in serverless/multi-instance; prefer a DynamoDB TTL counter keyed on the x-f… |
| H19 | data-integrity | Privacy & Data-Rights pages claim analytics/location are excluded from self-serve export, … | Pick one source of truth. Since the export already includes analytics in JSON/MD, update data-rights/page.tsx:43-45 and the privacy Data-export bullet… |
| H20 | data-integrity | Cookie Policy omits the persistent refresh_token cookie and cf_acq_* acquisition cookies; … | In cookies/page.tsx essential-cookies table add a 'refresh_token' row (Secure, httpOnly, Duration 30 days, purpose: silent session renewal) and correc… |
| H21 | data-integrity | Onboarding tone/chapter-order choices never reach the reader (legacy localStorage key only… | Best fix: hydrate the reader from the already-persisted server settings. In useBookPreferences' /me/settings effect (lines 825-836) and/or parseStored… |
| H22 | data-integrity | Chapter notes (and other reader state) silently overwritten by server state on load — data… | In the GET-merge effect, merge per-field instead of spreading: never let an empty server notes replace a non-empty local notes (keep prev.notes when p… |
| H23 | data-integrity | Reset progress only mutates local state and can be resurrected by the server union-merge | Add an explicit server reset endpoint (e.g. POST /me/books/[bookId]/state/reset) that hard-overwrites server progress to initial; make onConfirm async… |
| H24 | data-integrity | Event detail fabricates "Eligible Books" titles by title-casing the bookId slug instead of… | Import getBookById from @/app/book/data/booksCatalog and render {getBookById(bookId)?.title ?? bookId} (optionally the author/cover too). Remove the s… |
| H25 | ux | Notebook page is unreachable from navigation and missing the server access guard | Add a Notebook entry to TopNav.moreNavItems ({ id: "notebook", label: "Notebook", href: "/book/notebook", icon: NotebookPen }) and extend BookNavTab w… |
| H26 | data-integrity | Reminder time & timezone chosen in Settings never reach the reminder cron — all reminders … | Write the canonical fields into settings.notifications. In the TimePicker onChange at BookSettingsClient.tsx:1212, in addition to setReminderTime(v), … |
| H27 | data-integrity | Cross-device settings clobber: server settings only applied when localStorage is empty; st… | Reconcile on load instead of all-or-nothing. Store a local last-synced timestamp alongside STORAGE_KEY; in the GET handler compare payload.updatedAt a… |
| H28 | security | No document-level Content-Security-Policy header for an auth + payments product | Add a Content-Security-Policy on the /(.*) headers() source. Because Next emits inline bootstrap scripts, the robust path is a per-request nonce injec… |
| H29 | data-integrity | Profile page hardcodes a stale, overstated catalog size ("93+ more books", "21 categories"… | In BookProfileClient.tsx import { CATALOG_BOOK_COUNT, CATALOG_CATEGORY_COUNT } from "@/lib/catalog-stats". Replace `totalCategories={21}` (line 947) w… |

### Medium (53)

| ID | Cat | Issue | Fix (gist) |
|---|---|---|---|
| M1 | security | OAuth callback never validates the state nonce — CSRF defense rests solely on encrypted-st… | In resolveAuthState's fallback branch (callback:51-60), require req.cookies.get('oauth_state')?.value === stateParam (wh… |
| M2 | performance | economy-health full-table Scan runs synchronously inside the admin economy HTTP route — ti… | Lowest-risk now: set `export const maxDuration = 60` on app/app/api/book/admin/metrics/economy/route.ts (matching reconc… |
| M3 | data-integrity | Export contains analytics data but tells the user it doesn't (false disclaimer) | Replace the analyticsAndLocation string at export/route.ts:130-131 with accurate copy (e.g. 'Usage analytics and approxi… |
| M4 | prod-readiness | audio-name route swallows auth/4xx into 500, is an unrate-limited paid-TTS abuse surface, … | Delete the route — it is fully redundant with the on-demand generation in the chapter-audio route, which already handles… |
| M5 | security | User IP sent to third-party geolocation provider over plaintext HTTP | Prefer the CDN edge headers (inferLocationFromHeaders already runs first inside resolveLocation, for free over the exist… |
| M6 | correctness | Depth-recommendation broken: wrong DynamoDB key casing (pk/sk) and updateDepthModel never … | Change both Key and Item to PK: bookUserPk(userId), SK: depthModelSk(bookId) to match the table schema. Then wire update… |
| M7 | performance | Audio route writes a full per-user MP3 to S3 on every playback that is never read back | Either (a) HeadObject/GetObject the stitched key first and short-circuit return it when present, making the write useful… |
| M8 | correctness | Streak-day IP bonus anchored to UTC day while streak progresses on user-timezone day — den… | Key streak_day and welcome_back by the same user-tz day used for the streak decision, scoped per user, to dedupe correct… |
| M9 | correctness | FSRS review submit is not idempotent — a double-submit corrupts the card schedule and writ… | Make recordReview optimistic-concurrency safe: PUT the card with a ConditionExpression like 'lastReviewAt = :expected' (… |
| M10 | correctness | share-events endpoint returns 400 on every request — requireString called with the body ob… | Change lines 27-28 to: const cardType = requireString(body.cardType, 'cardType'); const destination = requireString(body… |
| M11 | security | Referral fraud same-device / same-IP checks are dead — inviter device/IP always passed as … | In ensure-book-started.ts before checkReferralFraud, look up the inviter's most recent recorded device/IP (via the risk-… |
| M12 | data-integrity | PartnerProgressCard shows no partner progress or name — feature delivers nothing it promis… | Add a partner-summary endpoint (extend GET /me/pairs to also return the partner's displayName from their profile and a m… |
| M13 | prod-readiness | Reconciliation route declares maxDuration=60 but the server Lambda timeout is 30s — long r… | Move reconciliation off the request ServerFn into a dedicated lambda.Function with a real 60s+ timeout (invoked async by… |
| M14 | performance | Multiple metrics routes do unbounded full-table DynamoDB scans on every request under a 30… | Precompute cross-cutting aggregates in a daily/hourly cron Lambda writing an ANALYTICS#ROLLUP item the dashboard reads; … |
| M15 | correctness | Admin email search is case-sensitive (contains on raw stored email) — admins can't find us… | Persist a normalized emailLower field on the snapshot at write time in analytics-repo.ts (alongside email/emailDomain) a… |
| M16 | data-integrity | Admin insight-point adjustment marks the target user as active (lastActiveAt = now), corru… | For admin-originated transactions, skip the lastActiveAt SET and the activity-counting event. Add an `adminOriginated` (… |
| M17 | data-integrity | Manual entitlement override (plan/proStatus/freeBookSlots) writes no audit trail | In the entitlements PATCH handler, require a reason string (mirror insight-points/adjust's 10-char minimum) and write an… |
| M18 | correctness | Adaptive depth-routing is entirely dead/broken — wrong DynamoDB key casing, no writer, no … | Decide explicitly. (a) Delete depth-routing.ts, the depth-recommendation route, and useDepthRecommendation.ts as dead co… |
| M19 | data-integrity | Ask and Audio load book content from the in-repo package list, diverging from the S3/API-b… | Source Ask and Audio chapter content from the same content-service the reader uses (getPublishedBookManifest + S3 chapte… |
| M20 | performance | Audio route writes a per-user stitched MP3 to S3 on every request but never reads it back;… | Either implement the intended cache (HeadObject/GetObject the stitched key first and stream it directly when present — t… |
| M21 | correctness | No QueryCommand in repo.ts paginates LastEvaluatedKey — every list silently truncates at 1… | Add a shared pagination helper (loop on ExclusiveStartKey until LastEvaluatedKey is undefined) and route every full-part… |
| M22 | correctness | listLicenseKeys applies FilterExpression AFTER a single un-paginated 1MB read | Paginate over LastEvaluatedKey and accumulate before client-side filtering; OR fold status into the SK / a GSI so a filt… |
| M23 | security | Secrets in 5 Lambdas sharing one over-broad role | Split env: a secrets-free baseInfraEnv (cache/queue/table-name vars only) for ImageFn/RevalidationFn/DynamoProviderFn/Wa… |
| M24 | prod-readiness | No Lambda log retention; CloudFront 403/404->200 soft-404s | Set logRetention (e.g. logs.RetentionDays.ONE_MONTH) on every lambda.Function in both stacks (or a stack-level aspect). … |
| M25 | prod-readiness | lambda/dist no build automation; no prod-secret guard; dead App Runner role+CI perms | (1) Replace fromAsset('../lambda/dist') with CDK NodejsFunction bundling (esbuild at synth) OR add an esbuild step in _d… |
| M26 | ux | Prominent 'Start 14-day free trial' CTA dumps logged-out users into the reader, never into… | Point the logged-out Pro CTA at a returnTo that lands on the upgrade/checkout surface (e.g. '/auth/login?returnTo=%2Fboo… |
| M27 | prod-readiness | Sitemap and robots advertise login-gated /book/* routes as public indexable URLs | In sitemap.ts, replace the /book/* and /chapterflow entries with the public pages: /, /books, /pricing, /contact, and th… |
| M28 | correctness | Recorded Terms-acceptance version (2026-06-10) does not match the displayed Terms/Privacy … | Reconcile the dates: bump terms/page.tsx:18 and privacy/page.tsx:18 effective dates to 'June 10, 2026' to match LEGAL_TE… |
| M29 | maintainability | Terms, Privacy, and Cookies pages hardcode entity name, support email, and pricing instead… | In terms/page.tsx replace 'SiliconX Software Solutions' literals with LEGAL_ENTITY_NAME and the pricing prose with inter… |
| M30 | prod-readiness | Deep-link login emits an absolute returnTo that the env-only allowlist rejects when site-U… | Emit a RELATIVE returnTo from middleware: loginUrl.searchParams.set('returnTo', `${req.nextUrl.pathname}${req.nextUrl.se… |
| M31 | ux | Pair-accept 401 (expired/invalid token) dead-ends with only 'Go to dashboard', no re-login… | In the catch, capture err.status; in the error branch special-case 401 to render a primary action linking to /auth/login… |
| M32 | correctness | quizPassed is always false in the live flow (state.quizResult is never written client-side… | Pick one source of truth. Either wire useQuizSession's pass result into setQuizResult so state.quizResult reflects reali… |
| M33 | dead-code | Reflection-IP award flow in ExamplesList is fully dead (endpoint never called) | Decide intent. If revealing the analysis should reward IP, call onSubmitReflection(example.id, ...) from ScenarioCard.ha… |
| M34 | dead-code | Entire app/book/home/components tree's 11 widgets are dead code (~1577 LOC) | Delete the 11 orphaned files. Keep TopNav.tsx, SearchBox.tsx, GlobalSearchPanel.tsx, InfoModal.tsx (still live). Optiona… |
| M35 | ux | Library 'Active Reads' silently drops the 2nd in-progress book (exactly-two case) | Make the thresholds consistent. Simplest: change ActiveReads.tsx:22 to `if (books.length < 1) return null;` so a single … |
| M36 | correctness | Progress page client redirect race bounces fresh-browser onboarded users off /book/progres… | Don't redirect on the optimistic localStorage default. Add an explicit 'onboarding status resolved' flag to useOnboardin… |
| M37 | data-integrity | Profile shows hardcoded "93+ books" and "X of 21 categories" that don't match the real cat… | Import BOOKS_CATALOG and derive both: use BOOKS_CATALOG.length (minus free starts) for the upgrade copy, and pass totalC… |
| M38 | data-integrity | Badges page presents an "IP" level/currency derived from badge points the server never gra… | Pick one truth. Preferred (a): relabel the badges-page currency as 'Badge Points'/'Prestige' and the detail panel as cos… |
| M39 | ux | Failed reward redemption is shown in a success-styled (accent) banner | Return/store a tone with the message ({ message, tone: 'error'\|'success' }) from redeemReward, and switch the banner to… |
| M40 | data-integrity | Free-tier book count hardcoded to "2" instead of reading freeBookSlots from the entitlemen… | Thread the value through: pass freeBookSlots={billingState.payload?.entitlement.freeBookSlots ?? 2} into <SubscriptionCa… |
| M41 | data-integrity | "Personalized recommendations" consent toggle is a no-op — no surface honors it | Either wire the flag into wherever book suggestions are generated so disabling it actually suppresses history-driven rec… |
| M42 | security | CSV export is vulnerable to formula (CSV) injection on user-controlled fields | In csv.ts escape(), after computing `str`, neutralize leading formula triggers BEFORE the comma/quote/newline quoting, f… |
| M43 | ux | Bulk 'Notify segment' sends to thousands with no pre-send count or confirmation | Reuse the existing /segments/preview endpoint (it already returns matchCount): on modal open, POST the segment.filters t… |
| M44 | data-integrity | Night-mode schedule is partially wired (a scheduler exists) but only runs on 2 pages and r… | Consolidate the schedule into the theme pipeline instead of a second classList toggle: (a) fold the schedule evaluation … |
| M45 | dead-code | accentColor, interfaceDensity, focusRingStrength are dead theming plumbing (no CSS consume… | Pick a direction. To prune (recommended given no UI exists): remove accentColor/interfaceDensity/focusRingStrength from … |
| M46 | accessibility | High-contrast mode flattens every semantic border color to one gray via `* !important` | Drop the blanket `*` border override. The intended neutralization is already achieved by redefining --cf-border/--cf-bor… |
| M47 | performance | Color-blind mode applies an SVG filter:url() to <html>, forcing full-page filtered repaint… | Prefer per-token palette swaps for the colorblind modes: define [data-color-blind-mode='deuteranopia']{--accent-emerald:… |
| M48 | maintainability | Multiple divergent copy-pasted ProgressRing implementations (more than the audit found) | Replace app/book/library/[bookId]/components/ProgressRing.tsx and app/book/badges/components/ProgressRing.tsx with impor… |
| M49 | prod-readiness | Unused heavy dependencies shipped (aws-sdk v2, pdf-lib, pdfjs-dist) + app-unused openai — … | Remove aws-sdk, pdf-lib, pdfjs-dist from package.json dependencies and run npm install; re-run `npm audit --omit=dev` to… |
| M50 | maintainability | Static JSON book-package imports against a directory with untracked files — latent CI buil… | Prefer a build-time-generated manifest: replace the 68 hand-written imports with a generated index sourced from `git ls-… |
| M51 | prod-readiness | E2E CI gate only smoke-tests the Turbopack dev build with auth bypassed — never exercises … | Add a second Playwright project/job against the production server: `next build` (NEXT_DIST_DIR set) then `next start`, w… |
| M52 | ux | Badges page blanks out on a dashboard API failure | Plumb error+refetch through the hook: useBadgeSystem already calls useBookAnalytics — also pull { error, refetch } there… |
| M53 | data-integrity | Profile renders a zeroed account on a dashboard API failure | Pull `error` and `refetch` from useBookAnalytics in BookProfileClient (the hook already returns them at useBookAnalytics… |

### Low + Polish (117)

**prod-readiness (9):** `L1` /auth/login throws an unhandled 500 (raw Next error page) when Cognito env is mi…; `L8` /api/auth/session and /api/me cannot distinguish 'logged out' from 'Cognito/JWKS…; `P2` Console.log noise in hot audio path and stray debug logging; `L41` Scenario submissions trigger a Claude moderation call with no per-user submissio…; `L44` getServerEnv permanently caches missing env vars, so a transient SSM error becom…; `L49` Ingestion writes all S3 artifacts sequentially with no rollback on partial failu…; `L84` OpenDyslexic accessibility font loads from a third-party CDN at runtime; `L88` next.config.ts redirects use permanent: true (308) — permanently browser-cached,…; `L91` CI app-checks build uses placeholder Cognito/table env that masks missing-config…

**correctness (32):** `L2` /auth/refresh parses Cognito's token response with un-guarded await tokenRes.jso…; `L10` invoice.payment_failed records the wrong Stripe field as the failure reason — ad…; `L12` Trial-ending email can be re-sent on webhook redelivery (recordStripeWebhookEven…; `L13` Disputed-then-paid race can re-activate a chargebacked subscriber (out-of-order …; `L19` Unsubscribe and settings writes are read-modify-write with no concurrency guard; `L20` Published-catalog query has no pagination (silent truncation as library grows); `L21` Local quiz answer key defaults to index 0 when missing, can silently mis-grade; `L24` Consistency score double-counts today and can exceed 100%; `L26` Concurrent redemption of the same one-time reward returns a raw 500 instead of a…; `L30` Pair invite accept is a TOCTOU with non-conditional writes — concurrent/duplicat…; `L31` journeys/[journeyId]/start does not validate the journeyId exists in definitions; `L32` events join does not validate the event exists/is active; `L33` recordEventChapter dereferences eventDef.badge unconditionally in the completion…; `L34` Device token SK truncates endpoint to last 32 alphanumerics — collision can drop…; `L37` Filtered scan with Limit returns fewer rows than expected (ingestion jobs / metr…; `L38` Segment filters stored without server-side validation of field/operator/value; `L39` events POST casts body.books to string[] without per-element validation; `L45` summarizeProgress 'booksCompleted' uses a fragile chapter-number-vs-count heuris…; `L46` getLambdaHealth reports the MEDIAN of per-bucket percentiles instead of the perc…; `L47` deviceTokenSk hashes only the last 32 chars of the endpoint, risking key collisi…; `P7` estimateMonthlyCost uses hardcoded US-region on-demand prices and a 30-day month; `L56` Hero/Problem make unsourced absolute retention claims ('only proven method', 'fo…; `P8` CurrentYear renders on the server, risking a stale copyright year and a year-bou…; `L58` Privacy Policy effective date (April 2, 2026) predates the data practices it des…; `L62` UnlockCelebration currentStreak prop is never wired; route's real streak/points …; `L73` Reflection filter and "reflection" entry type never produced by the notebook API…; `L75` Division-by-zero NaN width on journey/event progress bars when a journey has 0 b…; `L76` BadgeCelebration hero effect has stale-closure deps and uncleared staggered time…; `L79` Streak alerts can stay enabled (and fire) after streak mode is turned off; `L81` Currency-formatted KPIs always render a bare '

---

## 5. Visual QA (production server + headless Chromium / Playwright, on `main`)
The production build serves all public routes (HTTP 200) with markup + design tokens present. Spot renders confirm the merge landed: **pricing** is polished/complete with a correct trial-billing disclosure; **signup** has Google/Apple/email + a ToS/Privacy consent checkbox; **onboarding** is clean; the **landing** Hero no longer carries the fabricated "Sarah K." testimonial, the **book-request form** now POSTs to a real endpoint, and the **duplicate mobile bottom nav** is gone. The dev-server CSS break is fixed (`@source` scoping). *Not visually QA'd:* the authenticated app (`/book/*`, `/dashboard`, admin) needs a live AWS backend — recommend a Playwright visual-regression suite against a seeded staging env before launch.

## 6. Coverage gaps — verify manually before launch
1. **Re-run `stripe trigger checkout.session.completed` against a deployed URL** and curl a deliberate 403/404 — `X1`/`X2` are edge/middleware issues only observable end-to-end (and the automated audit missed them).
2. Confirm the **Stripe webhook endpoint API version** matches the fields the code reads (unpinned payload drift).
3. **Load-test** unbounded scans (`H9`, `H16`, GDPR export, economy/metrics) and AI cost; measure `repo.ts` 1 MB query truncation with a large account.
4. **Concurrency-test** the races (`H10`, ledger/shop idempotency); **dry-run a GDPR deletion**; confirm a fresh `cdk synth` matches the deployed stacks; resolve `npm audit` (8 vulns).

## 7. Suggested execution sequence
- **Sprint 1 (launch gate):** §2 in order — start with `X1` + `X2` (trivial/small, catastrophic), then `C1`/`H15`/`H14` (config), then `H4`/`H3`/`H12` (paywall/economy), then `H1` (MRR), `H2`/`H19`/`H20` (compliance), `H28` (CSP), and the cross-device data-loss set (`H22`/`H23`/`H26`/`H27`).
- **Sprint 2 (high):** remaining highs — abuse/rate-limits (`H9`/`H10`/`H11`/`H18`), infra lockdown (`H13`), dead-but-promised features (`H7`/`H8`/`H25`), fabricated-number cleanups (`H5`/`H6`/`H24`/`H29`), reminder-cron correctness (`H16`).
- **Sprint 3 (medium):** §4 mediums — currency/MRR edge cases, idempotency, modal a11y, token-system consolidation, secondary-surface error states.
- **Ongoing:** low/polish — dead-code removal, copy/domain config, remaining a11y, dependency hygiene.
 though amounts are CAD; `L85` Reading 'Sans-Serif' option maps to Inter, which is never loaded; `L86` Dialog/Sheet has no nested-dialog stack: a second open overlay breaks Escape and…

**security (14):** `L3` /auth/refresh has no rate limiting or abuse control on a token-minting endpoint; `L9` logout() relies on Cognito to reject an attacker-supplied returnTo; redundant co…; `L17` Trial-ending email interpolates Stripe customer name into HTML without escaping; `L18` requireActiveBookUser fails open — a deleted account stays reachable during a Dy…; `L22` Per-book metrics endpoint returns aggregate reader counts for any bookId without…; `L23` Ask-book endpoint trusts client-supplied conversation history as assistant turns; `L27` Badges PUT lets a client record any catalog badge with arbitrary earnedAt/tier w…; `L35` Push endpoint not validated against allowlist at registration time (defense-in-d…; `L36` Pair invite codes use Math.random() rather than a CSPRNG; `L40` concept-graph route has no route-level auth/active-account guard while paid chap…; `L42` Audio route logs user display name and userId to application logs (PII) plus hea…; `L43` Ask endpoint accepts client-supplied conversation history as assistant turns (pr…; `L50` Unhandled-error logger emits full stack traces to console (potential sensitive-d…; `L87` @anthropic-ai/sdk pinned to a range that includes a moderate CVE (insecure defau…

**maintainability (17):** `L4` Two parallel design-token systems split across the auth components (--cf-* vs --…; `P3` Streak-shield purchase return value carries a misleading balance:0; `P4` INSIGHT_POINTS_COOKIE_NAME ('cf_ref') is the referral-attribution cookie — misle…; `P5` insight-points/adjust duplicates requireAdminUser logic instead of reusing the s…; `P6` Stale-comment 'fire-and-forget' on awaited notify loop, and notifications dailyV…; `L51` EmailSuppression key format is duplicated across multiple build roots with only …; `L52` Lower-severity infra items (content-bucket public, dead stream, prod CORS, hardc…; `L61` AuthErrorBanner uses legacy design tokens while the rest of the auth UI uses --c…; `L63` INSIGHT_POINTS_COOKIE_NAME is actually the referral cookie ('cf_ref') — misleadi…; `P11` Onboarding books.ts / recommendations.ts comments hardcode '67-book catalog' — a…; `L70` Two parallel CSS token systems (--cf-* vs legacy --bg-/--text-/--accent-) split …; `P16` Two parallel design-token systems used across this area (--cf-* vs --bg-base/--t…; `P18` Content scenario chart zips submissions and approvals by array index, not by dat…; `P19` Stale 'Phase N / once live in production' copy implies tracking is not wired; `P22` Five coexisting token systems (~170+ CSS custom properties) create high theming-…; `L89` Lint is advisory-only (continue-on-error) and next build does not lint — real co…; `L90` README.md still claims the repo ships two product domains including the deleted …

**dead-code (8):** `L5` access_token cookie is set on every auth/callback+refresh but never read by the …; `L11` Webhook imports a block of dead symbols left over from the removed referral-conv…; `L28` review_session_complete IP (10) is defined in the economy but never awarded by a…; `P9` prose-legal class applied to every legal article is a no-op (not defined in CSS …; `P14` Dead exported mock constants and helpers in libraryData.ts (MOCK_BOOKS / MOCK_US…; `P17` saveQuizHistory and saveNotes privacy fields exist in the schema but have no Set…; `L92` Dead mock-data modules left in the live tree (progressMockData, MOCK_USER_STATS/…; `L93` Unmounted legacy home-screen components (CurrentlyReadingCard, TodaySessionCard,…

**ux (21):** `L6` Brief refresh race: id_token cookie expires (~1h) on its own maxAge while a sile…; `L7` Middleware-built absolute returnTo is silently dropped to /book if the serving h…; `P1` Signup Enter-to-submit and OAuth buttons no-op silently when the consent box is …; `L29` Journeys feature is orphaned post-merge (no reachable nav link) and dashboard no…; `L53` /pricing SEO description advertises 'Challenge mode' that the pricing page never…; `L54` Contact page sticky header uses invalid inline CSS 'var(--bg-base)/80' so its ba…; `L59` Data-Rights page links to /book/settings, which requires authentication (redirec…; `L60` AuthErrorBanner retry hardcodes returnTo=/book and dismiss strips all query para…; `L65` AudioPlayer resets playback speed to 1x on every (re)load while UI still shows t…; `P12` AskBookDrawer follow-up suggestions use a biased Math.random sort over a tiny po…; `L68` Progress 'Reading Activity' empty-state CTA renders literal '→' instead of an ar…; `L69` Daily quests bonus IP collapses to '+0 IP earned' the moment all quests complete; `L72` Dashboard 'next reward' always points at the first catalog reward, not the next …; `P15` Rewards page TopNav highlights the wrong active tab (badges); `L77` Delete/Deactivate modals close on failure, hiding the error behind a transient t…; `L78` Settings search index omits the privacy/analytics consent toggle and welcome-bac…; `L80` Retention 'Reading frequency' card shows all-zeros during load instead of a skel…; `L82` Ops ingestion 'view' error link is a dead element (no href/action); `L83` User-detail drawer dumps raw entitlement/erase JSON, exposing internal fields an…; `P20` Admin client mutations use native alert()/confirm() instead of in-app dialogs; `P21` EmptyState CTA renders a raw <a href> (full page reload) instead of Next Link, a…

**data-integrity (11):** `L14` Dispute billing-event record key falls back to a non-deterministic timestamp if …; `L15` Mixed-currency subscriptions silently corrupt MRR sum (warning is logged but the…; `L25` Reflection IP (5 IP/example) is granted on a client-supplied length, not on vali…; `L48` ingestBookPackageFromS3 re-validates but stores the RAW unvalidated upload as bo…; `L57` BrowseLibraryPage default 'Most popular' sort is essentially reverse-alphabetica…; `L64` Terms/Privacy consent from signup is only persisted at onboarding completion (cl…; `L71` Library 'Recently added' sort just reverses catalog order (no real recency signa…; `P13` Library UserStats hardcodes currentStreak=0 / streakIsActiveToday=false / nextBa…; `L74` Profile "Pinned takeaways" stat is a derived count, not real pin state; `L94` Library UserStats reports currentStreak:0 and a hardcoded nextBadge regardless o…; `L95` Book detail page presents a derived page-count estimate as a real "pages" figure

**performance (2):** `L16` Analytics beacon does a DynamoDB read per navigation event to re-check consent; `L67` Full book detail is re-fetched server-side on every chapter navigation

**accessibility (3):** `L55` Interactive demo auto-advances animated content with no reduced-motion guard or …; `P10` Onboarding swipe-deck arrow-key handler is global and unguarded (no input-focus …; `L66` Keyboard-shortcuts overlay (and AddScenarioModal) are bespoke dialogs without fo…



---

## 5. Visual QA (production server + headless Chromium / Playwright, on `main`)
The production build serves all public routes (HTTP 200) with markup + design tokens present. Spot renders confirm the merge landed: **pricing** is polished/complete with a correct trial-billing disclosure; **signup** has Google/Apple/email + a ToS/Privacy consent checkbox; **onboarding** is clean; the **landing** Hero no longer carries the fabricated "Sarah K." testimonial, the **book-request form** now POSTs to a real endpoint, and the **duplicate mobile bottom nav** is gone. The dev-server CSS break is fixed (`@source` scoping). *Not visually QA'd:* the authenticated app (`/book/*`, `/dashboard`, admin) needs a live AWS backend — recommend a Playwright visual-regression suite against a seeded staging env before launch.

## 6. Coverage gaps — verify manually before launch
1. **Re-run `stripe trigger checkout.session.completed` against a deployed URL** and curl a deliberate 403/404 — `X1`/`X2` are edge/middleware issues only observable end-to-end (and the automated audit missed them).
2. Confirm the **Stripe webhook endpoint API version** matches the fields the code reads (unpinned payload drift).
3. **Load-test** unbounded scans (`H9`, `H16`, GDPR export, economy/metrics) and AI cost; measure `repo.ts` 1 MB query truncation with a large account.
4. **Concurrency-test** the races (`H10`, ledger/shop idempotency); **dry-run a GDPR deletion**; confirm a fresh `cdk synth` matches the deployed stacks; resolve `npm audit` (8 vulns).

## 7. Suggested execution sequence
- **Sprint 1 (launch gate):** §2 in order — start with `X1` + `X2` (trivial/small, catastrophic), then `C1`/`H15`/`H14` (config), then `H4`/`H3`/`H12` (paywall/economy), then `H1` (MRR), `H2`/`H19`/`H20` (compliance), `H28` (CSP), and the cross-device data-loss set (`H22`/`H23`/`H26`/`H27`).
- **Sprint 2 (high):** remaining highs — abuse/rate-limits (`H9`/`H10`/`H11`/`H18`), infra lockdown (`H13`), dead-but-promised features (`H7`/`H8`/`H25`), fabricated-number cleanups (`H5`/`H6`/`H24`/`H29`), reminder-cron correctness (`H16`).
- **Sprint 3 (medium):** §4 mediums — currency/MRR edge cases, idempotency, modal a11y, token-system consolidation, secondary-surface error states.
- **Ongoing:** low/polish — dead-code removal, copy/domain config, remaining a11y, dependency hygiene.
