# Book-score baselines (owner-instrument records)

Dated snapshots of `.claude/skills/book-score/baseline.json` (the owner's 10-factor
composite scores for the catalog — the only durable record of them; `.claude/` is
gitignored). Snapshot again whenever a regen refreshes the live baseline
(Phase-6 flow step v in the A+ plan).

- `baseline-2026-06-30.json` — 131 books, min 46.8 / median 80.8 / max 85.3
  (97 gate PASS / 34 CAPPED). Pre-v24-catalog-regen reference.
