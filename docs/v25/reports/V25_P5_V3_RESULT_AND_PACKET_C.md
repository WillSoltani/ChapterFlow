# P5 v3 result — READER COMPLETE; source/quiz packet C (2026-07-16)

`s16-forward-pilot-role-readiness-v3` terminal: **BLOCKED_ROLE_READINESS** at
72/84 base (1 typed replay, 0 API; the 12-call remainder cannot fund a block).
Cumulative P5 spend: 192 base calls across three identities.

## Landed: the reader role pair is COMPLETE
- **reader primary `gpt-5.5@high` — READY** (the Option B density-for-pacing
  ruling proven live), **reader audit `gpt-5.6-sol@xhigh` — READY** (repeat).
- Both owner rulings (A: canary gold; B: craft map) validated end-to-end.

## Remaining blockers, precisely diagnosed
1. **Source (0/2)** — two distinct causes:
   - `gpt-5.6-sol@{high,xhigh}`: killed by ONE recurring assembly rule —
     "constructed/generic register finding requires plan evidence" (3 of 4
     invalid outputs across both efforts; every VALID defect case was
     detected, 9/9 and 8/8). The models cite chapter evidence where the
     assembly demands plan evidence. This is an instrument-interaction defect
     class (packet C candidate: either the rule admits chapter+plan citation,
     or the task instructions must state the plan-evidence requirement —
     currently implicit).
   - `gpt-5.5@xhigh`: failed on merits (8/10 sensitivity, 4/12 register) —
     but its register misses INCLUDE the exact adjudicated R2 divergence
     (`PARTIALLY_SUPPORTED/clearly_sourced` on source-bound-detail holdouts,
     the same family as the adjudicated canary). The R2 pattern likely
     extends into holdout gold; needs the same cross-profile consensus
     analysis (5 profile-runs of the same holdout are now retained).
2. **Quiz — still zero data after three campaigns** (budget ordering: reader
   and source always exhaust the envelope first under the frozen role order).

## Decision needed (packet C — no live calls until ruled)
- C1: rule on the plan-evidence assembly rule (admit chapter-cited findings on
  constructed/generic units, or make the requirement explicit in the task).
- C2: authorize the cross-profile consensus analysis → holdout-gold
  adjudication for the source register/support labels (same standard as R2).
- C3: budget + ordering for a v4 campaign — recommend quiz-first role order
  for the next envelope (quiz needs only 1 qualifier and has never run) or a
  targeted envelope (quiz ≤56 + source re-test ≤56 after C1/C2).

Evidence: evidence branch `c6529b484` (PR #406). Reader freeze intentionally
NOT minted (the runner freezes the role set only when ALL roles are ready).
