# S10 — Run the v25 pipeline tests in CI

Run on: Sonnet 5 implementer + Opus reviewer (or Opus). Mode: autonomous, PR. Depends on: S01. Parallel with everything in S02–S09.
Not a gate for S11 (it removes no run blocker; S11 runs the full suite locally), but part of the end state. Estimate: 2–3 h wall (one CI cycle ~20 min); ~$20–40 session quota. No pipeline model calls.

---PROMPT---
You are executing task S10 of the ChapterFlow v25 pipeline completion plan. Verified: the CI job "v21 Pipeline Typecheck + Tests" (`.github/workflows/ci.yml` ~157-189) runs root scripts `pipeline:typecheck` / `pipeline:test`, which target the LEGACY workspace `@chapterflow/v21-authored`; the v25 package `scripts/book/prompts/chapterflow-v24-author-pipeline` is only type-checked (via `typecheck:book`), and its ~3,290-case suite has never run in CI. Root scripts `pipeline24:typecheck` and `pipeline24:test` exist (package.json ~37-38) but no workflow calls them. Add a CI job that runs them, as one PR. I explicitly authorize multi-agent orchestration, including the Agent and Workflow tools. Never use Fable models; implement with Sonnet 5 or Opus, review with Opus.

Step 0 — load skills with the Skill tool: `engineering-advanced-skills:ci-cd-pipeline-builder`, `github-actions-hardening`, `github-actions-efficiency`, `superpowers:verification-before-completion`, `engineering-skills:adversarial-reviewer`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D10), every file listed by `ls ~/cf-wt/v25-execution/status/` (S01 DONE; if D1 = B write `RESULT: NOT NEEDED (D1 = B)` and stop). Read `.github/workflows/ci.yml`, root `package.json`, and the pipeline package's `package.json` (its `test` script: `CHAPTERFLOW_NO_API_CODEX_QC=1 tsx tests/run.ts`) and lockfile. Check how the pipeline package installs (it has its own lockfile; `wt.sh new` runs `npm ci` inside it) and what the suite needs from the environment: it prints `xenv(env-absent) 6` locally because the tracked gold corpus is absent in worktrees — find what that corpus is and whether CI's checkout has it; make sure no test needs network, API keys, `~/cf-canary`, or macOS-only tools.

Step 2 — `~/cf-wt/franklin-v7-tools/wt.sh new ci-v25-tests`; add a job "v25 Pipeline Typecheck + Tests" to `ci.yml`: `permissions: contents: read`; pinned action versions consistent with the file; Node 20; npm cache keyed on the pipeline lockfile; `npm ci` in the pipeline dir; `npm run typecheck` and `npm test` there with `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` explicitly empty; a `timeout-minutes` with headroom (~2x the measured local 15 min); make sure the job's final log shows the runner's `pass N  fail M` line (two spaces); fail the job on any `fail` > 0 (the runner exits non-zero — confirm). Run it on pull requests and pushes to main; use a paths filter only if the hardening/efficiency skills and the repo's required-checks setup make that safe (a filtered required check never reports) — explain the choice in the PR. Keep the legacy v21 job untouched.

Step 3 — `wt.sh push ci-v25-tests`, `wt.sh pr ci-v25-tests "ci: run the v25 book-pipeline typecheck and tests" <body>` (look up the PR number). Wait for the PR's own CI run (poll `gh pr checks <n>` with repeated calls each under 10 min; foreground sleep is blocked); paste the new job's result and the final `pass N  fail 0` line (two spaces) from its log (`gh run view --log` for that job). If it fails for an environment reason, fix the workflow (not the tests) and re-run; if a test genuinely depends on the local machine, list it and stop for me rather than skipping it.

Step 4 — one Opus reviewer (Agent tool) reviews the workflow diff with the github-actions-hardening lens (injection, permissions, pinning, secrets exposure) and efficiency lens; address must-fix. Merge per D10 or list for me. Write `~/cf-wt/v25-execution/status/S10.md` (first line `RESULT: …`; PR number/URL, the CI run URL, job duration, the pass/fail line verbatim, reviewer verdict). Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: the PR's own CI run shows the new v25 job green with `pass N  fail 0` in its log; hardening review PASS; status/S10.md written.
---END---
