# S06 — Reader-panel blocking rule per the owner's decision D2 (+ D3)

Run on: Opus 5.5 (or Opus 5), high/xhigh effort. Mode: autonomous, adversarial-workflow PR. Depends on: S01, D2, D3. Parallel with: S02–S05, S09a/b, S10.
Must be merged before S11. Estimate: 4–6 h wall; ~$60–90 session quota. No pipeline model calls.

---PROMPT---
You are executing task S06 of the ChapterFlow v25 pipeline completion plan: implement the owner's chosen reader-panel blocking rule so a book can actually pass the panel, as one PR through the adversarial fix workflow, with a replay proof over every stored panel of the live run. I explicitly authorize multi-agent orchestration with the Workflow tool. Implementers and reviewers run on Opus only (never Fable models). This is an owner-approved gate change — cite the decision id in the PR; change nothing beyond what the decision states.

Step 0 — load skills with the Skill tool: `workflow-authoring`, `engineering-advanced-skills:spec-driven-workflow` (write the acceptance spec first), `superpowers:test-driven-development`, `engineering-skills:adversarial-reviewer`, `superpowers:verification-before-completion`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D1, D2, D3, D10), and every file listed by `ls ~/cf-wt/v25-execution/status/` (S01 DONE). Stop early when: D1 = B → `RESULT: NOT NEEDED (D1 = B)`; D2 blank without accepted defaults → BLOCKED; D2 = B → `RESULT: NOT NEEDED (owner kept the union rule)`; D2 = A-semantic or D3 = B → BLOCKED with a design question (A-semantic needs an adjudicator call whose cost I must approve; D3 = B has no deterministic hook — the "stands alone" findings are ordinary `structurally_invalid` findings). Then read `assessment/reports/panel-analytics.md` (all of it) and `assessment/reports/verify-plan-critic.md` rows 12–13, and run `python3 ~/cf-wt/v25-execution/tools/corrob.py` (read-only; note its "ch+cat>=2" column counts GROUPS, not blockers, and ignores D2's exceptions).

Facts (verified at 9f0117cb7): any single seat's BLOCKER blocks — `semanticPanelReviewEvaluator.ts` ~361-364 ("ANY seat's … blocking finding blocks (union, fail-closed)"), outcome at ~389-393, per-seat findings unioned in `laneOrchestrator.ts` ~425-429; median floor `AUTHOR_CHAPTER_BAR = 70` (~328-336) never fired; the quiz-key rule already requires a strict majority (`panelQuizAdjudication.ts` ~111, ~118) — the in-code precedent. Blocking categories: unsafe, internal_contradiction, structurally_invalid, schema_or_app_breaking, unusable (`src/contracts/readerExperienceReview.ts` ~32). Stored seat-finding locations look like `ch04/seat-cold/<unit>` (the unit may itself contain " / "; split on the first two "/"). Quiz adjudication emits the SAME code `READER.BLOCKING.structurally_invalid` at `chNN/quiz/<questionId>` (`panelQuizAdjudication.ts` ~44): it is a strict-majority verdict, not a seat finding, and must stay a BLOCKER.

Required change (branch `v25/panel-corroboration-rule`), exactly as D2 says:
- D2 = A: a seat's `READER.BLOCKING.<category>` finding is a BLOCKER only when ≥2 distinct seats raise the same category on the same chapter; otherwise it is recorded as WARN `READER.SINGLE_SEAT.<category>` with the same message and location (nothing is lost from the review record). Single-seat findings still block for `unsafe` and `schema_or_app_breaking`.
- D2 = A2: as A, but only `schema_or_app_breaking` still blocks on a single seat.
- Put the rule in ONE pure exported function that the evaluator and the replay both call, and apply it to `panel.blockingFindings` inside the evaluator's chapter loop (~362-364) — never as a post-pass over issue codes (that would catch quiz verdicts). Unchanged: the baseline structural review, `BELOW_FLOOR`, quiz adjudication, ERROR on seat failure. The QC-repair lane keeps calling this same evaluator, so it inherits the rule — do not fork a union-rule copy for it.

Acceptance spec first (write it into the PR body and a test):
(1) unit tests for the rule function: two seats, same chapter + category → BLOCKER; one seat → WARN code; the D2 exceptions; different chapters → no corroboration; a quiz-adjudication finding at `chNN/quiz/…` stays a BLOCKER.
(2) a REPLAY test over the 14 panel reviews of run `book-run-39a37d06-59c8-430a-87fe-3ad3b19a1c14` (ids in `tools/corrob.py`; read-only from `~/cf-canary/books/the-autobiography-of-benjamin-franklin/reviews/`). Put a trimmed fixture in the repo — each review's reviewId, outcome and BLOCKER issues (code, severity, location, message) only; the full JSONs are 2.8 MB. The replay rebuilds {seat, category, chapter} only from locations whose second segment starts with `seat-`, calls the pure function, and prints per panel: new outcome, BLOCKERs kept / downgraded.
Expected table under D2 = A (re-derive it yourself before coding and make the replay match exactly; format outcome kept/downgraded): P1 FAIL 20/14; P2 FAIL 16/11 (single-seat unsafe ch12, ch16); P3 FAIL 5/16; P4 FAIL 6/11 (single-seat schema ch05); P5 FAIL 2/11; P6 FAIL 4/15; P7 FAIL 3/12 (unsafe ch06); P8 FAIL 5/4 (schema ch06); P9 FAIL 11/7 (unsafe ch05); P10 FAIL 1/8 (only the single-seat unsafe ch06); P11 FAIL 2/8; P12 PASS 0/15; P13 FAIL 3/13; P14 ERROR (2 SEMANTIC_PANEL_READER_FAILED) 0/5. Under D2 = A2, compute the table the same way (single-seat unsafe findings become WARN; P10 then passes) and put it in the spec. List every single-seat `unsafe` finding that A2 downgrades (chapter, seat, message) in the PR body for the owner.
Update any existing test that asserted the union rule; list each in the PR as "changed by owner decision D2".

Step 2 — copy `~/cf-wt/v25-execution/templates/adversarial-fix-workflow.js` to your session scratchpad, fill CONSTANTS (WT `panel-corroboration-rule`, TITLE `feat(v25): a reader-panel finding blocks only when two seats corroborate it (owner decision D2)`, RED_EXPECT = the new rule-function tests failing on the union code, lenses: "correctness" (rule exactly as D2 states, deterministic matcher, the exceptions, quiz verdicts untouched, the replay table matches the spec) and "gate-safety-minimality" (nothing beyond D2 changed; baseline, floor, quiz, ERROR paths untouched; WARNs preserved)), fill TASK as CONTEXT.md §6 "Filling the template's TASK" says, and run it (Workflow tool, scriptPath). If it returns passed:false, follow CONTEXT.md §6.

Step 3 — verify the PR yourself (re-run the replay and paste the table), merge per D10 or list it for me. Write `~/cf-wt/v25-execution/status/S06.md` (first line `RESULT: …`): PR number/URL (looked up), SHA, the replay table, the downgraded single-seat `unsafe` list (A2), RED/GREEN/suite lines verbatim, which tests changed and why. Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: PR with two reviewer PASSes (RED at the base reproduced); the replay over the 14 stored panels matches the spec table exactly; quiz verdicts provably still block; full suite fail 0; status/S06.md written.
---END---
