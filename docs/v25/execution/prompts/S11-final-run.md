# S11 — Merge gate, then the final Franklin run ridden to promotion (runs ALONE)

Run on: Opus 5.5 (or Opus 5). Mode: autonomous, long-running (1.5–3 days wall, possibly across a quota reset), self-paced with /loop.
Depends on: S01, S03, S04, S05, S06 DONE/merged; S08 merged if D13 set its scope to B or C; S07 DONE, NOT NEEDED, or its NEEDS-OWNER answered;
S09a + S09b merged if D6 = A; D4, D6, D12, D13 filled (D14 before S12). S10 is not a gate.
No other Claude sessions or agent workflows should run while this one rides the run (D9). Start within 24 h after a Tuesday 23:00Z quota reset.
Estimate: pipeline ~$700–1,100 API-eq realistic (fresh compile ~$120; ~$32 per review-repair round; fresh QC ~190 calls; QC-repair links
~263 calls each), session ~$50–100. Keep the Mac awake (`caffeinate -dis` in a spare terminal) and this terminal open.

---PROMPT---
You are executing task S11 of the ChapterFlow v25 pipeline completion plan: verify the merge gate, launch the final Franklin run on main code, and ride it — diagnosing and fixing any wedge through the established process — until promotion completes or an owner fallback applies. I explicitly authorize multi-agent orchestration with the Workflow tool, and the pipeline's own model calls through the driver. Fix agents run on Opus only (never Fable models). Never run `publish-final`.

Step 0 — load skills with the Skill tool: `loop`, `superpowers:systematic-debugging`, `workflow-authoring`, `superpowers:verification-before-completion`, `prep-compact`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8). If your context gets long, use `prep-compact` to write the handoff into `~/cf-wt/v25-execution/status/S11.md` before compaction.

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D1, D4, D6, D9, D10, D12, D13), every file listed by `ls ~/cf-wt/v25-execution/status/`, and `~/cf-wt/franklin-v7-tools/RUNBOOK.md` (from S04). If D1 = B, or D4 = C: write `RESULT: NOT NEEDED (<reason>; apply D12-A to rr21)` and stop. First `touch ~/cf-wt/franklin-v7-tools/PAUSE`, then bring the canonical checkout up to date (only S01 moved it; later PRs merged on origin only): `git -C ~/ChapterFlow-books-v25-completion fetch origin && git -C ~/ChapterFlow-books-v25-completion checkout main && git -C ~/ChapterFlow-books-v25-completion pull --ff-only` (if git refuses because untracked files sit at incoming paths, move those files to `~/cf-wt/v25-execution/untracked-originals/` and retry). Merge gate — verify each and paste evidence; if any fails, write status BLOCKED listing exactly what is missing and stop:
- Every PR named in status S03, S05, S06 (and S07, S08, S09a, S09b when they applied) is MERGED (`gh pr view <n> --json state,mergedAt`).
- The canonical checkout is on `main`, equal to `origin/main`, with a clean `src/` and `config/`.
- A local full suite on that commit: `git -C ~/ChapterFlow-books-v25-completion worktree add --detach ~/cf-wt/gate-main origin/main`, `cmp` both package-lock.json files, symlink root and pipeline `node_modules` from the canonical checkout, run typecheck and `npm test` in the background (~15 min), and paste the `pass N  fail 0` line (two spaces) and `v25-subprocess-suite: exit=0`; then `git worktree remove ~/cf-wt/gate-main`. If S10 has merged, the v25 CI job must also be green on that commit.
- `tools/detqc.mts` (run exactly as its header says, PIPE_SRC on the canonical checkout) against a copy of the rr21 candidate shows `UNLOCATED blockers: 0`.
- The driver changes from S04 are in place: `DRY_RUN=1` prints the book-run command with the expected knobs; `PAUSE` exists.
- D4 and D6 have CHOICE lines (or defaults are accepted and S02's composite makes the D4 default apply).
- Quota: it is within ~24 h after a Tuesday 23:00Z reset (if not, write `RESULT: WAITING-FOR-RESET` with the next reset time and stop), and no other interactive Claude Code sessions are running (record `pgrep -fl claude`; pipeline `claude -p` children do not count; if others run, note it and proceed only if D9's CHOICE allows).

Step 2 — launch per D6 using the exact commands in RUNBOOK.md, with `CHAPTERFLOW_RUBRIC_BAR` per D4 and `CHAPTERFLOW_OPERATOR_COMPILE_RETRIES`, `CHAPTERFLOW_REVIEW_REPAIR_ORDINALS`, `CHAPTERFLOW_QC_REPAIR_RUNS` per D13:
- D6 = A → FRESH run: no RESUME_RUN_ID; `RESEARCH_RUN_ID=20260918T123418217Z-0e835cda-3c5b-4984-8a0b-105634df246a` (so the 19-chapter map matches S09b's pins); LOG_PREFIX `fv9a`; all section packs recompile because prompts/scars changed. When the research phase COMPLETES, compare the new run's `source-freeze`/chapter map (chapter numbers, span starts and ends, titles) with S09b's checked-in fixture map; if they differ, touch PAUSE, kill the driver pids one by one, and write BLOCKED with the diff.
- D6 = B → RESUME `book-run-39a37d06-59c8-430a-87fe-3ad3b19a1c14` (needs S03 merged for the stored provider-blocked successors) with LOG_PREFIX `fv9a`.
Write the `=== FRESH|RESUME <utc> on <sha> LOG_PREFIX=fv9a ===` marker, launch with nohup/disown as the runbook says, confirm the process and the first round-log line, and record the run id from the intake event. Rewrite `AUTORESUME.env` for this run (RESUME_RUN_ID, LOG_PREFIX `fv9a` — autoresume.sh appends `-a<N>` itself — and the same knobs), then remove `PAUSE` so crash recovery works.

Step 3 — ride it with the loop skill in dynamic mode (no interval), invoked with exactly: "Continue task S11: re-read ~/cf-wt/v25-execution/prompts/S11-final-run.md Steps 3-5 and ~/cf-wt/v25-execution/status/S11.md, check the Franklin run, act, and log to status/S11.md". Each wake-up: read the driver out file markers, the newest round-log tail, and new events in `~/cf-canary/book-run-events/the-autobiography-of-benjamin-franklin.jsonl`; append one dated line per milestone to `status/S11.md` (research, compile rounds, editor, each review verdict with blocker count, each repair, fresh QC verdict + blocker codes, QC-repair links, rubric verdict + factor medians, promotion). Pick wake-up delays from the stage (compile rounds ~15–60 min, panels ~1 h, fresh QC 3–7 h); never poll faster than the stage can change.

Step 4 — when the driver stops, classify with the runbook:
- PROVIDER BLOCK → the usage limit: wait until the reset time in the message; apply D13's knob-raise authority for any ordinal the block provably burned; then resume per the runbook. Record it.
- `REPAIR_DIAGNOSIS_REQUIRED` / DIAGNOSIS GATE → run the `qc-diagnose` command from the runbook (deterministic), then resume.
- Rubric below bar → apply D4 (a composite-only FAIL is re-judged for free at a new bar). A factor-floor, churn or correctness FAIL/SPLIT is final for the candidate → apply D12.
- QC-repair budget exhausted, or quota exhausted before promotion → apply D13, then D12.
- A second disputed review ("start a fresh run") → D13: A = apply D12; B = land a small PR making `MAX_DISPUTED_REVIEW_SUPERSESSIONS` an env knob (default 1, range 1–3, #574 precedent) through the adversarial fix workflow, then resume with 2.
- WEDGE STOP or any other new fail-closed error → `touch PAUSE`; diagnose with systematic-debugging from the run state, events and review JSONs (read-only); reproduce in a hermetic test; fix with the adversarial fix workflow (`~/cf-wt/v25-execution/templates/adversarial-fix-workflow.js`, new worktree via wt.sh, RED at origin/main); merge per D10 (or list it for me and wait); `git checkout main && git pull --ff-only` in the canonical checkout; resume (the run tolerates a new sha: REOPENED_UNDER_DIFFERENT_SOURCE_SHA). Number the defect (#21, #22, …) in status/S11.md with the evidence and the PR.
- If the decision needed is blank, write NEEDS-OWNER with the evidence and stop. Never weaken a gate to get past a failure.

Step 5 — stop riding when the events log shows `promotion COMPLETED` (pointer 6 → 7), or when D12-A applies, or when you are blocked on me. Write the final section of `status/S11.md`: run id, final candidate id (and, under D12-A, the candidate S12 must evaluate: the last one that passed the panel or QC, else the latest), the verdict timeline, total model calls and wall time, defects found and PRs, and the exact release (reader-package) command printed by the run for S12. `touch PAUSE` again so nothing relaunches. Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: promotion COMPLETED for the Franklin run on main code (evidence: the events line and `~/cf-canary/books/the-autobiography-of-benjamin-franklin/current.json` revision advanced), or D12-A applied with the candidate id recorded, or a precise NEEDS-OWNER/BLOCKED/WAITING-FOR-RESET status with evidence; status/S11.md complete; PAUSE present at the end.
---END---
