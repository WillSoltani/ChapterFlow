# S02 — Two cheap probes: does the source-fidelity judge catch the known errors, and what would the rubric score?

Run on: Opus 5.5 (or Opus 5). Mode: autonomous. Depends on: S01 (main landed). Parallel with: S03–S06, S09a/b, S10.
Feeds: D4 (rubric bar) and whether S07 is needed. Estimate: 2–3 h wall; ≤ 15 pipeline model calls (~$5–15) + session quota.

---PROMPT---
You are executing task S02 of the ChapterFlow v25 pipeline completion plan. Goal: measure, on the real latest Franklin draft, two things nobody has ever run on post-09-02 code — (1) how well the source-fidelity judge catches the source errors we already verified by hand, and (2) what the whole-book rubric panel scores — so the owner can set the rubric bar (D4) and we know whether the accuracy lane needs work (S07). This is a measurement task: do not change pipeline code on any branch that will be merged. I explicitly authorize up to 15 real pipeline model calls through the claude-cli route for this probe, and multi-agent orchestration with the Workflow tool. Never use Fable models. If D1 = B, still run this task.

Step 0 — load skills with the Skill tool: `engineering-skills:senior-prompt-engineer` (you are evaluating an LLM judge against a labeled set), `superpowers:systematic-debugging`, `engineering-advanced-skills:self-eval`, `superpowers:verification-before-completion`. CONTEXT.md overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md`, every file listed by `ls ~/cf-wt/v25-execution/status/` (S01 must be DONE: origin/main contains the combined tree). Then read `assessment/reports/source-accuracy.md` §4 (the per-chapter error lists with source line numbers) and `assessment/reports/verify-accuracy-content.md` §1–2 (which of those were re-confirmed: 14 of 16 majors confirmed; ch13 trustee = debatable severity; ch19 decanter = unsupported, not contradicted; ch07 q05 and q09 keys wrong; ch19 q01 explanation wrong).

Step 2 — build the labeled set: `~/cf-wt/v25-execution/probes/S02/labels.json` = every confirmed major error and wrong quiz key in ch01, ch07, ch13, ch19 of candidate `review-repair-21-candidate-06d7596afd1f14dccd3eb2df99b22db6`, each with chapter, field, the chapter quote, and the source line range. Keep "debatable" and "unsupported" items in a separate list (they count for precision, not recall).

Step 3 — build a scratch harness (outside every git worktree, under `~/cf-wt/v25-execution/probes/S02/`), importing from the canonical checkout's `src/` on main like `tools/detqc.mts` does. Copy the candidate (and whatever inputs the judge needs: frozen source text, chapter map — they live under the candidate's `content/inputs/`) into a scratch books root; point every run-state/attempt root at scratch directories. Never write under `~/cf-canary`.
- Probe A — source-fidelity judge ONLY (not the whole CandidateQcEvaluator: with a runner it would also fire 171 answer-key calls). Find how the production composition builds the runner and the judge (`src/app/bookRunComposition.ts`, `src/app/candidateQcEvaluator.ts` ~649 and ~677-700, `src/critics/semantic/sourceFidelityJudge.ts` ~66-75) and call the judge for ch01, ch07, ch13, ch19 with the production route/effort from `config/model-routing.json` (role qc). 4 calls + at most 4 retries.
- Probe B — the catalog rubric panel on the whole candidate (`src/app/catalogRubricPanelEvaluator.ts`, `src/review/catalogRubric.ts`; 3 readers on the md5-seeded chapters [0,6,12,18]) with the production route. 3 calls + at most 3 retries. Judge the stored aggregate against bars 80, 75 and 70 with the pure judge function (no extra calls).
- Strip API keys (`env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY`); the claude-cli route uses the subscription login. If a call returns a usage-limit error, stop immediately and report — do not retry.
- If building either probe needs more than ~60 lines of glue or a production code change, stop that probe and report what is missing (that itself is a finding for S07/S11).

Step 4 — score the probes honestly:
- Probe A: recall = confirmed majors + wrong keys the judge flags (same claim, any wording) ÷ labeled total; precision = judge BLOCKERs that are real (check each against the cited source lines yourself) ÷ judge BLOCKERs; blockers per chapter; tokens and wall time per call (from the CLI transcripts under `~/.claude/projects/-Users-radinsoltani-*` for your scratch attempt root).
- Probe B: per-reader correctness verdicts, the ten factor medians, composite, churn, verdict at 80/75/70, tokens and time.
- Kill criteria (decide, do not soften): S07 is REQUIRED if recall < 50% or the judge averages > 5 blockers per chapter or precision < 60%. Flag D4 if the composite < 74, any factor median < 65, or the correctness vote is SPLIT/FAIL; for a SPLIT/FAIL correctness vote or a factor median below 65, recommend D4 = C (the bar cannot fix either; both are final per candidate). Also report any judge or rubric call that ended at its output cap or with MODEL_OUTPUT_INVALID (thinking to the cap): fresh QC makes ~190 such calls in sequence.

Step 5 — write `~/cf-wt/v25-execution/status/S02.md` (first line `RESULT: …`): the two tables, verbatim key output lines, the kill-criteria verdicts, a one-paragraph recommendation for D4 and for S07 (required / not required, and if required, which failure mode: low recall, over-flagging, or missing inputs), exact model-call count and cost estimate, and the harness file paths, and one line telling the owner to fill D4 in DECISIONS.md now. Put raw outputs in `probes/S02/`. Add one line under "Progress" in memory file `v25-status-assessment-2026-09-23.md`.

Done means: labels.json exists with source line cites; both probes ran (or a precise reason each could not); recall/precision and the rubric numbers are computed from saved raw outputs; kill-criteria verdicts stated; no file under ~/cf-canary or any repo worktree changed (show `git -C ~/ChapterFlow-books-v25-completion status --porcelain` is unchanged); status/S02.md written.
---END---
