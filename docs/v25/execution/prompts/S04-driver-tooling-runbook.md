# S04 — Driver, autoresume and scoring tooling fixes + the operator runbook

Run on: Sonnet 5 implementer + Opus reviewer (or all Opus). Mode: autonomous. Depends on: S01. Parallel with: S02, S03, S05, S06, S09a/b, S10.
Must be DONE before S11. Estimate: 2–4 h wall; ~$20–40 session quota. No pipeline model calls.

---PROMPT---
You are executing task S04 of the ChapterFlow v25 pipeline completion plan: make the Franklin driver, the launchd autoresume kit and the scoring workflow correct for the final run, and write the operator runbook. These files live in `~/cf-wt/franklin-v7-tools/`, which is NOT a git repository, so there is no PR: you snapshot, change, test with fixtures, and get an independent Opus review. I explicitly authorize multi-agent orchestration, including the Workflow tool and the Agent tool. Never use Fable models; implement with Sonnet 5 or Opus, review with Opus.

Step 0 — load skills with the Skill tool: `engineering-advanced-skills:runbook-generator`, `superpowers:systematic-debugging`, `superpowers:verification-before-completion`, `engineering-skills:adversarial-reviewer`, `workflow-authoring`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D6, D13), every file listed by `ls ~/cf-wt/v25-execution/status/` (S01 DONE; this task runs even if D1 = B, because S12 needs the scoring fix — then do item 4 and the scoring section of the runbook only; if S03 is DONE, read its exact new terminal token), and `assessment/reports/downstream-readiness.md` §3 and §6 items 6 and 8, `verify-gates-downstream.md` rows 20–23, `verify-panel-and-wedge.md` row 4f. Then read `drive-franklin-v7.sh`, `autoresume.sh`, `AUTORESUME.env`, `AUTORESUME.env.example`, `AUTORESUME.md` and `score-franklin-v7.js` in full.

Step 2 — snapshot: `cp -Rp ~/cf-wt/franklin-v7-tools ~/cf-wt/franklin-v7-tools.bak-$(date -u +%Y%m%dT%H%MZ)`. Confirm `~/cf-wt/franklin-v7-tools/PAUSE` exists (S01 created it); create it if not. Do not start the driver in this task.

Step 3 — changes (minimal, each with a fixture test you run):
1. `provider_block()`: today it greps a line offset over a glob-ordered concatenation of all `runs/*/attempts.jsonl` (new records land mid-list, so it reads OLD lines) and has no 429 pattern. Replace with: write a marker file at round start; after the round, scan only files newer than the marker — `runs/*/attempts.jsonl`, `books/<id>/reviews/*.json`, and the round log — with the extended regex `hit your (weekly|session|usage) limit|api_error_status\\?"?:?429|api_error_status=429|BOOK_RUN_PROVIDER_BLOCKED|Not logged in` (no bare "hit your" or "usage limit": review JSONs and round logs contain reader-facing text); on a hit write `=== STOP <utc> PROVIDER BLOCK: <first matching line, trimmed> ===` to the driver out file and exit non-zero. Confirm autoresume treats that marker as stay-stopped (a `=== STOP` marker already is — verify in `autoresume.sh`).
2. Knobs, in BOTH the fresh and the resume branches: replace the inline `CHAPTERFLOW_REVIEW_REPAIR_ROUNDS=4 CHAPTERFLOW_QC_REPAIR_RUNS=4` with `${CHAPTERFLOW_REVIEW_REPAIR_ROUNDS:-4}` / `${CHAPTERFLOW_QC_REPAIR_RUNS:-4}`; pass `CHAPTERFLOW_REVIEW_REPAIR_ORDINALS` (default 40), `CHAPTERFLOW_OPERATOR_COMPILE_RETRIES` (default 50 — the code default of 20 was exhausted once, franklin-v7b-driver.out:73), `CHAPTERFLOW_CAPTURE_INVALID_OUTPUT` (default 1), and `CHAPTERFLOW_QC_JUDGE_RUNS`, `CHAPTERFLOW_RUBRIC_RUNS`, `CHAPTERFLOW_RUBRIC_BAR` when set. Keep `RESEARCH_RUN_ID` support unchanged. Add `DRY_RUN=1`: print the exact book-run command line (with env) instead of running it, then exit 0.
3. `autoresume.sh`: forward the same knobs from `AUTORESUME.env`; update `AUTORESUME.env.example` and `AUTORESUME.md` (note: autoresume appends `-a<N>` to LOG_PREFIX itself, so set the base prefix). Add a first-line comment to `AUTORESUME.env` marking it STALE (S11 rewrites it) so nobody reuses LOG_PREFIX=fv8d.
4. `score-franklin-v7.js` is a Workflow script (`export const meta`; inputs via `args` {pkg, source, label}; Workflow scripts have no filesystem or crypto access, so it cannot compute the chapter sample itself). It hard-codes chapters `[0, 1, 2, 3]` (~40-41) and calls the book "4-chapter" (~139). Make it REQUIRE `args.idxs` (array of 0-based chapter indexes) and `args.chapterCount` (throw a clear error if either is missing), use them in the reader prompt, the extraction command and the churn prompt, and state the real chapter count. The caller computes idxs with `python3 ~/ChapterFlow/.claude/skills/book-score/score.py the-autobiography-of-benjamin-franklin --path <PKG>` (its `seeded_idxs`; Franklin with 19 chapters → [0, 6, 12, 18]); put that command and the full Workflow `args` object in the runbook.
Tests (no model calls): build fixtures under `~/cf-wt/v25-execution/probes/S04/` from copies of `~/cf-canary/fv8h-r1.log`, `reviews/review-35abdd05c52c1642a511756a397ecd6e.json` and one successor `attempts.jsonl`; prove the new scan fires on them, does not fire on a clean round's copies, and does not fire on a review JSON whose reader text contains "hit your target" (make that fixture); prove the knob defaults and pass-through with `DRY_RUN=1` in both branches (FRESH with `RESEARCH_RUN_ID` set, and RESUME); prove `score.py` prints `seeded_idxs` [0, 6, 12, 18] for a 19-chapter fixture package (build a minimal `{chapters:[…19…]}` JSON if needed); prove the edited `score-franklin-v7.js` still parses (copy it, wrap the body in `async function w(){…}`, rename to .mjs, `node --check`) and that `command grep -n '0, 1, 2, 3' score-franklin-v7.js` prints nothing. Do not run the scoring workflow.

Step 4 — write `~/cf-wt/franklin-v7-tools/RUNBOOK.md` with the runbook skill's structure, specific to this pipeline: pre-flight checklist (checkout on main, equal to origin/main, clean `src/` and `config/`; PAUSE present; quota window; no other sessions; Mac awake — `caffeinate -dis` in a spare terminal); launch FRESH per D6 with `RESEARCH_RUN_ID=20260918T123418217Z-0e835cda-3c5b-4984-8a0b-105634df246a` and RESUME (exact commands with LOG_PREFIX, knobs, nohup/disown, and the `=== FRESH|RESUME <utc> on <sha> LOG_PREFIX=<p> ===` marker); the autoresume install/PAUSE/unpause procedure; how to read progress (driver out markers, round logs `~/cf-canary/<prefix>-r<N>.log`, the events jsonl); what each stop means (WEDGE STOP, PROVIDER BLOCK, DIAGNOSIS GATE / `REPAIR_DIAGNOSIS_REQUIRED` → run `qc-diagnose` — find the exact invocation in `src/cli.ts` and run its usage/help — rubric below bar → DECISIONS D4/D12); how to stop safely (touch PAUSE, kill pids one by one, resume later with `--reconcile-unsettled`, which the resume branch passes); the release and scoring hand-off (D14 categories/tags from the tracked rev-6 package; the score.py + Workflow args). Every command in the runbook must be one you ran or dry-ran.

Step 5 — independent review: spawn one Opus reviewer (Agent tool) with lenses "does the driver still stop on wedges, dirty tree, missing source; does the new provider scan fire exactly on new records and never on reader text; are fresh and resume branches identical except for the resume flags; does the runbook match the scripts". Fix any must-fix, re-run the fixture tests.

Step 6 — write `~/cf-wt/v25-execution/status/S04.md` (first line `RESULT: …`): backup path, a unified diff of each changed file (`diff -u <backup> <new>`), fixture test outputs verbatim, the reviewer verdict, and the exact FRESH/RESUME commands S11 should use. Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: backup exists; the four changes are in place with fixture proof pasted; RUNBOOK.md exists and every command in it was run or dry-run; reviewer PASS; PAUSE still present; status/S04.md written.
---END---
