# S08 — Harden the never-run downstream lanes against usage-limit blocks (OPTIONAL per D13)

Run on: Opus 5.5 (or Opus 5). Mode: autonomous, adversarial-workflow PR. Depends on: S01, and S03, S05, S09a MERGED (same service file,
predicate, repair port and its tests). Runs only if DECISIONS D13 sets S08 scope to B or C. Parallel with: S02, S04, S10.
Must be merged before S11 when it runs. Estimate: 4–6 h wall; ~$60–90 session quota. No pipeline model calls.

---PROMPT---
You are executing task S08 of the ChapterFlow v25 pipeline completion plan. Fresh QC, the QC-repair lane and the rubric gate have never run on post-09-02 code, and each burns a bounded budget when the Claude usage limit hits — the same class as defect #20, which S03 fixed for the review lane only. Extend S03's mechanism to the remaining lanes as one PR (two if it grows large) through the adversarial fix workflow. I explicitly authorize multi-agent orchestration with the Workflow tool. Implementers and reviewers run on Opus only (never Fable models).

Step 0 — load skills with the Skill tool: `workflow-authoring`, `superpowers:systematic-debugging`, `superpowers:test-driven-development`, `engineering-skills:adversarial-reviewer`, `superpowers:verification-before-completion`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D1, D10, D13), and every file listed by `ls ~/cf-wt/v25-execution/status/`. If D1 = B or D13's S08 scope is A (skip): write `status/S08.md` with first line `RESULT: NOT NEEDED (<reason>)` and stop. If S03, S05 or S09a is not MERGED yet: write `RESULT: BLOCKED (waiting for <task> to merge)` and stop. Read S03's PR diff (`gh pr diff <n>`) to reuse its predicate `isProviderBlockedReview`, its provider classification, and its terminal token. Read `assessment/reports/quota-defect.md` §3, `assessment/reports/downstream-readiness.md` §2–§3 and §6 items 3 and 5, and `assessment/reports/verify-plan-critic.md` rows 14 and 16. Re-locate every cited line on current main by symbol.

Required change (minimal; branch `v25/provider-block-downstream-lanes`). Scope B = items 1–3; scope C = items 1–6.
1. Fresh-QC judge walk (`bookRunApplicationService.ts` ~2180-2269; `candidateQcEvaluator.ts` ~710-722, ~889-904): a judge failure whose error chain classifies via `providerBlockKind` ends the run with S03's `BOOK_RUN_PROVIDER_BLOCKED:<kind>` token, and the FAILED judge run's durable reason records the classification (today it is only `evaluation.error.code`, service ~2257 and `bookRunComposition.ts` ~681 — the walk can read nothing else); the next resume walks past such a run without counting it against `MAX_QC_JUDGE_RUNS` (bounded, same pattern as S03).
2. Rubric walk (~2370-2487; `catalogRubricPanelEvaluator.ts` ~279-287): same treatment against `MAX_RUBRIC_RUNS`. Rubric ordinal ids derive from the candidate, so a burned ordinal would survive into a fresh run on the same candidate.
3. Review-repair and QC-repair ordinal walks: a chapter-repair `REPAIR_MODEL_FAILED` caused by a provider block (`candidateRepairApplicationPort.ts` ~1492-1495) is recorded with a provider-blocked terminal reason that the existing `forgivableTerminalReason` walk skips uncounted (its own bound, not the 2 `MAX_FORGIVEN_INFRA_ORDINALS` slots), and the run exits with the token instead of continuing.
4. QC-repair successor judge (`bookRunComposition.ts` ~617-690) uses a fixed roundId + "-judge" run with no successor walk, so any judge failure costs the whole QC-repair ordinal (repair + a full panel): give it the same bounded walk the fresh-QC judge has, provider-blocked failures uncounted. No test references `REPAIR_QC_JUDGE_UNAVAILABLE` today — add one.
5. Disputed-review successor on a 429 (service ~1784-1800): a stored provider-blocked successor is treated as S03 treats review successors (not a verdict; a resume after the reset re-runs it), not "restore that successor review or start a fresh run".
6. QC-repair successor re-review (`contentRepairWorkflow.ts` ~240-270): a provider-blocked ERROR review must not be retried in the same invocation (today `MAX_REPAIR_REVIEW_ATTEMPTS=3` re-runs it against the same wall, and `REPAIR_REVIEW_ERROR` then uses a forgiveness slot); stop, record a provider-blocked terminal reason that the walk skips uncounted, and exit with the token.
Out of scope: changing any budget size, verdict, bar or gate; the driver (S04); the dispute bound itself. Not counting provider-blocked ordinals is the owner-mandated wedge fix (CONTEXT.md §1 item 2), not a budget change — say so in the PR body.

RED first, one hermetic test per item in scope (rig with a scripted runner that returns "You've hit your weekly limit · resets … (api_error_status=429)"): today each burns an ordinal or wedges; after the fix the run exits with the provider token, and a resume after the "reset" lands on the same or the next ordinal without spending. Also pin that a NON-provider failure in each lane still spends exactly as today.

Step 2 — copy `~/cf-wt/v25-execution/templates/adversarial-fix-workflow.js` to your session scratchpad, fill CONSTANTS (WT `provider-block-downstream-lanes`, TITLE `fix(v25): usage-limit blocks stop fresh QC, rubric and repair lanes instead of spending their budgets`, RED_EXPECT listing the items in scope, lenses: correctness (each lane: provider-blocked → stop + not counted; non-provider → unchanged; bounds hold across resumes) and gate-safety-minimality), fill TASK as CONTEXT.md §6 "Filling the template's TASK" says (note: write roundId + "-judge", never a dollar-brace), and run it (Workflow tool, scriptPath). If it returns passed:false, follow CONTEXT.md §6. If the non-test diff grows past ~600 lines, split items 4–6 into a second PR with the same workflow and say so.

Step 3 — verify, merge per D10 or list for me. Write `~/cf-wt/v25-execution/status/S08.md` (first line `RESULT: …`; PR numbers/URLs looked up, SHAs, RED/GREEN/suite lines verbatim, per-item before/after behaviour). Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: PR(s) with two reviewer PASSes (RED at the base reproduced) covering every item in scope; full suite fail 0; no budget/verdict/gate change; status/S08.md written.
---END---
