# S03 — Defect #20: a usage-limit block must stop the run, never burn review budgets; make it visible to the driver

Run on: Opus 5.5 (or Opus 5). Mode: autonomous, adversarial-workflow PR. Depends on: S01. Parallel with: S02, S04, S05, S06, S09a/b, S10.
Blocks: S08 (same file). Estimate: 3–5 h wall; ~$50–80 session quota. No pipeline model calls.

---PROMPT---
You are executing task S03 of the ChapterFlow v25 pipeline completion plan: fix defect #20 with the smallest change that obeys the owner's rules, as one PR, through the adversarial fix workflow. I explicitly authorize multi-agent orchestration with the Workflow tool. Implementers and reviewers run on Opus only (never Fable models).

Step 0 — load skills with the Skill tool: `workflow-authoring`, `superpowers:systematic-debugging`, `superpowers:test-driven-development`, `engineering-skills:adversarial-reviewer`, `superpowers:verification-before-completion`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults (worktrees via wt.sh only). Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D10), every file listed by `ls ~/cf-wt/v25-execution/status/` (S01 must be DONE; if D1 = B write `RESULT: NOT NEEDED (D1 = B)` and stop), and `assessment/reports/quota-defect.md` (the full trace, file:line per hop, and fix proposals F1/F2) plus `assessment/reports/verify-panel-and-wedge.md` rows 4a–4g.

What happened (verified): on 2026-09-20 14:55Z the Claude weekly limit hit during the round-3 reader panel. `semanticPanelReviewEvaluator` recorded `SEMANTIC_PANEL_READER_FAILED` BLOCKERs and returned ERROR; `reviewService` stored it durably; `reviewIsUncertain` treats ANY stored ERROR as uncertainty, so `#reviewSuccessor` minted successor-1 in the SAME invocation 13 ms later; its baseline reviewer hit the same 429 (`REVIEW_EVALUATOR_ERROR`), stored ERROR; two driver relaunches burned successors 2 and 3; `#successorLanding` skips stored-ERROR ordinals as spent; `MAX_REVIEW_SUCCESSOR_ORDINALS = 3` (a constant) → every resume fails closed "canonical review successor budget exhausted". The terminal line was only `BOOK_RUN_REVIEW_FAILED:canonical review outcome=ERROR`, and the durable attempt journal keeps a raw 400-char stdout head that (since CLI 2.1.265 puts `usage` before `result`) no longer contains the provider text, so the driver never saw a provider block.

Required change (minimal; one PR; branch `v25/provider-block-review-lane`):
1. A pure predicate `isProviderBlockedReview(review)`: an ERROR review in which AT LEAST ONE infrastructure BLOCKER (`SEMANTIC_PANEL_READER_FAILED`, `REVIEW_EVALUATOR_ERROR`) carries a message that the existing `providerBlockKind()` classifies (`src/runtime/modelErrors.ts` ~67). Inspect only those infrastructure codes (the live review-35abdd05 also holds 5 real READER.BLOCKING blockers).
2. When a review PRODUCED IN THIS INVOCATION (the canonical review, a review-repair re-review, or a successor) comes back provider-blocked, book-run does NOT mint a successor in that invocation; it returns a distinct failure `BOOK_RUN_PROVIDER_BLOCKED:<kind>: <provider message>` (the terminal line the driver will grep). A provider-blocked ERROR that `exactReview` REPLAYS from the store on a later flagged resume is NOT stopped: it is uncertainty and goes to the successor walk of item 3 (stopping on it would wedge run 39a37d06 forever). `exactReview` returns a replayed record and a fresh one identically today (~1057-1064 vs ~1165-1180), so carry the distinction explicitly (e.g. a `fresh` flag); never infer it from the record.
3. On a later resume, `#successorLanding` walks past stored provider-blocked ERROR ordinals WITHOUT counting them against `MAX_REVIEW_SUCCESSOR_ORDINALS`, under its own separate constant of 3 (same pattern as `forgivableTerminalReason`, but NOT `MAX_FORGIVEN_INFRA_ORDINALS`, which is 2; 3 is the minimum that recovers run 39a37d06, whose successors 1-3 are all provider-blocked). `#successorLanding` is also called by `#supersedeDisputedReview` (~1784): that path must end no worse than today. A non-provider ERROR (e.g. `MODEL_OUTPUT_INVALID`) still spends an ordinal; `--reconcile-unsettled` consent is still required; exhaustion still fails closed.
4. The durable attempt journal records the route's classified provider message in `terminalDetail` (`modelGateway.ts` ~498-525) only when `providerBlockKind()` classifies it — a sanitized, capped `providerBlock=<kind>;providerMessage=<text>` field; an `is_error` envelope that is not a provider block stays out — provider error text only, never model output — so any lane's provider block is greppable. Add a claude-route test fixture using the 2026-09-20 envelope key order (usage before result).
Out of scope (S08 per D13, or registered as deferred): QC-judge, rubric, QC-repair and review-repair ordinal walks; the disputed-successor path. Do not touch the driver (S04). Do not change any gate, bar or verdict rule. Walking past provider-blocked ordinals without counting them is the owner-mandated wedge fix (CONTEXT.md §1 item 2), not a budget change: the ceiling's size is unchanged and every verdict-bearing or non-provider ERROR ordinal still counts. Say so in the PR body.

RED first (in `tests/v25/v4-book-run-service-resilience.test.ts` with the existing rig; and the model-gateway / claude-route tests): (a) with `reconcileUnsettled: true` (a successor is minted in the same invocation only under consent; the driver's resume rounds pass it), a panel whose seats fail with "You've hit your weekly limit · resets … (api_error_status=429)" → today a successor is minted in the same invocation (assert zero successor STARTED events and the `BOOK_RUN_PROVIDER_BLOCKED` terminal line after the fix); (b) three stored provider-blocked successors + consent → today "budget exhausted"; after the fix the walk lands on successor-4; (c) a stored `MODEL_OUTPUT_INVALID` ERROR successor is still spent; (d) the gateway journal for a 429 envelope in the new key order contains "weekly limit"; the fixture must put `result` after character 400 like the live 1,192-byte envelope (its first 400 chars are in `~/cf-canary/run-state/books/the-autobiography-of-benjamin-franklin/runs/review-run-b1066b7e2305b9c9106999603290369d/attempts.jsonl`), otherwise the RED passes on main; (e) a replayed stored provider-blocked review on a later flagged resume goes to the walk, not to a stop.

Step 2 — copy `~/cf-wt/v25-execution/templates/adversarial-fix-workflow.js` to your session scratchpad, fill the CONSTANTS block (WT `provider-block-review-lane`, BASE `origin/main`, TITLE `fix(v25): a usage-limit block stops the book run instead of burning review successors`, RED_EXPECT naming (a)–(e), a correctness lens that checks the predicate cannot swallow a non-provider ERROR and that the live 09-20 record shapes under `~/cf-canary/books/the-autobiography-of-benjamin-franklin/reviews/review-35abdd05c52c1642a511756a397ecd6e.json`, `review-b1066b7e2305b9c9106999603290369d.json` satisfy it — read-only), fill TASK as CONTEXT.md §6 "Filling the template's TASK" says, and run it with the Workflow tool (scriptPath). If it returns passed:false, follow CONTEXT.md §6.

Step 3 — when both reviewers PASS and the PR is open: verify the PR yourself (diff scope, `gh pr checks`), merge per D10 (squash) or list it for me. Write `~/cf-wt/v25-execution/status/S03.md` (first line `RESULT: …`): PR number/URL (looked up), head SHA, RED/GREEN/suite lines verbatim, the new terminal token exactly as emitted (S04 greps for it), and the resume behaviour for run 39a37d06 (would it now resume? — state it as inferred unless tested). Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: PR open (or merged per D10) with two reviewer PASSes, RED reproduced by both at the base; `wt.sh verify` fail 0 pasted; no gate/verdict change; status/S03.md written with the exact terminal token.
---END---
