# S01 — Land the tested baseline on main and lock the workspace (sequential, FIRST)

Run on: Opus 5.5 (or Opus 5). Mode: autonomous. Depends on: D1, D7, D10 in DECISIONS.md. Blocks: every S02–S13 task (they branch from origin/main).
Estimate: 2–3 h wall, ~$40–60 session quota. No pipeline model calls.

---PROMPT---
You are executing task S01 of the ChapterFlow v25 pipeline completion plan. Goal: make `origin/main` contain exactly the tested combined pipeline code that the live runs used (local commit `9f0117cb7` = main `be9c44ed8` + PRs #566–#575), after an adversarial check of the three hand-resolved commits, and lock the workspace so nothing runs by accident. I explicitly authorize multi-agent orchestration with the Workflow tool. Run all agents on Opus only (never Fable models).

Step 0 — load skills with the Skill tool, in this order: `superpowers:verification-before-completion`, `workflow-authoring`, `engineering-skills:adversarial-reviewer`, `superpowers:finishing-a-development-branch`. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design; the rules in `~/cf-wt/v25-execution/CONTEXT.md` override every skill default (no interactive merge menus; worktrees only via wt.sh; merge authority per D10). You stop only by writing your status file.

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md` (all of it) and `DECISIONS.md` (D1, D7, D10); list `~/cf-wt/v25-execution/status/` with `ls` and read any files there. If D1, D7 or D10 is blank and `ACCEPT_RECOMMENDED_DEFAULTS` is not `yes`, write `status/S01.md` with first line `RESULT: BLOCKED` and the question, and stop. If D1 = B, do Steps 2–5 and 7, skip Step 6, and write in the status: "D1 = B: next S02, then S12 in CANDIDATE mode on rr21 (D12-A), then S13; S03–S11 NOT NEEDED."

Step 2 — lock and verify state (read-only except PAUSE):
- `touch ~/cf-wt/franklin-v7-tools/PAUSE`; confirm `pgrep -fl drive-franklin` is empty.
- In `~/ChapterFlow-books-v25-completion`: `git fetch origin`; confirm `git log --oneline origin/main..HEAD` lists exactly dfb1f6fb6 688ba6c71 7f3861708 71c19cc1e fb105fd00 24cbe9402 713536c11 a0429724d 27f3115c2 9f0117cb7 with HEAD at 9f0117cb7 and a clean `src/`/`config/`. If HEAD differs, stop BLOCKED with the exact output.
- If `origin/main` is no longer `be9c44ed8` (main is shared with web/backend work): if `git diff --stat be9c44ed8 origin/main -- scripts/book/prompts/chapterflow-v24-author-pipeline` is empty, create branch `v25/land-combined` at 9f0117cb7 in a worktree (`git -C ~/ChapterFlow-books-v25-completion worktree add ~/cf-wt/land-combined -b v25/land-combined 9f0117cb7`), merge `origin/main` into it (no conflicts allowed in the pipeline dir), confirm `git rev-parse HEAD:scripts/book/prompts/chapterflow-v24-author-pipeline` still prints `b43cb4be2e345f30129ef6be197f9bd811554489`, and use that commit as LANDING_SHA in Steps 4–5 (otherwise LANDING_SHA = 9f0117cb7). If the pipeline dir changed on main, stop BLOCKED with `git log --oneline be9c44ed8..origin/main -- scripts/book`.

Step 3 — adversarial review of the three hand-resolved commits (no PR branch contains these resolutions). Author a small Workflow with two Opus reviewers in parallel, lenses "semantic-equivalence" and "test-integrity". Each compares `git diff <X>^ <X>` with `git diff <PR head>^ <PR head>` for: 24cbe9402 vs #571 head e2af3eddc (src/cli.ts: union of `--reader-concurrency` and `--compile-concurrency` in usage, allow-list, parse, validation); 27f3115c2 vs #574 head 7e7da4517 (bookRunApplicationService.ts `#reviewSuccessor` gating `if (args.disputed === undefined && !reviewIsUncertain(review) && !contradiction) return review;` plus both `;reason=` suffixes; the resilience test keeps both the R-219 and R-288 blocks); 9f0117cb7 vs #575 head 8cd5499ce (`reconcileUnsettled: false` moved into the lifted `exactReview` call inside `#reviewSuccessor`; the rig keeps `await options.afterReviewRepair?.(staged)` plus `completedAgainstReview.set(...)` and `[REPAIRED_CHAPTER]`). Each reviewer states whether every behaviour of both sides survived and nothing else changed, citing file:line and pasting hunks. Prior evidence: `~/cf-wt/v25-execution/assessment/reports/open-ledger.md` §3.

Step 4 — full suite on LANDING_SHA: use `~/cf-wt/replay-cp-dry` if its HEAD is LANDING_SHA; otherwise create a detached worktree at LANDING_SHA as CONTEXT.md §5 describes (symlink node_modules after `cmp`). Run typecheck, then `npm test` in the pipeline dir in the background into a log (~15 min; CONTEXT.md §6). Expect a line matching `pass [0-9]+ +fail 0` (the runner prints two spaces; 3291 for 9f0117cb7) and `v25-subprocess-suite: exit=0`. Paste both.

Step 5 — land per D7, only if Step 3 has no must-fix and Step 4 is fail 0:
- D7 = A (fast-forward), allowed when D10 = B: `git -C ~/ChapterFlow-books-v25-completion push origin <LANDING_SHA>:refs/heads/main`. If D10 = A or the push is refused: print the exact push, verify, checkout and pull commands for me, SKIP the rest of Step 5 and all of Step 6 (do NOT check out main in the canonical checkout — it would put the run checkout on pre-fix code), write status `RESULT: NEEDS-OWNER`, and stop. After a successful push: verify `git ls-remote origin refs/heads/main` prints LANDING_SHA and `git rev-parse origin/main:scripts/book/prompts/chapterflow-v24-author-pipeline` prints `b43cb4be2e345f30129ef6be197f9bd811554489`. Confirm #566 shows MERGED on its own (same SHA); for each of #567–#575 (and #566 if not MERGED) run `gh pr close <n> --comment "Landed on main by fast-forward to <LANDING_SHA> (tested combination; full v25 suite <N>/0). The hand-resolved conflict for this PR was reviewed in task S01."`.
- D7 = B (per-PR): follow `assessment/reports/open-ledger.md` §4 "Option A" (its letters are swapped relative to D7), re-verifying after each conflicting step; stop BLOCKED if any resolution differs from the checkout commit.
- Then move the canonical checkout onto main: `git -C ~/ChapterFlow-books-v25-completion checkout main && git -C ~/ChapterFlow-books-v25-completion pull --ff-only`; confirm HEAD equals origin/main and `git status --porcelain -- scripts/book/prompts/chapterflow-v24-author-pipeline/src scripts/book/prompts/chapterflow-v24-author-pipeline/config` is empty.

Step 6 — record the plan in the repo: `~/cf-wt/franklin-v7-tools/wt.sh new docs-v25-execution`; copy into `docs/v25/` the four untracked files `S_TIER_EXECUTION_PLAN_2026-09-02.md`, `S_TIER_PHASE_A_REPORT_2026-09-02.md`, `S_TIER_ISSUE_REGISTER_2026-09-02.md`, `S_TIER_ISSUE_REGISTER_2026-09-02.json` from the canonical checkout's `docs/v25/`, plus `docs/v25/execution/` containing `README.md`, `CONTEXT.md`, `DECISIONS.md`, `templates/`, `tools/`, `prompts/` and `assessment/reports/` from `~/cf-wt/v25-execution/` (NOT `assessment/draft-rr21/`, NOT `status/`, NOT `probes/`). Before committing, run `npx eslint docs/v25/execution` from the worktree root and require 0 errors (the repo's "Lint Ratchet" CI job fails on any new error). Commit (`docs(v25): record the S-tier plan, the 2026-09-23 assessment and the execution kit`), `wt.sh push`, `wt.sh pr`; look up the PR number; merge per D10. Once merged, move the four originals out of the canonical checkout (`mkdir -p ~/cf-wt/v25-execution/untracked-originals` and `mv` each `docs/v25/S_TIER_*` file there — git refuses a fast-forward over untracked files at incoming paths even when identical), run `git -C ~/ChapterFlow-books-v25-completion pull --ff-only`, and paste that HEAD equals origin/main. Leave the kit at `~/cf-wt/v25-execution/` as the live working copy.

Step 7 — write `~/cf-wt/v25-execution/status/S01.md` (first line `RESULT: …`): the reviewers' verdicts with key evidence, the suite lines, origin/main's SHA now, which PRs were closed/merged (numbers looked up), the docs PR number, PAUSE present, and anything S02–S13 must know. Add a line under "Progress" in memory file `~/.claude/projects/-Users-radinsoltani-ChapterFlow/memory/v25-status-assessment-2026-09-23.md`: "S01 <date>: main = <sha> …".

Done means (verify each before claiming): origin/main contains the tested pipeline tree (hash above) — or the owner has the exact commands and the status is NEEDS-OWNER; canonical checkout on main and clean; the three resolutions reviewed with no must-fix; suite fail 0 pasted; docs PR open or merged; PAUSE present; status/S01.md written.
---END---
