# S07 — Accuracy lane: make the source-fidelity judge catch what we already know is wrong (CONDITIONAL on S02)

Run on: Opus 5.5 (or Opus 5), xhigh effort for the analysis. Mode: autonomous. Depends on: S02 (its verdict), S05 (same QC area — rebase after it merges), and S08 if it is open (both edit candidateQcEvaluator.ts — rebase after it merges).
Must be merged (or declared not needed) before S11. Estimate: 0 h if not needed; otherwise 4–6 h wall, ≤ 40 pipeline model calls (~$15–40) + ~$60–90 session quota.

---PROMPT---
You are executing task S07 of the ChapterFlow v25 pipeline completion plan. The owner's first quality criterion is "accurate with the source". The only automated accuracy gate is the source-fidelity judge inside fresh QC; S02 measured it against the errors we verified by hand. Your job depends on S02's verdict. I explicitly authorize multi-agent orchestration with the Workflow tool (including the adversarial fix workflow) and up to 40 real pipeline model calls for judge tuning. Implementers and reviewers run on Opus only (never Fable models).

Step 0 — load skills with the Skill tool: `engineering-skills:senior-prompt-engineer`, `workflow-authoring`, `superpowers:test-driven-development`, `engineering-skills:adversarial-reviewer`, `superpowers:verification-before-completion`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md`, every file listed by `ls ~/cf-wt/v25-execution/status/` (S02 required; if D1 = B write `RESULT: NOT NEEDED (D1 = B)` and stop), and `assessment/reports/source-accuracy.md`, `verify-accuracy-content.md`. Decide:
- If S02 says S07 is NOT required (recall ≥ 50%, precision ≥ 60%, ≤ 5 blockers/chapter): write `status/S07.md` with first line `RESULT: NOT NEEDED` and S02's numbers, and stop.
- If S02 could not run a probe: finish that probe first (reuse the S02 harness in `~/cf-wt/v25-execution/probes/S02/`), then apply this decision.
- Otherwise identify the failure mode from S02's raw outputs — low recall (misses inverted sequences, misattributed credit, invented motives, wrong quiz keys), over-flagging (false positives), or missing inputs (judge not given the right span, chunking, quiz items not included) — and fix only that.

Step 2 — offline eval loop in a throwaway worktree (`~/cf-wt/franklin-v7-tools/wt.sh new s07-judge-eval`, never pushed; point the S02 harness's PIPE_SRC at it; `wt.sh rm s07-judge-eval` when done): eval set = `probes/S02/labels.json` (confirmed errors for ch01/07/13/19 of rr21) + the "debatable/unsupported" list as negatives-for-precision. Use the S02 harness with a scratch run-state root (never `~/cf-canary`). Try at most 3 prompt or input variants of `src/critics/semantic/sourceFidelityJudge.ts` (and its prompt/profile files), 4 calls each, recording recall/precision/blockers-per-chapter/tokens per variant in `probes/S07/eval.md`. Stop when a variant meets the S02 thresholds or after 3 variants. If none meets them, do not open a PR: write `status/S07.md` with first line `RESULT: NEEDS-OWNER`, the eval table and two choices for me (proceed to S11 with the best variant as a PR, or proceed unchanged and rely on S12's accuracy audit), then stop. If a usage-limit error appears, stop and report.

Step 3 — land the winning variant as one PR through the adversarial fix workflow: copy `~/cf-wt/v25-execution/templates/adversarial-fix-workflow.js`, fill CONSTANTS (WT `source-fidelity-judge-recall`, TITLE `fix(v25): the source-fidelity judge checks sequence, attribution and quiz keys against the source span`, RED_EXPECT = the new contract tests (prompt contains the new checks / inputs include the quiz items and span) failing on main, lenses: correctness (the eval numbers are reproducible from saved raw outputs; no over-fitting to Franklin wording — the prompt must be book-agnostic) and gate-safety-minimality (judge still fails closed on error; verdict mapping unchanged)), fill TASK as CONTEXT.md §6 "Filling the template's TASK" says, and run it. If it returns passed:false, follow CONTEXT.md §6. Tests must be hermetic (no model calls); the eval evidence goes in the PR body.

Step 4 — verify the PR, merge per D10 or list it. Write `~/cf-wt/v25-execution/status/S07.md` (first line `RESULT: …`) with the decision taken, eval table, PR number/URL (looked up), SHA, suite lines. Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: either "NOT NEEDED" with S02's numbers, or a merged/open PR whose variant meets the thresholds on the eval set with saved raw outputs, two reviewer PASSes, `wt.sh verify` fail 0, and ≤ 40 model calls used (count them).
---END---
