# S12 — Release the book (or evaluate the best candidate), score it, and deliver the evaluation to the owner

Run on: Opus 5.5 (or Opus 5). Mode: autonomous. Depends on: S11 with promotion COMPLETED (RELEASE mode), or S11/D12-A, D4 = C, or D1 = B
(CANDIDATE mode). D14 decided for RELEASE mode.
Estimate: 3–5 h wall; scoring ~$60–120 session quota (6 blind readers + adjudication + a 3-reader book-score panel + accuracy auditors). The release makes 0 model calls.

---PROMPT---
You are executing task S12 of the ChapterFlow v25 pipeline completion plan: produce the release package (or, if the book was not promoted, assemble the best candidate), score it honestly with the same instruments used before (so the numbers are comparable with rev-6's 64.6), check its accuracy against the Gutenberg source, and deliver a clear evaluation page to me — with the `publish-final` command only in RELEASE mode, and only for me to run. I explicitly authorize multi-agent orchestration with the Workflow tool, including running `~/cf-wt/franklin-v7-tools/score-franklin-v7.js` by scriptPath. Never use Fable models for any agent; readers, auditors and adjudicators run on Opus or Sonnet 5.

Step 0 — load skills with the Skill tool: `workflow-authoring`, `book-score`, `artifact-design`, `dataviz`, `engineering-advanced-skills:self-eval`, `humanizer`, `superpowers:verification-before-completion`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D1, D12, D14), every file listed by `ls ~/cf-wt/v25-execution/status/`, `~/cf-wt/franklin-v7-tools/RUNBOOK.md` (release and scoring sections), and for comparison `assessment/reports/reader-quality.md` and `source-accuracy.md` (the rr21 baseline: 66% of 182 checked claims correct; per-chapter reader scores 61–68) and `~/ChapterFlow-books-v25-completion/docs/v25/S_TIER_PHASE_A_REPORT_2026-09-02.md` (rev-6: 64.6, gate 0/6, churn HIGH). Decide the mode: RELEASE if status/S11.md shows promotion COMPLETED; CANDIDATE if it applied D12-A (use the candidate id it names), or D4 = C, or D1 = B (use rr21, `review-repair-21-candidate-06d7596afd1f14dccd3eb2df99b22db6`).

Step 2 — produce the package (0 model calls):
- RELEASE mode: confirm the promoted candidate's `sourceGitSha` is on origin (`git merge-base --is-ancestor <sha> origin/main`). Take the release command S11 recorded (the `promote-book … --resume-unfinished-release` form; the wording at `cli.ts` ~564 about "advancing one more revision" is stale — trust the command) and, per D14 = A, replace its literal `--categories <…>` and `--tags <…>` placeholders with the values in the tracked `$PIPE/book-packages/the-autobiography-of-benjamin-franklin.v21.json` on origin/main (re-read them with `git show origin/main:<path>` and paste them; never invent reader-facing metadata). If D14 = B, print the filled command for me and write `RESULT: NEEDS-OWNER`. Run it; verify the package `/Users/radinsoltani/ChapterFlow-books-v25-completion/scripts/book/prompts/chapterflow-v24-author-pipeline/book-packages/the-autobiography-of-benjamin-franklin.v21.json` and its production-manifest sidecar exist, the chapter count is 19, and record their sha256. Do NOT run `publish-final`, and do not commit the package.
- CANDIDATE mode: no release, no publish-final. Assemble a scratch package `{chapters:[…]}` from the candidate's `content/content/chapters/*.chapter.json` under `~/cf-wt/v25-execution/probes/S12/` (never under ~/cf-canary or a repo) and use its absolute path as PKG.

Step 3 — score, three ways, blind to each other:
- The 6-reader instrument: compute the sample with `python3 ~/ChapterFlow/.claude/skills/book-score/score.py the-autobiography-of-benjamin-franklin --path <PKG>` (its `seeded_idxs`; expect [0, 6, 12, 18]), then run the Workflow tool with scriptPath `/Users/radinsoltani/cf-wt/franklin-v7-tools/score-franklin-v7.js` and args `{pkg: '<PKG>', source: '/Users/radinsoltani/cf-canary/sources/the-autobiography-of-benjamin-franklin.txt', label: 'franklin-v25-final', idxs: <seeded_idxs>, chapterCount: 19}` (6 blind readers + adjudication + churn). Paste its composite, gate votes, factor medians, churn.
- The `book-score` skill on the same package, passing the ABSOLUTE package path (from `~/ChapterFlow` it would otherwise look for `book-packages/` locally, then on origin/main, and report the book as unpublished). Paste its scorecard (gate, 10 factors, composite, tier, high-quality verdict ≥ 85).
- Accuracy: repeat the rr21 audit method on the four seeded chapters (every reader-visible factual claim checked against the source text with line numbers; every quiz key). Use 2 Opus auditors on different chapters and a third to spot-check 10 of their verdicts. Report correct / contradicted (major) / unsupported, and compare with rr21's numbers.

Step 4 — deliver: build one HTML page following the artifact-design guidance (a document-style evaluation, not a dashboard) and the dataviz rules for any chart, then publish it with the Artifact tool, titled "Franklin v25 Evaluation". Content, in plain language: the verdict first (RELEASE: ships / does not ship and why; CANDIDATE: "not promoted — stopped by <gate>"); both scorecards side by side with rev-6; the accuracy table vs rr21; the top 5 remaining reader-impact defects with quotes; what changed in the pipeline (the PRs from #566 on, one line each, numbers looked up); known gaps and deferred items; RELEASE mode only: the exact `publish-final` command for me to run (copy button) with the precondition checks to do first. Run the humanizer skill on the prose sections. Keep numbers in tables.

Step 5 — write `~/cf-wt/v25-execution/status/S12.md` (first line `RESULT: …`; mode, package path + sha256, both scorecards verbatim, the accuracy table, the artifact URL, the publish-final command in RELEASE mode) and run the self-eval skill on your own report (paste its two-axis score). Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: the package (RELEASE) or scratch assembly (CANDIDATE) exists with recorded hashes; both scorecards and the accuracy audit computed from saved raw outputs; the evaluation page published and its URL recorded; publish-final NOT run; status/S12.md written.
---END---
