# S09a — Readability: no more wall-of-text summaries; fix the memorable-line "Mr." splitter

Run on: Opus 5.5 (or Opus 5). Mode: autonomous, adversarial-workflow PR. Depends on: S01. Parallel with: S02–S06, S09b, S10.
Must be merged before S11 when D6 = A (fresh run) and before S08. Estimate: 4–5 h wall; ~$50–80 session quota. No pipeline model calls.

---PROMPT---
You are executing task S09a of the ChapterFlow v25 pipeline completion plan. Two verified reader defects: (1) in the latest Franklin draft (rr21), 8 of 19 full reads (486–774 words) and 11 of 19 deep reads contain no newline at all, so the app (which splits paragraphs on blank lines) shows each as one block; no repair ever fixed them because no gate checks it; (2) the memorable-line sentence splitter breaks after abbreviations — ch18 shipped the fragment "Canton to test the pointed rod himself, and Canton found it worked." from "…sent Mr. Canton to test…". Fix both as one PR through the adversarial fix workflow. This tightens (never loosens) the pipeline. I explicitly authorize multi-agent orchestration with the Workflow tool. Implementers and reviewers run on Opus only (never Fable models).

Step 0 — load skills with the Skill tool: `workflow-authoring`, `superpowers:test-driven-development`, `engineering-skills:senior-prompt-engineer` (for the writer-contract wording), `engineering-skills:adversarial-reviewer`, `superpowers:verification-before-completion`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D1, D6, D10), every file listed by `ls ~/cf-wt/v25-execution/status/` (S01 DONE; if D1 = B write `RESULT: NOT NEEDED (D1 = B)` and stop), `assessment/reports/reader-quality.md` (top-5 defects table) and `assessment/reports/verify-accuracy-content.md` §3 and §6. Confirm the facts yourself on `~/cf-canary/books/the-autobiography-of-benjamin-franklin/candidates/review-repair-21-candidate-06d7596afd1f14dccd3eb2df99b22db6/content/content/chapters/*.chapter.json` (read-only). Find where these tiers are produced and checked: the summary section task and writer card (`src/sections/sectionTasks.ts` and the writer contract), the section gate `validateSectionPack` (`src/sections/sectionGate.ts`), the chapter editor pass (`src/app/chapterEditorPass.ts` — it re-validates every edited pack through `validateSectionPack`, ~199-245), and the repair writer contract (`buildRepairWritingContract` composes the section contract; size pin `REPAIR_WRITING_CONTRACT_MAX_CHARS` in `candidateRepairWritingContract.ts` ~59-122).

Required change (minimal; branch `v25/summary-paragraphs-and-splitter`):
1. Writer contract: the deep read and full read are written as paragraphs separated by a blank line, each paragraph at most 150 words (the fast read may stay one paragraph). One or two sentences in the card, no new template machinery.
2. A deterministic check inside `validateSectionPack` (new SEC code, BLOCKER at compile like its neighbours) that rejects a deepRead or fullRead longer than 150 words with no blank-line break, or containing any paragraph over 180 words (looser than the instruction on purpose, so a compliant draft never trips it). Its message goes through the existing retry feedback and names the tier and the lengths.
3. Flattening. Editor pass: add no new guard — once item 2 lives in `validateSectionPack`, a flattened edit is refused and the unedited chapter kept; add one test proving it. Repair lane: there is no preservation guard, and its only post-repair content check (`revalidateMemorableLines`, ~672) fails the WHOLE ordinal, so do not add a new failure path there. The repair writer already receives the paragraph instruction through `buildRepairWritingContract`; add a test that the rendered repair contract contains it, expect a measured re-pin of the repair-contract size pin (a prompt-size alarm, not a gate), and record "no deterministic check that a repair keeps paragraphs" as a deferred minor.
4. The PRIVATE `splitSentences` in `src/optimizers/memorableLines.ts` (~335) only — not `critics/textUtils.splitSentences`, which sectionGate (SEC130/SEC131) and several critics share: do not split after Mr., Mrs., Ms., Dr., St., Gov., Col., Capt., Gen., Rev., Hon., Esq., "No." before a digit, vs., e.g., i.e., or a single capital-letter initial other than "I"; unit tests include the ch18 sentence and must show real sentence ends still split.
Out of scope: example templating, memorable-line selection policy, quiz stems, any web-app change.
Note in the PR: prompt/task-card changes change the section-pack cache digest, so they reach a book only through a fresh compile (D6 = A).

RED first: a gate test asserting a 400-word single-paragraph fullRead is rejected (today it is accepted, so the test fails); an editor-pass test asserting a flattened edit is refused and the unedited chapter kept; a repair-contract test asserting the paragraph instruction is rendered; splitter tests with the ch18 sentence. Real-data proof (no model calls): run the new check over rr21's 19 chapters and paste which tiers it rejects — expect 22: the 11 zero-break deep reads (ch02, ch04, ch06, ch07, ch09, ch10, ch11, ch12, ch14, ch15, ch19), the 8 zero-break full reads (ch02, ch04, ch06, ch07, ch09, ch10, ch12, ch19), and 3 paragraphed full reads with one paragraph over 180 words (ch01 205, ch05 242, ch13 205), counting words on whitespace and paragraphs on blank lines. Also run it over run 39a37d06's compile output (candidate `compiler-operator-retry-13-run-06d7596afd1f14dccd3eb2df99b22db6-candidate`) and paste the count: that is the extra compile-retry pressure the final run will see (D13 sets the retry budget).

Step 2 — copy `~/cf-wt/v25-execution/templates/adversarial-fix-workflow.js` to your session scratchpad, fill CONSTANTS (WT `summary-paragraphs-and-splitter`, TITLE `fix(v25): write summary tiers in paragraphs and gate walls of text; split memorable lines after abbreviations correctly`, RED_EXPECT, lenses: correctness (gate thresholds consistent with the instruction; the editor pass refuses flattening; the splitter is right on the listed abbreviations and does not over-merge real sentence ends) and gate-safety-minimality (no existing gate loosened; the new check cannot deadlock a compile — check the retry feedback path and the carry-over of the last rejected draft (#558); no new repair failure path)), fill TASK as CONTEXT.md §6 "Filling the template's TASK" says, and run it (Workflow tool, scriptPath). If it returns passed:false, follow CONTEXT.md §6.

Step 3 — verify, merge per D10 or list for me. Write `~/cf-wt/v25-execution/status/S09a.md` (first line `RESULT: …`; PR number/URL looked up, SHA, RED/GREEN/suite lines, the rr21 and compile-candidate gate tables, the deferred minor). Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: PR with two reviewer PASSes (RED reproduced at the base); the new check flags exactly the 22 listed tiers on rr21; splitter fixed with tests; full suite fail 0; status/S09a.md written.
---END---
