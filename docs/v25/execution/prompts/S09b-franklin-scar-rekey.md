# S09b — Re-key the Franklin fact pins to the real 19-chapter map (and pin the verified errors)

Run on: Opus 5.5 (or Opus 5). Mode: autonomous, adversarial-workflow PR (content config + one structural test). Depends on: S01. Parallel with: S02–S06, S09a, S10.
Must be merged before S11 when D6 = A (fresh run). Estimate: 3–4 h wall; ~$40–60 session quota. No pipeline model calls.

---PROMPT---
You are executing task S09b of the ChapterFlow v25 pipeline completion plan. The Franklin scar file (`config/book-scars/the-autobiography-of-benjamin-franklin.json`, 42 rules from PR #538) scopes its rules with `(chNN)` labels written for the OLD 4-part book. `src/lib/bookScars.ts` (~106-125, `bookRuleChapters`) matches rules by chapter number only, so in the live 19-chapter runs ch01–ch04 received pins about other episodes (the virtue pins went into "Beginning Life as a Printer", the civic pins into "Arrival in Philadelphia", the London/proprietors pins into "First Visit to Boston") and ch05–ch19 received none (verified from the compile transcripts of 2026-09-19). Fix the content and add a test so this cannot silently happen again — one PR through the adversarial fix workflow. I explicitly authorize multi-agent orchestration with the Workflow tool. Implementers and reviewers run on Opus only (never Fable models).

Step 0 — load skills with the Skill tool: `workflow-authoring`, `superpowers:test-driven-development`, `engineering-skills:adversarial-reviewer`, `superpowers:verification-before-completion`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D1, D6, D10), every file listed by `ls ~/cf-wt/v25-execution/status/` (S01 DONE; if D1 = B write `RESULT: NOT NEEDED (D1 = B)` and stop), `assessment/reports/merged-ledger.md` §0 item 3, `assessment/reports/verify-accuracy-content.md` §5, and `assessment/reports/source-accuracy.md` §4 with the verifier's corrections in `verify-accuracy-content.md` §1–2 (the confirmed major errors with source line numbers). Read the scar file, `src/lib/bookScars.ts`, and the existing structure test `$PIPE/tests/franklin-scars-structure.test.ts` (a root test run by `tests/run.ts`; it pins each FACT PIN label to exactly one chapter, caps a pin at 60 words, requires ≥ 20 pins, and checks a 9,600-char rendered budget only for chapters 1–8).

The chapter map: key the rules to THIS map — the one in candidate `review-repair-21-candidate-06d7596afd1f14dccd3eb2df99b22db6` (`content/inputs/research/chapter-map.json`, read-only under `~/cf-canary/books/the-autobiography-of-benjamin-franklin/candidates/`). The final run reuses this run's research (D6: `RESEARCH_RUN_ID=20260918T123418217Z-0e835cda-3c5b-4984-8a0b-105634df246a`) precisely so the map stays the same. Its start/end offsets index the candidate's own frozen text `content/inputs/research/source-text.txt` (sha256 8d71d7dc…, equal to the map's sourceTextSha256) — NOT `~/cf-canary/sources/…txt` (CRLF endings and `_italic_` markers make its offsets wrong; its line numbers coincide). Work by line; the spans as line ranges are: ch01 1-395, ch02 396-788, ch03 789-1058, ch04 1059-1336, ch05 1337-1486, ch06 1487-1952, ch07 1953-2504, ch08 2505-2920, ch09 2921-3522, ch10 3523-3949, ch11 3950-4203, ch12 4204-4523, ch13 4524-5019, ch14 5020-5105, ch15 5106-5235, ch16 5236-5676, ch17 5677-5976, ch18 5977-6131, ch19 6132-6693 (verify). Do not change `bookScars.ts`.

Required change (branch `v25/franklin-pins-19-chapters`):
1. Re-key every chapter-scoped rule (a `chNN` parenthesis in the label and not SAFETY — about 26 FACT PINs and 2 NAMED ACTOR rules): locate each quoted string in the source after collapsing whitespace (quotes after "Never" are false variants absent from the source — ignore them), and re-label the rule to the chapter whose span holds its source quotes. Keep the rule text; change only the label. Expected mapping, 0-based `prohibitions[]` index (verify each): #3→ch03, #4→ch02, #5→ch02, #6→ch03, #7→ch02, #8→ch02, #9–#16→ch09, #17→ch11, #18→ch13, #19→ch13, #20→ch08, #21→ch08, #22→ch07, #23→ch12, #24→ch13, #25→ch11, #26→ch18, #27→ch10, #29–#33→ch19. Known special cases: FACT PIN #16 (Art of Virtue, line ~3339) also quotes "[unfinished]" from the last line (ch19) — keep it ch09 and note the second quote; NAMED ACTOR (old ch03) quotes only ch13 but governs every civic chapter — label it (ch08, ch11, ch12, ch13); NAMED ACTOR (old ch04) quotes ch15 and ch19 — label it (ch15, ch19). Multi-chapter labels only for NAMED ACTOR rules. Put the old → new table (with source lines) in the PR body.
2. Add fact pins, in the file's existing format, ONLY for the confirmed major errors of the latest draft, each with its source quote and line range, scoped to the chapter whose span holds it. Write each as a prohibition of the false version plus the source fact ("Do not say …; the source says …"), never as an instruction to include the fact (Phase A found "include this" pins cause token hammering). Candidates: ch07 — Keimer's paper was Franklin's FIRST paper; Baird spoke at the merchants' Every-night club, not the Junto; Franklin consented to the partnership before Meredith's father approved; he took half from each friend "because I would not give an unkind preference to either"; he went back to Keimer's employ before the Burlington job; ch13 — Clifton is credited with the idea of lighting the city and Franklin with the lamp's form; Franklin himself was the no-sect trustee; ch19 — Fort George fell during Franklin's absence at Halifax, after his ship left the fleet; the proprietors' answer was a long message offering to treat with "some person of candour"; ch01 — Josiah trained as a dyer and the smith custom applied to eldest sons; the Uncle Thomas resemblance was noticed by Franklin's son. No pins for debatable or merely unsupported items. Respect the test's 60-word cap; if a budget blocks a pin, report it instead of raising the budget.
3. Structural test (the RED): fails unless every chapter-scoped rule (scope decided by `bookRuleChapters()`, so SAFETY "(ch03)" labels stay book-wide) has at least one quoted string of 12+ characters that occurs, whitespace-collapsed, inside the line span of a chapter its label names. False-variant quotes and incidental ones (e.g. "their governor", which also occurs in ch16) must not fail it. Use a checked-in fixture copy of the 19-chapter map as line ranges and the source lines the test needs (tests must not read `~/cf-canary`). Extend the existing rendered-budget loop from chapters 1–8 to 1–19 (not a budget raise; after the re-key the heaviest chapters render ~7,600 chars). The fixture map is also what S11 compares the final run's map against.
Note in the PR: scar changes change the section-pack cache digest; they reach a book only through a fresh compile (D6 = A).

Step 2 — copy `~/cf-wt/v25-execution/templates/adversarial-fix-workflow.js` to your session scratchpad, fill CONSTANTS (WT `franklin-pins-19-chapters`, TITLE `content(v25): key the Franklin fact pins to the 19-chapter source map and pin the verified errors`, RED_EXPECT = the new span test failing on the current scar file, lenses: "source-accuracy" (every pin quote verbatim from the source at the cited lines; every new pin states the source fact without adding claims; no pin contradicts the source) and "gate-safety-minimality" (rule text unchanged for re-keyed rules; no budget raised; test hermetic)), fill TASK as CONTEXT.md §6 "Filling the template's TASK" says, and run it (Workflow tool, scriptPath). If it returns passed:false, follow CONTEXT.md §6.

Step 3 — verify, merge per D10 or list for me. Write `~/cf-wt/v25-execution/status/S09b.md` (first line `RESULT: …`; PR number/URL looked up, SHA, the re-key table, the new pins, the fixture map path, RED/GREEN/suite lines). Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: PR with two reviewer PASSes (RED reproduced at the base); every chapter-scoped rule sits in its chapter's span (test green); new pins only for confirmed errors; full suite fail 0; status/S09b.md written.
---END---
