# S05 — Fresh-QC reachability: give the book-level F4 blocker a chapter location (and BP15 per D5)

Run on: Opus 5.5 (or Opus 5). Mode: autonomous, adversarial-workflow PR. Depends on: S01, D5. Parallel with: S02, S03, S04, S06, S09a/b, S10.
Must be merged before S11. Estimate: 3–4 h wall; ~$40–70 session quota. No pipeline model calls.

---PROMPT---
You are executing task S05 of the ChapterFlow v25 pipeline completion plan: remove the deterministic wedge that would stop the QC-repair lane the first time fresh QC runs, as one PR through the adversarial fix workflow. I explicitly authorize multi-agent orchestration with the Workflow tool. Implementers and reviewers run on Opus only (never Fable models).

Step 0 — load skills with the Skill tool: `workflow-authoring`, `superpowers:test-driven-development`, `engineering-skills:adversarial-reviewer`, `superpowers:verification-before-completion`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults. Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md` (D5, D10), every file listed by `ls ~/cf-wt/v25-execution/status/` (S01 DONE; if D1 = B write `RESULT: NOT NEEDED (D1 = B)` and stop), `assessment/reports/downstream-readiness.md` (§2, §6 items 1–2) and `assessment/reports/verify-gates-downstream.md` (rows 2–10). Reproduce the baseline yourself with the model-free replay: copy candidate `review-repair-21-candidate-06d7596afd1f14dccd3eb2df99b22db6` from `~/cf-canary/books/the-autobiography-of-benjamin-franklin/candidates/` to a scratch books root under `~/cf-wt/v25-execution/scratch/` and run `~/cf-wt/v25-execution/tools/detqc.mts` exactly as its header says → expect `outcome FAIL … blockers 33`, `UNLOCATED blockers: 1 ["F4"]`.

The defect (verified): `critics/bookGate.ts` (~827-832) emits F4 ("soft-banned phrase 'rather than' appears 24 times (budget 15)") with no `path` and no `chapters`; `candidateQcEvaluator.ts` (~627) therefore gives it no location; the QC-repair preflight (`candidateRepairApplicationPort.ts` ~341-343, `groupFindings` ~413-423, preflight ~1122) refuses ANY location-less blocker with `REPAIR_FINDING_UNSCOPED` and aborts the whole repair — before any model call, on every resume, forever for that run (the QC round `derivedId("qc", runId)` is write-once). No test covers a location-less blocker (the nearest test pins the compiler-owned branch, and the rig defaults a missing location to the chapter file).

Required change (minimal; branch `v25/qc-f4-chapter-scope`): F4 (the only soft-ban budget finding) must carry `chapters`: the FEWEST chapters, highest per-chapter count first (ties → lower chapter number), whose phrase occurrences, if removed, bring the book total within budget. Count per chapter over exactly the fields `checkSoftBannedBudgets` concatenates (the per-chapter counts must sum to the book count). The finding stays a BLOCKER with the same budget; its message keeps its current text and appends the scope and per-chapter counts (e.g. "… (budget 15). Scoped to ch03 (5), ch08 (4): remove every occurrence in these chapters.") so each chapter's repair writer knows its share — nothing is loosened. Expected on rr21: ch03 (5) + ch08 (4) = 9 ≥ 24 − 15. The preflight keeps refusing a truly unscoped blocker. Other bookGate findings emitted with no chapters that map to a QC BLOCKER (F3, BP20, BP21, D1, BP26, BP27, BP33, AS4; BOOK_PATTERN_AUDIT_UNBOUND is compiler-owned) would wedge the preflight the same way if a repair ever triggered one; none fires on rr21 — list them in status/S05.md as a deferred risk and do not fix them here.
If D5 = B (owner approved): additionally give BP15 (`critics/quizQuality.ts` ~114-139) the same prose carve-out SEC52 has (`sections/sectionGate.ts` ~3282-3291, ~3363-3365: a trigger word that appears in the chapter's own prose is not a distractor tell), citing D5 in the PR. If D5 = A or blank-without-defaults: do not touch BP15.

RED first: a bookGate unit test with a synthetic book over the F4 budget (expect `chapters` absent today), an evaluator test that the QC issue gets a `chNN` location, and a repair-port preflight test that a location-less blocker is refused today while the new located F4 is accepted and scoped to the chosen chapters. Proof on real data: `detqc.mts` with `PIPE_SRC` pointing at your worktree's `src` → F4 now shows location `ch03,ch08` (or the equivalent format the evaluator produces for multi-chapter findings) and `UNLOCATED blockers: 0`; blocker count unchanged at 33 (or, with D5 = B, BP15 drops to 3: ch01 "entirely", ch05 "always", ch16 "automatically").

Step 2 — copy `~/cf-wt/v25-execution/templates/adversarial-fix-workflow.js` to your session scratchpad, fill CONSTANTS (WT `qc-f4-chapter-scope`, TITLE `fix(v25): scope the book-level soft-ban budget blocker to the fewest chapters so QC repair can act on it`, RED_EXPECT from above, a correctness lens that checks chapter selection is minimal and deterministic and that the repair port's scope contract is unchanged for other codes), fill TASK as CONTEXT.md §6 "Filling the template's TASK" says, and run it (Workflow tool, scriptPath). If it returns passed:false, follow CONTEXT.md §6.

Step 3 — verify the PR yourself (diff scope, the detqc proof re-run), merge per D10 or list it for me. Write `~/cf-wt/v25-execution/status/S05.md` (first line `RESULT: …`; PR number/URL looked up, SHA, RED/GREEN/suite and detqc lines verbatim, D5 applied or not). Add one "Progress" line to memory file `v25-status-assessment-2026-09-23.md`.

Done means: PR with two reviewer PASSes (RED reproduced at the base); detqc on rr21 shows 0 unlocated blockers with the same budget; `wt.sh verify` fail 0; status/S05.md written.
---END---
