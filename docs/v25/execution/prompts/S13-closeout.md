# S13 — Closeout: everything merged, workspace clean, deferred work registered, memory and docs current

Run on: Sonnet 5 or Opus. Mode: autonomous. Depends on: S12 DONE in RELEASE or CANDIDATE mode (and the owner's publish-final, if they chose
to run it — not required). Estimate: 2–3 h wall; ~$20–40 session quota. No pipeline model calls.

---PROMPT---
You are executing task S13, the last task of the ChapterFlow v25 pipeline completion plan: prove the work is complete, clean up, and leave an honest record so the next person (or session) starts from facts. I explicitly authorize multi-agent orchestration, including the Agent and Workflow tools. Never use Fable models for agents.

Step 0 — load skills with the Skill tool: `superpowers:finishing-a-development-branch`, `engineering-advanced-skills:tech-debt-tracker`, `engineering-advanced-skills:changelog-generator`, `si:memory-review`, `superpowers:verification-before-completion`. `~/cf-wt/v25-execution/CONTEXT.md` overrides skill defaults (no interactive merge menus; merges and branch deletion per D10). Work autonomously: do not ask me questions, and do not invoke superpowers:brainstorming, writing-plans or executing-plans — this prompt is the approved design. You stop only by writing your status file (CONTEXT.md §8).

Step 1 — read `~/cf-wt/v25-execution/CONTEXT.md`, `DECISIONS.md`, `README.md` and every file listed by `ls ~/cf-wt/v25-execution/status/`. Build a completion checklist from CONTEXT.md §1 (definition of done) and verify each item with evidence: every PR from S01–S11 merged, or closed as superseded by S01's fast-forward (#567–#575) (`gh pr view`); the v25 CI job green on the latest main (S10); promotion + release done, or D12-A applied (S11/S12); evaluation page published (S12 URL); publish-final command handed over (RELEASE mode). Mark each item DONE / NOT DONE with the evidence line.

Step 2 — git hygiene, only for this kit's merged work (never delete unmerged work):
- For every worktree listed by `git -C ~/ChapterFlow-books-v25-completion worktree list` that is under `~/cf-wt/` on a `v25/*` branch this kit created (see the status files) and is merged or closed as superseded, run `git worktree remove` (no --force). Also remove `~/cf-wt/assess-main`, `~/cf-wt/replay-cp-dry` and any `rev-*`/`gate-main` leftovers. Never touch other `~/cf-wt` entries (iOS/web campaigns live there), `franklin-v7-tools`, `v25-execution`, or any `*.bak-*` snapshot. List what you removed.
- Delete remote branches of merged/superseded v25 PRs (`git push origin --delete v25/<name>`) per D10; if not authorized, print the commands.
- #559 per D8 (close with a comment, or leave with a note). The stale July lines `feat/v25-pipeline-live` (PR #401), `plan/v25-s-tier-implementation`, `impl/v25-evaluator-selection`: do NOT delete; list them with ahead/behind counts for my decision.
- Leave the release's modified `$PIPE/book-packages/the-autobiography-of-benjamin-franklin.v21.json` in the canonical checkout for publish-final (RELEASE mode): report it; do not revert or commit it.

Step 3 — register what was deferred: collect every "deferred" / "non-blocking" / "follow-up" item from all status files and the reviewers' notes — for example: panel per-seat resume after an outage; fresh QC sequential and all-or-nothing; QC-judge per-chapter persistence; example templating; memorable-line selection policy; quiz stem sameness; #567's roman-numeral false positives; #569's duplicate-title guard; the repair role's occasional thinking-to-cap schema failure; S05's other location-less bookGate findings; S09a's missing repair-side paragraph check; S08's items if D13 skipped them; the constant disputed-review bound (unless S11 made it a knob). Also record run `book-run-39a37d06-59c8-430a-87fe-3ad3b19a1c14` and its candidates as superseded by the final run (keep its data). Build a tech-debt register with the tech-debt-tracker skill (severity, evidence, trigger to act) and save it as `docs/v25/execution/DEFERRED.md` via a docs PR (`~/cf-wt/franklin-v7-tools/wt.sh new docs-v25-closeout`), together with a changelog of the v25 completion (changelog-generator over the merged PRs from #525 on; numbers looked up) as `docs/v25/execution/CHANGELOG.md`, and the final `status/*.md` copied into `docs/v25/execution/status/`. Run `npx eslint docs/v25/execution` before committing (0 errors). After the docs PR merges, move any untracked file it now tracks out of the canonical checkout before `git pull --ff-only`.

Step 4 — memory: with `si:memory-review`, update `~/.claude/projects/-Users-radinsoltani-ChapterFlow/memory/`: rewrite `v25-status-assessment-2026-09-23.md` into a concise "v25 pipeline complete" record (what shipped, where the book is, the scores, open decisions) and update its MEMORY.md line; replace the bloated `v25-s-tier-phase-b-2026-09-02.md` (546 KB, a line duplicated ~18x) with a deduplicated version that keeps every unique fact (remove exact repeated segments only; show before/after sizes and prove no unique date-stamped segment was lost); update the "Books worktree is canonical" memory to say the v25 campaign used `~/ChapterFlow-books-v25-completion`.

Step 5 — write `~/cf-wt/v25-execution/status/S13.md` (first line `RESULT: …`) with the checklist, removals, register and changelog PR numbers, memory changes, and the decisions still open for me. Then give me a short final summary: what is done, what I still need to do (publish-final if not done; the July branches; anything NOT DONE), and where everything is.

Done means: the checklist is fully evidenced; only this kit's merged work was cleaned up; DEFERRED.md + CHANGELOG.md in a docs PR (merged or listed); memory updated and the Phase B file deduplicated with no fact lost; status/S13.md written.
---END---
