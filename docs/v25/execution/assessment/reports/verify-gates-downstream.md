# verify gates-downstream

## corrections
- H5b: Replace with: "Fresh QC (after a panel PASS): a deterministic-only replay of the fresh-QC evaluator on the latest candidate is FAIL with 33 blockers (19 BP15, 4 SC11.2, 3 SC11.7, 3 B1, 2 EI1, 1 EI2, 1 F4). This is a floor, because the live round also runs the source-fidelity and answer-key model judges. One of the 33 blockers, F4 ('rather than' 24x, budget 15), is book-level and has no location. Because of it the QC-repair preflight refuses the entire repair with REPAIR_FINDING_UNSCOPED before any run or model call, and it does so again on every resume. That is a zero-cost deterministic wedge."
- H5b (second sentence): Replace with: "Each QC-repair ordinal also needs the successor's full canonical panel to PASS on its first verdict (only ERROR is retried). A FAIL ends the invocation with REPAIR_REVIEW_FAILED and spends the ordinal. The next ordinal re-repairs the original QC-failed candidate. Budget: default 3, driver 4."
- H5c: Replace the panel numbers with: "Using the per-chapter panel as a proxy (median over 19 chapters of each factor), the composite is 75.9-77.5 in all 14 panels, always below the bar of 80. The weakest factor median swings between 65 and 72, and the latest completed panel (review-repair-20) clears the 70 floor with limits 72 and density 71. The rubric itself is a different instrument: 3 readers scoring only ch01/ch07/ch13/ch19."
- H5c: Replace '(Phase A 64.6 vs 76.4 on same bytes)' with: "(Phase A, same rev-6 bytes: 6 fresh book-score readers gave 64.6, against a 76.4 handoff card from an earlier 3-reader book-score panel, and against 75.4-79.7 from the pipeline's per-chapter panel)".
- H5c: Replace 'A rubric FAIL is final for a candidate (no repair lane)' with: "A rubric score is final for a candidate: the stored reader scores replay forever and there is no rubric repair lane. The verdict is re-computed each run against the bar in force (--rubric-bar or CHAPTERFLOW_RUBRIC_BAR, 60-95), so a below-bar result can be re-decided at no cost; a correctness-gate FAIL or SPLIT cannot."
- E5: Replace '(must land before the run commits its QC round)' with: "(an emission-side fix in bookGate or the evaluator must land before this run commits its fresh-QC round, because the round keyed derivedId('qc', runId) is write-once; a preflight-side fix that scopes a location-less book-wide blocker to the chapters containing the phrase would also un-wedge an already-committed round)".
- E5 (addition): Append: "Give BP15 the same prose carve-out SEC52 has. On the latest candidate that clears 16 of the 19 BP15 blockers. The remaining 3 (ch01 'entirely', ch05 'always', ch16 'automatically') break both rules, and 2 of them were introduced by review-repair edits after compile."
- E3 (autoresume part): Replace with: "Forward CHAPTERFLOW_REVIEW_REPAIR_ORDINALS, CHAPTERFLOW_QC_JUDGE_RUNS and CHAPTERFLOW_RUBRIC_RUNS in autoresume.sh, and set CHAPTERFLOW_REVIEW_REPAIR_ORDINALS=40 and LOG_PREFIX (currently the stale fv8d) in AUTORESUME.env. Note that drive-franklin-v7.sh:46,54 hard-codes CHAPTERFLOW_REVIEW_REPAIR_ROUNDS=4 and CHAPTERFLOW_QC_REPAIR_RUNS=4 inline, so autoresume's REVIEW_REPAIR_ROUNDS=10 has no effect and those two knobs can only be changed in the driver."
- E3/H4 (context): Add: "With the default ordinal ceiling of 20, any relaunch replays ordinals 1-20 free in about 3 s and then fails closed with 'review-repair successor budget exhausted after 20 ordinals', before reaching ordinal 21 or its stored ERROR review; the driver then WEDGE STOPs. Autoresume currently cannot relaunch at all, because the last driver marker is WEDGE STOP."
- Investigator report downstream-readiness.md:8 and :155: Replace 'The test v4-candidate-repair-application-port.test.ts:146 pins this refusal' with: "The nearest test (v4-candidate-repair-application-port.test.ts:146) pins REPAIR_FINDING_UNSCOPED for a book finding located at critics/book-pattern-audit.json (the compiler-owned branch). No test covers a location-less blocker or F4; the rig defaults a missing location to the chapter file."

## confirmations
- detqc.mts is model-free: the evaluator is built with no runner and no taskContext, so both judges are skipped (candidateQcEvaluator.ts:649); the critics gate modules have no subprocess, network or gateway calls; the writeLock throws; the byte copy's sha256 was unchanged after the run.
- Replay on the byte copy of review-repair-21 = 'outcome FAIL issues 212 blockers 33 warns 179' with exactly 19 BP15, 4 SC11.2, 3 SC11.7, 3 B1, 2 EI1, 1 EI2, 1 F4 (verify/detqc-verify.out, identical to downstream/detqc.out).
- F4 is emitted as severity 'major' with no path or chapters (bookGate.ts:827-832); it is a QC-blocking major (majorPolicy.ts:42-89), so it becomes a BLOCKER.
- candidateQcEvaluator.ts:627 gives F4 an undefined location.
- QC-repair preflight: issueChapters returns REPAIR_FINDING_UNSCOPED for a missing location (candidateRepairApplicationPort.ts:341-343); groupFindings aborts the whole repair on it (:413-423); this happens in preflight before any run or model call (:1122, :1253). Only F4 of the 33 blockers is unscoped (verify/scope.js).
- The fresh-QC round id is derivedId('qc', runId) (bookRunApplicationService.ts:3360), reused on resume (:3362-3383), and write-once in the store (qcStore.ts:403-416); the successor walk is for ERROR only (:3398).
- BP15 (quizQuality.ts:114-139) has no prose carve-out; SEC52 (sectionGate.ts:3282-3291, 3363-3365) does. 16 of 19 BP15 hits would be carved out by SEC52's rule.
- The v25 compile path never calls runShipGate, the chapter gate composite or the book gate; only candidateQcEvaluator.ts:577,614 does. The compiler port imports sectionGate and bookPatternAudit only.
- contentRepairWorkflow.ts:242-264: the successor review must PASS on its first verdict; FAIL returns REPAIR_REVIEW_FAILED at once; only ERROR is retried (3 ids).
- A QC-repair successor FAIL marks the repair run FAILED (port :1626-1632), ends the book-run (:3530-3533), and spends the ordinal; only REPAIR_REVIEW_ERROR is forgivable (:2029-2037).
- Rubric: bar 80 (catalogRubric.ts:178), factor floor 70 (:185), churn not HIGH, unanimous correctness PASS required, SPLIT fails closed (:547, :630-690).
- The stored rubric record replays for the candidate with no re-scoring (bookRunApplicationService.ts:2330-2359).
- No rubric repair lane: a non-promotable verdict returns failed (:3606-3611), and a grep finds no routing into repair.
- The candidate release gates only on book-gate blockers and records the rubric as evidence (cli.ts:2473-2536); promotionService.ts, promoteBook.ts and candidateReleaseGate.ts have 0 rubric references.
- autoresume.sh forwards only REVIEW_REPAIR_ROUNDS, OPERATOR_COMPILE_RETRIES and CAPTURE_INVALID_OUTPUT (:33-52, 122-125); the plist env is PATH only.
- AUTORESUME.env has no CHAPTERFLOW_REVIEW_REPAIR_ORDINALS, and LOG_PREFIX=fv8d is stale.
- With the default ordinal ceiling of 20 (MAX_REVIEW_REPAIR_ORDINALS, :457/:593), a resume replays 1-20 free and then fails closed with BOOK_RUN_REPAIR_UNAVAILABLE 'review-repair successor budget exhausted after 20 ordinals' (:1938-1987, :3017-3045). Live evidence that replay is free: the last round logged exit=1 elapsed=3s new_attempts=0.
- Phase A report: 64.6 (6 fresh book-score readers) vs 76.4 (earlier 3-reader card) on the same rev-6 bytes, about 12 points apart (S_TIER_PHASE_A_REPORT_2026-09-02.md:9-17).
- The rubric samples chapters 1, 7, 13 and 19 of Franklin's 19 (selectRubricChapterIndexes = [0,6,12,18], sample size 4, whole-book only when there are 6 or fewer chapters).

## report

## Adversarial verification: H5b, H5c, E5, E3 (autoresume part)

Method: I only read code at the run checkout 9f0117cb7, plus live data in ~/cf-canary and ~/cf-wt/franklin-v7-tools. I re-ran one deterministic replay on a byte copy of the candidate and computed two pure functions. No model was called and nothing was written outside verify/.

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 1 | detqc.mts makes no model call | CONFIRMED | detqc.mts:17 builds `new CandidateQcEvaluator(reader)` with no options and passes no `taskContext`. Both model judges sit behind `if (this.#runner !== undefined && request.taskContext !== undefined)` (candidateQcEvaluator.ts:649), so they are skipped and the round gets a SOURCE_FIDELITY_NOT_RUN WARN instead (:922-928). A grep of critics/, qc/ and the compiler/source/books modules the gates import for child_process, spawn, fetch, ModelTaskRunner, gateway or openai found only quizKeyGate.ts, and that file only reads a stored keyjudge JSON. The script's writeLock throws on any write. It ran with the API keys unset and CHAPTERFLOW_NO_API_CODEX_QC=1. |
| 2 | Deterministic replay on the latest candidate = FAIL, 33 blockers (19 BP15, 4 SC11.2, 3 SC11.7, 3 B1, 2 EI1, 1 EI2, 1 F4) | CONFIRMED | I copied review-repair-21-candidate-06d7…; all 203 file sha256s matched the source, and they were still unchanged after the run. `tsx verify/detqc.mts verify/books review-repair-21-…` printed `outcome FAIL issues 212 blockers 33 warns 179` and `BLOCKER codes: {"BP15…":19,"EI1…":2,"B1":3,"SC11.2…":4,"SC11.7…":3,"EI2…":1,"F4":1}`. The output is byte-identical to downstream/detqc.out (verify/detqc-verify.out). Caveat: this is the deterministic lane only. Live fresh QC also runs the source-fidelity and answer-key judges (:649-906), which can add more blockers. |
| 3 | bookGate emits F4 with no path or chapters | CONFIRMED | critics/bookGate.ts:827-832 pushes `{catalogId:"F4", severity:"major", message, evidence}` with no `path` or `chapters`. F4 is not in ADVISORY_MAJOR_PREFIXES (majorPolicy.ts:42-70), so mappedSeverity turns it into a BLOCKER (candidateQcEvaluator.ts:150-152). Output line: `B F4  soft-banned phrase "rather than" appears 24 times (budget 15)`. |
| 4 | candidateQcEvaluator gives F4 no location | CONFIRMED | candidateQcEvaluator.ts:627: `finding.path ?? (finding.chapters?.length ? … : undefined)` evaluates to undefined. |
| 5 | QC-repair preflight refuses location-less blockers with REPAIR_FINDING_UNSCOPED | CONFIRMED, and wider than the draft says | candidateRepairApplicationPort.ts:341-343. groupFindings (:413-423) returns on the first unscoped blocker, so the whole QC repair is refused, not just F4. preflight (:1122-1123) runs from run() (:1253) before any run or model call. Replicating matchedChapters/compilerOwned over the 33 blockers (verify/scope.js) gives `unscoped 1: F4 <no location>`. The stored round is re-read on every resume, so it is a zero-cost repeat refusal. |
| 6 | A test pins this refusal (v4-candidate-repair-application-port.test.ts:146, cited in downstream-readiness.md:8,155) | PARTLY TRUE | The test at :146-154 uses `location: BOOK_PATTERN_AUDIT_LOGICAL_PATH` ("critics/book-pattern-audit.json"), which hits the compiler-owned branch (:345-346), not the no-location branch (:342-343). The rig turns a missing location into the chapter file (repairPortRig.ts:200), so no test can express a location-less blocker. No test mentions F4. |
| 7 | The fresh-QC round is immutable per run (derivedId("qc", runId)) | CONFIRMED | bookRunApplicationService.ts:3360. On resume the stored round is reused (:3362-3383). qcStore.commitRound is write-once (createFileAtomic; a conflicting round gives QC_ROUND_ID_CONFLICT, qcStore.ts:403-416). A successor round is minted only for outcome ERROR (:3398); a FAIL round is permanent. QC repair takes `failedRoundId = qc.value.roundId` (:3489). |
| 8 | The F4 fix must land before the round commits | PARTLY TRUE (true for the fix E5 proposes) | True for a fix at emission (bookGate or evaluator location), because the committed round keeps its location-less F4 forever. Not true for a preflight-side fix: scoping a location-less book-wide blocker to the chapters that contain the phrase, or to all chapters, in groupFindings/issueChapters would also un-wedge a round that is already committed. A new runId also gets a fresh round. |
| 9 | BP15 lacks the prose carve-out that SEC52 has | CONFIRMED | quizQuality.ts:114-139 checks distractors only and suppresses only a hypothetical clause (`as if`, `pretend`…). sectionGate.ts:3282-3291 builds `proseAbsolutes` from the chapter's own prose, and :3363-3365 skips any trigger found there. Measured (verify/bp15carve.js, same prose fields as chapterProseText): 16 of the 19 BP15 blockers use a trigger word that appears in that chapter's prose, so SEC52 would carve them out. 3 would remain: ch01 "entirely", ch05 "always", ch16 "automatically". The ch05 and ch16 hits are not in the compile candidate's replay (17 BP15), so review-repair edits introduced them. |
| 10 | The v25 compile path never runs the QC gate stack | CONFIRMED | Under app/ and review/, runShipGate, runChapterGateCompositeFromCandidate and runBookGateFromCandidate are called only in candidateQcEvaluator.ts:577,614. compilerApplicationPort.ts imports validateSectionPack (sectionGate) and runBookPatternAudit only (:20,27). sectionTasks.ts:368 says soft-banned budgets are enforced only in bookGate. |
| 11 | A QC-repair ordinal needs the successor canonical review to PASS on its first verdict | CONFIRMED | contentRepairWorkflow.ts:242-264: only ERROR is retried (up to 3 review ids), and a FAIL returns REPAIR_REVIEW_FAILED on the first attempt. Screening must also return SHORTLIST (:222-226). |
| 12 | What a FAIL does | CONFIRMED | The port finishes the repair run FAILED with reason "canonical review outcome: FAIL" (candidateRepairApplicationPort.ts:1626-1632). book-run emits repair FAILED and returns (:3530-3533). On resume the ordinal counts as spent: only REPAIR_REVIEW_ERROR is forgivable, at most 2 (:2029-2037, :1971-1978). The walk then mints the next ordinal, which re-repairs the original QC-failed candidate; the successor that failed review is dropped and has no review-repair lane. Budget: CHAPTERFLOW_QC_REPAIR_RUNS, default 3 (:377); the driver hard-codes 4. |
| 13 | Rubric gate: composite >= 80, factor medians >= 70, churn not HIGH, unanimous correctness, SPLIT fails closed | CONFIRMED | catalogRubric.ts:178 (bar 80), :185 (floor 70), :547 (PASS only when fail=0; mixed votes = SPLIT), :630-690 (FAIL, then SPLIT, then composite, floor and churn). The bar can be set 60-95 via --rubric-bar or CHAPTERFLOW_RUBRIC_BAR (:188-200). |
| 14 | A stored rubric verdict replays forever for the candidate | PARTLY TRUE | The stored record (the reader scores) replays forever (bookRunApplicationService.ts:2330-2359, keyed by candidateId). The verdict is recomputed on every run from that aggregate against the bar in force (:3592-3593; catalogRubric.ts:614-619, "an operator who moved the bar re-decides a stored panel for free"). So RUBRIC_BELOW_BAR can be reversed by changing the bar without re-scoring. RUBRIC_GATE_FAIL and SPLIT fail at any bar. |
| 15 | No rubric repair lane | CONFIRMED | A non-promotable verdict makes the run return failed (:3606-3611). A grep finds no rubric-to-repair routing; rubricReaderRepairNote only repairs reader JSON. |
| 16 | promote-book / candidate release does not enforce the rubric verdict | CONFIRMED | cli.ts:2473-2482 gates only on evaluateCandidateReleaseGate (book-gate blockers). :2496-2531 records the rubric as evidence only. promotionService.ts, promoteBook.ts and candidateReleaseGate.ts contain 0 mentions of "rubric". |
| 17 | H5c: the panel's own numbers give about 76, with limits and density about 69 | PARTLY TRUE | Over the 14 panels in this run (FACTOR_SCORES across 19 chapters, weighted with REVIEW_WEIGHTS), the composite ranges 75.9-77.5, always below 80. The weakest factor median ranges 65-72. The latest completed panel (review-ba9e…, review-repair-20) has composite 77.5, limits 72, density 71, so it clears the 70 floor. The rubric is also a different instrument: 3 readers read only chapters 1/7/13/19 (`selectRubricChapterIndexes` = [0,6,12,18]). On the latest panel ch01 scores density 62 / limits 65 and ch13 scores limits 65. |
| 18 | H5c: fresh-reader instruments score about 12 points lower (Phase A 64.6 vs 76.4 on the same bytes) | PARTLY TRUE | The Phase A report (lines 9-17) says 76.4 was the previous session's 3-reader book-score handoff card, not the pipeline panel. The pipeline per-chapter panel on those same bytes gave 75.4/76.7/79.7/78.6. The 64.6 is the median of 6 fresh book-score readers. The catalog rubric is a port of that book-score instrument (catalogRubric.ts header), so 64.6 is the relevant comparison. |
| 19 | H5c: a rubric FAIL is final for a candidate | PARTLY TRUE | See row 14. |
| 20 | autoresume.sh forwards only 3 knobs | CONFIRMED | autoresume.sh:33-52 and 122-125 forward REVIEW_REPAIR_ROUNDS, OPERATOR_COMPILE_RETRIES and CAPTURE_INVALID_OUTPUT, plus the required RESUME_RUN_ID and LOG_PREFIX. The plist sets only PATH, and no shell profile sets any CHAPTERFLOW_ variable. |
| 21 | AUTORESUME.env lacks CHAPTERFLOW_REVIEW_REPAIR_ORDINALS | CONFIRMED | AUTORESUME.env has 5 lines: RESUME_RUN_ID=book-run-39a37d06…, LOG_PREFIX=fv8d (stale; the last manual prefix was fv8h), REVIEW_REPAIR_ROUNDS=10, OPERATOR_COMPILE_RETRIES=50, CAPTURE_INVALID_OUTPUT=1. |
| 22 | (Not in the draft) Forwarding REVIEW_REPAIR_ROUNDS has no effect | NEW FINDING | drive-franklin-v7.sh:46,54 set `CHAPTERFLOW_REVIEW_REPAIR_ROUNDS=4 CHAPTERFLOW_QC_REPAIR_RUNS=4` inline on the book-run command, which overrides any inherited value. The driver log confirms `round=1/4`. ORDINALS, QC_JUDGE_RUNS, RUBRIC_RUNS and RUBRIC_BAR are not set inline, so forwarding those would take effect. |
| 23 | A relaunch at ordinal 21 with the default ceiling of 20 would fail | CONFIRMED by reading the code; not run | A resume replays ordinals 1..20 with no model calls. Live evidence: the last round logged `exit=1 elapsed=3s new_attempts=0 … ordinal=21/40`, and the events show REVIEW_REPAIR_REPLAY SKIPPED for 17-21 within 14:58:02-03. After ordinal 20's stored FAIL review (ba9e…), #chooseLaneOrdinal walks from 21 to 20, an empty range (:1938), and returns BOOK_RUN_REPAIR_UNAVAILABLE "review-repair successor budget exhausted after 20 ordinals; …raise CHAPTERFLOW_REVIEW_REPAIR_ORDINALS (1-50) or start a fresh run" (:1981-1987, :3017-3045). The run never reaches ordinal 21 or its ERROR review, spends 0 model calls, and the driver WEDGE STOPs after 3 identical rounds. Today autoresume will not relaunch at all: the last marker is WEDGE STOP (autoresume.sh:86-96; log idle through 00:53Z), and it has never launched (0 `=== AUTORESUME` markers). A relaunch would happen only after an operator-started driver died without writing a stop marker. |

Scratch files: verify/detqc-verify.out, verify/scope.js, verify/bp15carve.js, verify/sample.mts, verify/src.sha, verify/copy.sha.