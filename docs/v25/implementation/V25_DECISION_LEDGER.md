# V25 S-Tier Program — Decision Ledger

Append-only. Every architectural decision, owner ruling, and plan deviation lands here with evidence.

| # | Date | Decision | Basis / evidence | Status |
|---|---|---|---|---|
| L-1 | 2026-07-16 | Audit verdict **C — Simplify** accepted by owner; findings V25-01..18 are program inputs | Audit report + owner instruction (this program's brief) | FINAL |
| L-2 | 2026-07-16 | **GPT-5.5 removed from target architecture** (no writer/reviewer/repair/fallback/benchmark dependency); candidates = gpt-5.6-sol/terra/luna | Owner directive §Model Policy | FINAL |
| L-3 | 2026-07-16 | Authoritative V25 ref = `origin/feat/v25-pipeline-live`; planning base SHA `97b78bf710e3ac434ff78acb2eee655051d433b4`; dedicated branch `plan/v25-s-tier-implementation`, worktree `/private/tmp/cf-v25-s-tier-plan` | `git ls-remote`, PR #401 state, worktree inspection (master plan §2) | FINAL |
| L-4 | 2026-07-16 | Target architecture = single author-first pipeline; D7 Claude-side close-read as ship gate; reviewer lanes advisory-only; one consolidated deterministic floor | Master plan §5; findings V25-01/02/04/05 | FINAL (pending owner approval of plan) |
| L-5 | 2026-07-16 | Bakeoff corpus = the three owner-audited chapters (made-to-stick-ch04, the-happiness-hypothesis-ch06, nudge-ch03). Rationale: sealed blind-adjudicated D7 baselines (67.7/68.8/70.8) make before/after attribution exact; they stress narrative construction, conceptual/evidence synthesis, and applied/practical instruction respectively. Known limitation: all three are business/behavioral nonfiction — matches the catalog's center of mass; accepted. | docs/v25/rubric-audit-2026-07-15/ (sealed); V25_OWNER_RUBRIC_RECONCILIATION.md | FINAL (pending plan approval) |
| L-6 | 2026-07-16 | Screening advancement bar pre-registered: 0 hard-gate failures AND D7 mean ≥75 (screening bar, deliberately below the ≥85 release bar); if NO config clears → program halt + owner escalation (audit change-condition C→D) | Master plan §10; bakeoff protocol §Stage 1 | FINAL (pending plan approval) |
| L-7 | 2026-07-16 | Tie-break order pre-registered: (1) D7 composite mean outside ±2.0 noise band → (2) worst-chapter min → (3) cross-sample variance → (4) repair burden → (5) floor failures → (6) latency p50 → (7) owner blind preference | Bakeoff protocol | FINAL (pending plan approval) |
| L-8 | 2026-07-16 | Finding V25-01 updated post-audit: P5 v5 (84/84, BLOCKED) produced the first-ever source-role qualification (`gpt-5.6-sol@xhigh`, identity-scoped); v6 ruling committed, possibly in flight. Core P0 unchanged. Advisory-role seeding proposal → owner D-2 | `docs/v25/reports/PILOT_ROLE_READINESS_LIVE_RESULT.md` @ 97b78bf71 | RECORDED |
| L-9 | 2026-07-16 | PR #405 merged (evidence split executed for the two largest trees); #401 residue → owner D-4 | gh pr view 405 (MERGED 2026-07-15); #401 = 1,794 files / +803,642 | RECORDED |
| L-10 | 2026-07-16 | Cost currencies = call count + latency (tokens structurally unmeterable on the subscription route); "cheaper model" claims carry no weight absent latency data | cost-tracker.ts NOT_METERED; metrics.ts tokens:null | FINAL |
| L-11 | 2026-07-16 | Rejected: full rewrite (E), repair-in-place of qualification machinery (B), Sol-everywhere & same-model self-review (evaluator independence), terra/luna routing tiers before evidence | Audit §Rejected approaches | FINAL |

| L-12 | 2026-07-16 | Drafter verification corrected three audit-era claims: (1) V25-04 refined — the author writer already spawns through the hermetic envelope; the env-pin bypasses only the routing DECISION; the genuinely unenveloped stack is the provider-router path (v23 compiler agents, categorizer, quizKeyJudge); (2) V25-18 partially stale on the live base — zz-fixtures/v24-archive absent at 97b78bf71; `source-reality-legacy-exemptions.json` is empty but load-bearing (never delete); (3) `generate-book` verb already exists (cli.ts:5764) bound to the legacy compiler — WP-601 REDEFINES it. Also surfaced: D7 marked "PENDING OWNER RATIFICATION" in V25_OWNER_RUBRIC_RECONCILIATION.md → owner D-8 | Drafting workflow wf_530cc384, notes D1/D2/D3/D4 | RECORDED |
| L-13 | 2026-07-16 | Resequencing accepted from drafter evidence: WP-301 precedes WP-201 (default flip requires the author route to be policy-resolved first); WP-205 moves to Phase 4 after WP-401/402 (consolidation needs to know which signals D7 owns; threshold values are WP-402's); WP-201 → WP-601 → WP-207 verb-retirement chain; WP-202 is quarantine-only, physical deletion at the WP-804/D-5 gate | D2/D3/D4 notes; master plan §7/§9 updated | FINAL (pending plan approval) |

| L-14 | 2026-07-16 | **OWNER APPROVED the master plan** (verbatim: "approved"). Per §16, recommendations adopted as defaults: D-1(b) let v6 conclude then freeze; D-2(a) advisory seeding; D-3(b) live-call authorization DEFERRED to Phase-6 entry (written authorization still required — no live calls in Phases 1-5); D-4(a) evidence move; D-5(a) Phase-8 deletion gate; D-6(a)+(i) D10 now, concatenation gated on authoring marker; D-7(a) one-time corpus freeze (executes in Phase 3/WP-701, compile calls ledgered and count against Phase-6 budget); D-8(a) D7 bar 85/80/3.0 ratified-by-approval, failing-chapters-only re-author, one round per book per audit; D-9(a) tellRate demote-to-warn, (b) provisional BASELINE_MODEL=gpt-5.6-sol, (c) ledger at state/run-ledger/ with size cap | Owner message 2026-07-16 | FINAL |
| L-15 | 2026-07-16 | **P5 v6 CONCLUDED: PILOT_ROLE_SET_READY** at live 8224f079a (freeze d54cc753; 6 identities, 432 base calls, 0 API, 0 gate weakening). Frozen roles: reader primary sol@high, reader audit sol@xhigh, source primary sol@xhigh, source adjudicator **gpt-5.5@xhigh (VOID for target architecture — no-5.5 directive; advisory lane runs without a blocking adjudicator since it never blocks)**, quiz sol@xhigh + deterministic checker. Effect: D-1 freeze condition met naturally; WP-202 retirement proceeds at Phase 2 with a clean closure artifact; D-2 advisory seeding upgraded to the frozen v6 set (minus the 5.5 slot); the old-line P6 staged pilot is SUPERSEDED by this program — no P6 calls should be spent under the old architecture | git show 8224f079a; PILOT_ROLE_FREEZE_V1.md | RECORDED |

| L-16 | 2026-07-16 | **DEVIATION (orchestrator-approved, owner-visible): WP-208 minted** — WP-102 proved two frozen IMP-24 qualification-instrument invariants in the DEFAULT suite block ALL src-touching WPs: imp24PreLiveFreeze.ts:555 requires the CURRENT manifest to contain exactly 16 contracts, and forwardProductionInstrumentSeal.ts byte-fingerprints every file under src/ against the retained live seal (any src edit → ForwardProductionInstrumentSealError). The Phase-2 packages that retire these instruments (WP-202/203/204) themselves edit src/, so plan-order resolution is circular. WP-208 (Lane 2, Phase 1, opus): (1) retained/historical verification binds ONLY to retained snapshots — never the current checkout; (2) the active-candidate whole-src seal check and the ===16 current-manifest check move behind CHAPTERFLOW_CAMPAIGN_INSTRUMENT_CHECKS=1, default-off with skip-with-reason ("superseded by S-tier program, L-16; formal retirement in WP-202/203/204"); (3) ZERO edits to retained evidence bytes; (4) also fixes the pre-existing publish-after-qc-preflight ENOENT class (4 tests scandir untracked state/chapters — absent in every fresh worktree of this lineage) via fixture-create-or-skip-with-reason. Owner approval judged NOT required: lifecycle mechanics inside the already-approved retirement direction, no evidence rewritten, no threshold weakened, fully reversible via the flag; reported transparently in the next owner update. Affected: unblocks WP-101/103/104/201/301/302/501 and every later src-touching WP | WP-102 escalation report (wp102-report.md); base-proof: imp24 tests 5/5 at pristine bb50d1dcf, 2 fail with any manifest/src edit | FINAL |

| L-17 | 2026-07-16 | **Batch 1 INTEGRATED @ 1134adfee** (merge order 208→102→103→104→206→405, no conflicts). Red-team verdicts: 5×PASS-WITH-NOTES, 0 FAIL. Load-bearing notes adopted: (a) rt102 — seal-collision blast radius corrected to 3 tests in 2 files (adds imp24f-candidate-instrument), covered by WP-208; imp24-local-activation-v2 shows 26 branch-env failures on ANY non-live branch worktree but 27/27 detached — G1 full suite MUST run on a DETACHED checkout; deep-level emission fields remain a documented gap (envelope-only closed-world) — carried to WP-101; corpus intel: emission-parity flags 5/140 shipped packages (2 genuine pre-existing drift) — WP-101 must scope to FRESH emissions. (b) rt103 NOTE 1/2 (non-hermetic resume test; false docstring) FIXED pre-integration by orchestrator micro-fix a1566295e: injectable briefLookup threaded through checkReaderBudgets/runBudgetsCapturingWarn from io.readBrief — leak-guard run now exit 0; reader-budgets 41/41 + budget-carry-lock 7/7 green (micro-fix exceeds WP-103 test-only scope by design; recorded here for G1 re-review). Post-integration smoke: package tsc green; 59/0/2-xenv across the 7 critical families; contract-validate PASS incl. emission-parity | wqjn057he reports; smoke run 2026-07-16 | FINAL |

| L-18 | 2026-07-16 | **Batch-2 red-team: 2×PASS-WITH-NOTES, 0 FAIL.** WP-101 merged (additive-only; fixture sha256 fd469b81… independently reproduced; real promoteBook path confirmed unstubbed; two dead adapter read-paths documented — memorableLines.location/why + quizQuestion.depthLevel stripped pre-emission; wrong-but-in-range key PROVEN to pass both adapters → WP-401 justification anchored). **WP-302 HELD unmerged by orchestrator sequencing decision:** rt302 proved integrating it alone turns ~24 downstream tests red across 7 files (BASELINE_MODEL/ROUTE_POLICY_VERSION importers expecting gpt-5.5) and found 2 HISTORICAL-identity sites aliased to the live baseline (spec.ts:51 55-H confirmatory cell; forwardLocalActivationMaterializer.ts:329 previousProfile) that silently re-point on the flip — the 55-H cell degenerates onto 56S-H. WP-501 dispatched STACKED on wp-302 HEAD 478a5212f with expanded charter: original purge + static checks + truthful downstream test updates + FREEZE historical-identity sites to literal gpt-5.5 (allowed as historical evidence) with explicit allowlist entries. 302+501 merge together after 501 red-team | why5hdrxb reports | FINAL |

| L-19 | 2026-07-16 | **WP-501 red-team PASS-WITH-NOTES + one MEDIUM defect FIXED pre-merge:** rt501 disproved the implementer STOP rationale by direct hash computation — both roleQualification runner candidate orders still aliased BASELINE_MODEL (degenerate sol-vs-sol post-cutover) and the imp24-successor-execution-identity frozen-SHA pin (b688f64a…) was authored for the gpt-5.5-frozen order and was RED since the WP-302 base; orchestrator micro-fix 1bbd27638 froze both to HISTORICAL_BASELINE_55, restoring the pin GREEN (90/0 across the affected families; no retained-evidence bytes). **302+501 stack MERGED @ a0fea3193.** **Orchestrator hygiene incident (owned):** git add -A in micro-fix commits a1566295e and 1bbd27638 swept untracked test droppings (5 state/books/zz-* current-run.json dirs + .npm-ci.log) into history; repaired with untrack commits on both branches (no history rewrite; droppings are regenerable non-evidence). RULE ADOPTED: orchestrator commits stage explicit paths only, never add -A | w31ik9y59 report; merge a0fea3193 | FINAL |

| L-20 | 2026-07-16 | **GATE G1 PASSED @ f3b83a4fe** (detached checkout): pipeline tsc OK; contract-validate PASS (incl. emission-parity); FULL no-API suite 3,032 pass / 0 fail / 12 xenv / 39 skip; root typecheck clean; root build compiled (138/138 pages). Two gate-caught defects fixed pre-certification: (1) root-strictness type error in the WP-101 test (813292d86 — pipeline tsc is laxer than root; check added to future WP evidence requirements: root typecheck required for any WP touching app/**); (2) imp24f candidate current-bytes binding was the one ACTIVE-CANDIDATE check WP-208 missed — gated behind the campaign flag with identical semantics (f3b83a4fe). Phase 1 complete: contracts frozen, 5.6-only routing, 5.5 purged with static gate, cross-boundary schema test live, seal lifecycle defused. Phase 2 dispatching: 301 → 201 → {202 ∥ 204 ∥ 402} → 203 | g1-gate-final.log | FINAL |

| L-21 | 2026-07-16 | **WP-301 integrated** (rt PASS-WITH-NOTES, no correctness defect): production author write+repair route through modelPolicy at normal-profile precedence; CHAPTERFLOW_AUTHOR_* env bypass dead (tamper-proven inert); overrides preserved call-explicit; bakeoff QC-delegation change contained+documented (carry to WP-703/704: winner no longer pins the QC repair author). Notes: (N1) the frozen RouteResultV1 sidecar has no persisted tier discriminator — normal-profile vs call-explicit distinguishable only via profileName+value identity; note for WP-503/704 evidence design, no action (contract frozen). (N2) stale env-override prose in docs/v24/STIER2-PLAN (historical, left). V25-04 fully closed on the routing half; default-flip half = WP-201 (dispatching) | wglsxuwxs report | FINAL |

| L-22 | 2026-07-16 | **LIVE-CALL INCIDENT (verifier process error) + WP-201 integrated.** (1) The rt201 verifier accidentally ran a NON-plan book-autopilot in cf-wp-201: ONE research codex call COMPLETED (exitCode 0, 128.8s, gpt-5.6-sol@high, sidecars retained at cf-wp-201 logs/exec 162107-auto-research-*) and one retry was killed pre-completion (manifest only, 162317). Program live-call count is now 1 completed + 1 partial, both UNAUTHORIZED, both fully envelope-documented — the first live evidence that the 5.6 provisional-56 routing works as designed. (2) The verifier used pkill -f codex, which may have signaled the OTHER session processes; a P6 pilot session (start-with-why ch03, sol@xhigh) started 13:23:32 and is running — owner should check that session own ledgers for a killed/retried session near 13:22-13:23. (3) DISCOVERY: the old-line P6 stage-1 pilot is LIVE (authoring start-with-why ch03 on /private/tmp/ChapterFlow-books-v25-live) — D-10 is now factual, not hypothetical. (4) CORRECTIVE RULES (all future agents): non---plan CLI verbs FORBIDDEN outright; live-capable verbs never run even for guard testing (fixture the guard at module level); process kills by exact PID only, NEVER pkill patterns. (5) WP-201 itself verified correct on all 7 attack surfaces (rt PASS-WITH-NOTES) and is MERGED — the incident was verifier procedure, not WP code. V25-04 now FULLY closed (routing + default) | wpsvv8ski report; cf-wp-201 logs/exec sidecars; ps evidence | FINAL |

| L-23 | 2026-07-16 | **WP-202 + WP-402 integrated** (both rt PASS-WITH-NOTES, zero correctness defects). 202: 50 campaign verbs fail-closed behind --campaign (set COMPUTED from families minus exemptions — cannot drift); ship path has ZERO runtime reach into the forward/qualification family (BFS import-closure guard with proven teeth); CAMPAIGN_QUARANTINE.md closure record; V25-01 ship-path exposure closed, V25-05 partially (deletion at WP-804/D-5). 402: tellRate demoted to warn (no fail path exists — verifier-proven synthetically AND end-to-end: a tellRate warn cannot enter failing[] or trigger regen); shortest-side lengthTell cap still blocks; config values byte-identical; calibration: old gate would have failed 1,718/1,903 chapters across 131/140 shipped books — V25-11 closed. rt202 notes: known F-018 test-scratch leak surfaces under the broader suites (pre-existing); an observability-freeze dry-build throws against current repo state (pre-gate behavior, restored exactly) | wm28hm143 reports | FINAL |

| L-24 | 2026-07-16 | **WP-203 + WP-204 integrated** (both rt PASS-WITH-NOTES). 203: all 5 thinning targets proven deletion-blocked (verifier independently confirmed 2 enumerations — no fabricated blockers); marked SUPERSEDED-BY-S-TIER with the exact base→successor map; **rt203-N2 carried to WP-804: the helper re-homing dedup (shrinking the bases) was declined and is now explicitly WP-804 scope alongside physical deletion**. 204: legacyReaderReviewAdapter DELETED (237 LOC incl. test; zero importers; version-wedge invariant independently re-pinned); Stage-Q v3 made self-contained (byte-equivalent re-homing, v2 zero live importers); stageQv2 quarantined — REAL blocker: retained .mts owner-drivers import the full v2 API and closed-registry-sync asserts their closure message, hard-delete would break ESM resolution pre-halt; forwardGoldEvaluatorInstrument quarantine-marked (self-hash data-derived, comment-safe; deletion at WP-804 with its 4 forward-stack importers). V25-05 marking complete; V25-10 residue quarantined. Phase-2 packages ALL landed → gate G2 | wlwnt5azh reports | FINAL |

| L-25 | 2026-07-16 | **GATE G2 PASSED @ ef41abe57** (detached): suite 3,058/0/12xenv/39skip; tsc + contract-validate + root typecheck + root build green. Phase 2 CLOSED. One gate-caught drift fixed pre-certification (WP-301 comment stretched the spawn→logSession scanner window past 1,200 chars; spawn was always logged; comment condensed — invariant strength unchanged). **Phase-3 re-slice (conservative, post-L-22):** WP-701 split — its model-free parts (sealed D3-baseline packet, fixture packaging, harness wiring) fold into WP-303; the LIVE compile-chain corpus freeze (D-7a) defers to Phase-6 entry alongside the D-3 written authorization, keeping Phases 1–5 strictly zero-live-calls. Phase 3 dispatched: 303+701a, 304, 305, 404 in parallel (disjoint file sets) | g2-gate-final.log | FINAL |

| L-26 | 2026-07-16 | **Phase 3 INTEGRATED** (rt: 303 clean PASS, 304/305/404 PASS-WITH-NOTES, all notes doc-precision). 303+701a: sealed-baseline packet hash-pinned to retained evidence (verifier recomputed pins independently + live-mutation-proved the version-pin teeth); F-1/2/3/5 anchored to real deterministic critics, F-4/6/7/8 honestly documented as instruction-text-only (no deterministic critic exists — carried as a Phase-4/8 note). 304: ship path PROVEN zero-router-calls (independent 220-module reachability graph); router now policy-resolved with per-call ProviderRouteResultV1 sidecars on success AND every failure branch; env kill-points verified by tamper. 305: 18th contract source-projection-boundary (tool-generated manifest byte-verified); source-review packet ⊃ writer projection proven on real data; the advisory lane input is source-EQUIPPED — V25-10 root cause closed at the contract level. 404: de-facto cap-1 → bounded 2-round loop with per-round fresh-evidence gate (verifier traced both independent bounds; 3rd round impossible); REPAIR_EXHAUSTED taxonomy at every spent-lane halt. Gate G3 opening | wc4rhkbxz reports | FINAL |

| L-27 | 2026-07-16 | **GATE G3 PASSED @ f8c74a760** (detached): suite 3,107/0/12xenv/39skip; tsc + contract-validate + root typecheck + root build green. Phase 3 CLOSED. Phase 4 dispatching: WP-401 first (D7 ship gate, owner-ratified 85/80/3.0 per D-8/L-14), then {403, 205} serialized on the promote/review surface | g3-gate2.log | FINAL |

| L-28 | 2026-07-16 | **WP-401 D7 SHIP GATE integrated @ e6a330c2d after THREE red-team rounds** (FAIL forged-custody → FAIL tampered-adjudication → PASS-WITH-NOTES gate-status residual → closed → PASS-WITH-NOTES final). Final state: publish/promote of a NEW/CHANGED book requires a sealed D7 receipt binding content hashes + the ratified bar (mean>=85/min>=80/core>=3.0/layer-independence/±3.0-void); the verdict is FULLY bound against tamper — scores+domains arithmetically, gate statuses by blind-pair rater consistency, custody+pair-chain by validatePairChain, audit_id sanitized. REQUIRE mode (CHAPTERFLOW_REQUIRE_D7_SHIP_GATE=1, owned by WP-601/802) mandates the retained audit dir. 140 historical books EXEMPT by byte-identity (no retroactivity). On-fail: 1 re-author round then BLOCKED_QUALITY_BAR. Decisive proof: the gate FAILS the 3 owner-audited chapters at their sealed 67-70 scores (book CDS 69.08 vs 85 bar). Suite d7-ship-gate 32/0; families 125/0; do-not-touch files byte-identical. V25-02 CLOSED (the invalid internal composite is replaced by the owner-rubric close-read as the ship gate). Two residuals → owner D-11 (both inside the accepted state-writer/keyless boundary; no runtime ship path) | rt401/wvc8si5co/wb2ooqfhe/w58uziw6s | FINAL |

| L-29 | 2026-07-16 | **WP-403 + WP-205 integrated** (both rt PASS-WITH-NOTES, notes cosmetic). 403: KEY FINDING — the split-lane reviewer was ALREADY off the ship path (only the quarantined forward conductor read finalStatus; verifier 239-module closure confirmed zero ship reach); WP built the advisory mechanism (severance→repair inputs, shipBlocking literal-false, source-EQUIPPED via assertSourceReviewPacketEquipped before the model seam → V25-10 root cause closed, all reviewer scales pinned [0,100] → V25-16 closed, different-model/M9 fallback + precision-drop). 205: consolidated the deterministic floor; the real sprawl was gate RE-INVOCATION across lifecycle stages — a content-addressed FloorLedger dedups it (majors-scan no longer re-runs the ship/book gate on identical bytes); verifier INDEPENDENTLY disproved the stale-cache attack (4 content-key attacks: changed bytes never inherit a prior pass); zero deterministic outcome regressed (verbatim delegation, no leaf critic touched); floor runs BEFORE the D7 gate. Phase 4 packages ALL landed → gate G4 | wocwle3tf reports | FINAL |

| L-30 | 2026-07-16 | **GATE G4 PASSED @ 116f214d7** (detached): suite 3,176/0/12xenv/39skip; tsc + contract-validate + root typecheck + root build green. Phase 4 CLOSED (D7 ship gate live; reviewer lanes advisory + source-equipped; deterministic floor consolidated). One gate-caught cross-WP interaction fixed: WP-403 comments naming the VOID gpt-5.5 adjudicator tripped WP-501 forbidden-model gate → reworded 5.5-era (gate kept strict, not allowlisted). Phase 5 dispatching: WP-503 (unified ledger) ∥ WP-504 (fallback/fail-closed config) ∥ WP-602 (preflight) → WP-601 (generate-book) → WP-603/604 | g4-final.log | FINAL |

| L-31 | 2026-07-16 | **Phase 5 batch 1 integrated** (503/504/602, all rt PASS-WITH-NOTES). 503: unified per-run call ledger (codex-exec + Claude-side D7 raters) at state/run-ledger/, size-capped, no-unlogged-call invariant extended to the Claude side, tokens explicitly NOT_METERED — V25-15 CLOSED. 504: UnsupportedModelConfigError extends RoutePreflightError (one hierarchy); NO silent fallback (fail-closed on unsupported 5.6; alternate only by explicit config); no 5.5 in ROLLBACK_ORDER. 602: deterministic generate-preflight verb (worktree/base-SHA/branch/model-config/schema-fixtures/name-bank checks; exit 2 fatal; reuses publishBranchError/contractFreeze/loadNameBank/WP-504 check). Post-merge smoke 87/0. WP-601 (generate-book, wires all three) dispatched → then 603/604 → gate G5. rt notes carried: test-hygiene leak under the non-gitignored run-ledger dir (pre-existing autopilot tests; footgun for git add -A — orchestrator uses explicit-path staging so contained) | wqy8k8mnf reports | FINAL |

| L-32 | 2026-07-16 | **WP-601 generate-book integrated** (rt PASS-WITH-NOTES + 1 note fixed pre-merge). THE OPERATOR DELIVERABLE: one command runs the author-first pipeline end-to-end via the 12-step lifecycle (preflight WP-602 → config precedence → model-confirm WP-504 → runId+D7-REQUIRE WP-503/401 → author conductor bounded-repair WP-201/404 → CAS → floor+D7 gate WP-205/401 → assemble/strip/verify×2/publish → validate → truthful exit 0/1/2/3 → print package+D7-receipt+ledger paths). Verifier-proven: print==run (no model/effort lie — --model AND now --effort both fail-closed on non-wired values, orchestrator NOTE-1 fix c/w a pinning test), D7 quality-block→exit 3 (sidecar only on true verdict≠PASS; integrity blocks→1 correctly), zero live-call in any tested path, resume-only-missing + duplicate-lock, clobber-safe, legacy behind --compiler. Auto-publish default → owner D-12. generate-book redefined off the legacy compiler (V25-04/terminal-autonomy directive satisfied). WP-603/604 dispatched → gate G5 | warhno0z1 report | FINAL |

| L-33 | 2026-07-16 | **FUNCTIONAL DEFECT found by WP-604, orchestrator-verified → WP-602b minted.** runGeneratePreflightChecks unconditionally runs runDoctorChecks(bookId), whose per-book checks (checkCanonicalChapterSet/checkChapterNumbers/checkTocContract/checkSweepHistory/checkDualBrief) are FATAL for any bookId with no pre-existing canonical index — but a FRESH generate-book run (the primary use case) has no chapters yet (the pipeline creates them). Net: generate-book cannot START a new book — it always hits PREFLIGHT_FATAL. This blocks Phase 6 (bakeoff authors the 3 fixed chapters; pilot authors a new book). Root cause: WP-602 reused the existing-book doctor as the fresh-authoring preflight. FIX (WP-602b): gate the per-book EXISTING-STATE checks on (resume || book-has-canonical-state); a fresh new book runs global checks only (shadow-state/imports/locks/worktree/base-sha/branch/model/schema/name-bank/D7-tooling all stay). Sequenced AFTER 603/604 integrate (shared generateBookCommand.ts). Not a WP-604 defect — 604 correctly ROUTED AROUND it with a fixture and escalated | WP-604 escalation #3 (wp604-report.md); doctor.ts:524 verified | FINAL |

| L-34 | 2026-07-16 | **WP-603 + WP-604 integrated** (both rt PASS-WITH-NOTES). 603: stage progress (quiet=milestones/--verbose=all, halts never silent), preflight-report + run-summary JSON artifacts, operator runbook; one rt doc-overclaim (run-report exists for ANY non-zero exit) SCOPED to conductor-reached exits (1/3) pre-merge. 604: definitive CLI regression suite (real-subprocess exit codes, real acquireBookLock duplicate-lock, resume-at-5-boundaries via real CAS primitives, validate-only-on-bad-fixtures) — the suite that FOUND the L-33 preflight defect. Post-merge smoke 84/0. WP-602b (L-33 fix) dispatched on top | wjq8kq4jk reports | FINAL |

| L-35 | 2026-07-16 | **WP-602b integrated — L-33 CLOSED** (rt PASS-WITH-NOTES, notes non-defects). The generate-book preflight now gates the per-book existing-state checks on (resume || book-has-canonical-state): a genuinely fresh new book passes preflight and can START (verifier constructed it on real disk); resume/existing/CORRUPT-state books still run integrity checks fatal-capably (a corrupt/0-byte index is treated as EXISTING → surfaced, never fail-open as fresh); all 11 globals unchanged; runDoctorChecks byte-identical (standalone doctor verb + optimizedPipeline preflight untouched); the fail-open override is test-only, not operator-reachable. generate-book is now functionally able to author a new book — the Phase-6 prerequisite. Opening gate G5 | wxwmmkzm4 report | FINAL |

| L-36 | 2026-07-17 | **GATE G5 PASSED @ c7746bab5** (detached): suite 3,323/0/12xenv/39skip; tsc + contract-validate + root typecheck + root build green. **PHASE 5 CLOSED → all 5 BUILD PHASES COMPLETE.** The model-free pipeline is done: author-first default, 5.6-only central routing (no 5.5) inside the hermetic envelope, campaign machinery quarantined, D7 owner-rubric close-read as the sole ship gate (tamper-bound, 3 red-team rounds), consolidated deterministic floor, advisory source-equipped reviewers, unified call ledger, fail-closed model config, generate-book terminal command (functionally able to author a new book, L-33 closed). **PROGRAM NOW AT THE PHASE-6 HARD STOP** — every remaining WP (bakeoff 502/701/702/703/704/705, pilot 802, hardening 801/803/804, deletions 207/804) requires the first intentional LIVE model calls and owner D-3 authorization. Handing back to owner | g5-gate.log | FINAL |

| L-37 | 2026-07-17 | **OWNER DECISIONS D-3/D-7/D-10/D-12 RECEIVED — PHASE 6 AUTHORIZED.** All four answered as recommended (AskUserQuestion, this session): **D-3 = AUTHORIZE, hard ceiling 150 codex sessions total** (capability probe ≤10 + staged Sol/Terra/Luna bakeoff + the deferred WP-701 one-time corpus-freeze compile-chain per L-25; every codex AND Claude-side call ledgered per WP-503). The approval explicitly bundles **D-8 RATIFIED** (D7 ship bar mean≥85 / min≥80 / core domains≥3.0; failing-chapters-only re-author, ONE round per book per audit; never retroactive on the 140 historical books) and the three binding acknowledgments: (i) terra/luna may probe UNSUPPORTED_MODEL_CONFIG → the bakeoff shrinks to the supported set, never substitutes; (ii) the pre-registered screening halt (no config reaches D7 mean ≥75 → program halt + owner escalation, audit change-condition C→D) is binding — the bar is never lowered mid-flight; (iii) tie-break 7 and pilot acceptance require owner blind-read availability. **D-7 = (a)** one-time compile-chain run (research → packets → briefs) against the 3 fixed chapters' source material, frozen + hashed as the corpus; calls count against the ceiling. **D-10 = (a)** HALT old-line P6 — the owner stops that session themselves; this program is the sole authorized live-call vehicle and continues to never touch that worktree. **D-12 = (b)** generate-book default flips to halt-at-ready with --publish opt-in; the one-line flip is booked into WP-802 (pilot). Phase 6 OPEN. Dispatched at authorization: WP-801 + WP-701-mf (both model-free, workflow wf_3c695a3b-192, worktrees cf-wp-801/cf-wp-701 @ 11e0362ac) and WP-502 build (cf-wp-502 @ 11e0362ac); live probe execution follows 502 integration under orchestrator custody | this session (owner AskUserQuestion answers, verbatim in V25_OWNER_DECISIONS.md) | FINAL |

| L-38 | 2026-07-17 | **WP-801 + WP-701-mf INTEGRATED (both rt PASS).** 801 (@1d3fd38eb, merged 650f49450): ten-scenario failure-injection suite, 662 lines additive-only, zero product-code edits; rt ran 4 guard-mutation probes (identity check, isSupportedModelId, write-attempt ladder, doctor fatal-level) — every probed test went RED under guard disable, proving no test stubs over its own guard; lock scenario verified structurally (mutation-probing it would have let the real autopilot run past the lock — correctly refused); 2 non-blocking observations (5b resume-skip claim rests on a conductor double — real path covered in resume-mid-kill-cas:391; cap assertion derives from the exported constant — loop-exceeds-cap IS caught by probe C). Notable: all ten scenarios were ALREADY fail-closed in product code — zero guard gaps found. 701-mf (@a2c8f3fd6 + orchestrator rt-hardening micro-fix 5b5343800, merged e912bf63f): sealed corpus packet — manifest with 3 units, full-precision diagnostics (70.75657894736842 / 67.66447368421052 / 68.8157894736842) recomputed from sealed adjudicated JSON via instrument arithmetic, content-hash POINTERS (no sealed-byte copies), 6-file provenance chains, authoringSource=UNRESOLVED → not-ready-for-bakeoff fail-closed; rt confirmed byte-stability independently + swap-defense; 2 rt findings FIXED pre-merge: FINDING-1 denylist→ALLOWLIST authoringSource resolution (placeholders ""/TODO/PENDING/etc. now veto readiness — matters at the imminent D-7 freeze) + FINDING-2 direct adjudicated-record source_hash/book_id/chapter cross-binding (a swapped sealed record now fails directly; note: sealed book.book_id carries the UNIT id, e.g. "nudge-ch03"). Post-merge smoke on plan head e912bf63f: tsc clean; pipeline-failure-injection 12/0, bakeoff-corpus-fixtures 12/0, generate-book-command 35/0, rubric-audit-instrument 12/0. WP-702 dispatching off e912bf63f-lineage | wf_3c695a3b-192 reports; merges 650f49450 + e912bf63f | FINAL |

Owner answers beyond the approval will be appended here verbatim when received.
