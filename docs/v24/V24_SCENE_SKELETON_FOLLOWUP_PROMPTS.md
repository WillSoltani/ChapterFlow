# V24 Scene-Skeleton Follow-up Prompts (classification C — first-write monoculture)

**Status: NOT TRIGGERED.** Written 2026-07-10 by the scene-origin campaign per its Phase 16
policy (classification C → initial scene-dealer prompt). Do not implement until the owner
explicitly launches the campaign. Evidence base: `V24_FRESH_SCENE_ORIGIN_GOLD_RUN_REPORT.md`
§8/§10/§23 (range first-write coding + before/after), radical-candor CF-J report §19/§22.

> **ROLLBACK NOTE (2026-07-10, appended):** the SOL migration + range campaign were abandoned
> and rolled back to the GPT-5.5 baseline `acdc51c13`; this file is retained as evidence by
> owner instruction. The findings below stand on their own; the range evidence corpus itself
> was deleted. D-1's premise (SOL as active writer model) no longer holds at the restored
> baseline — it applies only if/when a SOL migration is re-attempted.

## What is now known (do not re-derive)
- The mold is **minted at first write** by the authoring card's own recipes, independent of
  model (gpt-5.5@xhigh and gpt-5.6-sol@high produce the same class) and NOT amplified by repair
  (range: regen wave diluted the kit; churn MEDIUM). Prop-level de-stamping (CF-I/CF-J) worked —
  the mold re-expressed at the rhythm/recipe level.
- The five stamped surfaces, in priority order:
  1. **Example-arc deck** — one dramatic transaction re-dealt: "error quietly hardening →
     check-in/backward-trace rescues it just in time, or freeze-frame cut before the outcome."
     House vocabulary: "quietly", "check-in", "answers for", "just in time / just before",
     "half-works / The move worked", ledger/cost-column imagery, small-object sensory twitch.
  2. **Practice shells** — audit-for-10-minutes / scripted-exact-line / count-record-compare /
     teach-one-person, cloned pairwise across chapters.
  3. **MemorableLines recipe** — reversal + redefinition + short-command, self-documented in
     why-fields.
  4. **Hook forms** — one verbatim-clone class observed ("Everyone agreed, but no one knew…").
  5. **Freeze-frame/rescue rhythm** as the default example resolution.

## Prompt C-1 — scene-shape deal (the campaign core)
Add a **dealt scene-shape axis** to the machine brief VARIETY deal (compiler `chapterBrief.ts` /
`briefRotation.ts`, same dealing style as leads/devices): a per-chapter draw from a small
orthogonal deck of dramatic shapes (e.g. discovery-forward, comparison-of-two-paths,
process-walkthrough, failure-postmortem-unrescued, decision-under-uncertainty-resolved-late,
rescue-just-in-time capped at ≤2 chapters/book). Render it as a LEAD SHAPE line on the card with
2-3 concrete don'ts drawn from the OTHER chapters' deals ("this chapter does NOT use a
last-moment rescue or a cut-before-outcome"). Deal↔pin tests: same-book deals must cover ≥N
distinct shapes; rescue/freeze-frame shapes bounded; deterministic redeal stability (the
weekly-practice-plan dealing tests are the pattern to copy).

## Prompt C-2 — practice-shell and memorable-line de-recipe
De-mint the recipes the card itself teaches: (a) practice-shell variety deal (the four observed
shells become a dealt rotation with a ≤2-chapters-per-shell cap, mirroring contentDeviceDeal);
(b) memorableLines: replace the fixed reversal/redefinition/command taxonomy in the card with a
dealt 2-of-N move set per chapter; why-fields must stop naming the move taxonomy verbatim
(write-contract advisory, not a gate).

## Prompt C-3 — house-vocabulary watchlist (detector, advisory-minor)
Extend C33/BP34 (machineryPhrases single-source pattern) with the accounting-drama kit as a
BOOK-LEVEL echo family: "quietly harden*", "check-in" (non-literal uses), "answers for",
"just in time/just before" rescue framing, "half-works"/"The move worked", ledger/column-of-cost
imagery, cut-away-before-the-outcome constructions. Fire at ≥4 chapters carrying ≥2 kit items;
route through registerAdvisories (write-retry + regen + repair cards). Corpus-pin on
gold/HOM/multipliers/culture-code/radical-candor/range so thresholds start honest.

## Decision item D-1 (owner, separate from the scene campaign) — SOL fabrication-labeling
gpt-5.6-sol@high writers narrate invented stand-ins declaratively (10/12 first-round ship=false
on range; zero such waves on the two 5.5@xhigh books). Options: (a) accept the review-net cost
(~44 extra reads + 10 regens/book); (b) one card line in AUTHOR_HOUSE_RULES making hypothetical
framing an explicit self-verify item (a prompt change — requires the owner to waive the
"no prompt changes for the new model" constraint, with an A/B book to prove no texture cost);
(c) revert writers to 5.5@xhigh pending SOL tuning. Evidence: range reviews rounds 1-2.

## Explicitly out of scope
Any repair-lane "architecture preservation" work (classification B's lane — the evidence says
repair is not the driver); gate changes; acceptance-policy changes; mass edits to shipped books.
