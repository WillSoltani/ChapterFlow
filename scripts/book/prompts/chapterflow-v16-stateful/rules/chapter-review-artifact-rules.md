
# Chapter Review Artifact Rules

Every validated chapter must also produce:
- `validated/chXX.review-package.json`

This wrapper must include:
- book metadata
- exactly one chapter
- packageId on `{bookId}.chapter-gate.chXX` pattern for chapter-gate artifacts

The wrapper is for review and validation.
The final release package is separate.
