
# No Generator Rules

Never replace the chapter loop with:
- a bulk book generator
- a per-book generator script
- a seed-to-JSON builder
- an all-at-once continuation shortcut

Do not create or reuse scripts that author reader-facing content.

If you find a repo script that writes breakdown text, examples, prompts, quiz questions, cards, or review prose from seed fields, do not use it.
