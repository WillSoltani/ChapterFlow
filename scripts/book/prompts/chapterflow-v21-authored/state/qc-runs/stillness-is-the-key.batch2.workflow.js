export const meta = {
  name: 'qc-run',
  description: 'Semantic QC of a book: tooled blind-key verification, dual-lens bar reads, cross-chapter sweep, adversarial adjudication, then qc-attest',
  phases: [
    { title: 'Blind keys', detail: 'derive every quiz answer from quiz-blind output only, verify mechanically' },
    { title: 'Bar read', detail: 'two independent lenses per chapter against the publishable-bar rubric' },
    { title: 'Sweep', detail: 'one cross-chapter read for skeletons, persona drift, repeated shapes' },
    { title: 'Adjudicate', detail: 'adversarially verify every corruption-class claim before it counts' },
    { title: 'Attest', detail: 'record the verdicts via qc-attest (reviewer = this harness run)' },
  ],
}

// ─── Generated by `qc-run <bookId>` — do not hand-edit; re-generate instead. ──
// CONFIG carries the LIVE rubric/weights/floors from src/critics/semantic/
// publishableBar.ts at generation time, so prompts cannot drift from the code.
const CONFIG = {
  "bookId": "stillness-is-the-key",
  "pipelineDir": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored",
  "reviewer": "harness:qc-run-stillness-is-the-key-2026-06-11",
  "chapters": [
    {
      "n": 10,
      "file": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/stillness-is-the-key-ch10.v21-native.chapter.json",
      "sidecar": "/Users/radinsoltani/ChapterFlow/.chapterflow/runs/stillness-is-the-key/20260611-142459/sidecars/source/ch10.source.json"
    },
    {
      "n": 11,
      "file": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/stillness-is-the-key-ch11.v21-native.chapter.json",
      "sidecar": "/Users/radinsoltani/ChapterFlow/.chapterflow/runs/stillness-is-the-key/20260611-142459/sidecars/source/ch11.source.json"
    },
    {
      "n": 12,
      "file": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/stillness-is-the-key-ch12.v21-native.chapter.json",
      "sidecar": "/Users/radinsoltani/ChapterFlow/.chapterflow/runs/stillness-is-the-key/20260611-142459/sidecars/source/ch12.source.json"
    },
    {
      "n": 13,
      "file": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/stillness-is-the-key-ch13.v21-native.chapter.json",
      "sidecar": "/Users/radinsoltani/ChapterFlow/.chapterflow/runs/stillness-is-the-key/20260611-142459/sidecars/source/ch13.source.json"
    },
    {
      "n": 14,
      "file": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/stillness-is-the-key-ch14.v21-native.chapter.json",
      "sidecar": "/Users/radinsoltani/ChapterFlow/.chapterflow/runs/stillness-is-the-key/20260611-142459/sidecars/source/ch14.source.json"
    },
    {
      "n": 15,
      "file": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/stillness-is-the-key-ch15.v21-native.chapter.json",
      "sidecar": "/Users/radinsoltani/ChapterFlow/.chapterflow/runs/stillness-is-the-key/20260611-142459/sidecars/source/ch15.source.json"
    },
    {
      "n": 16,
      "file": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/stillness-is-the-key-ch16.v21-native.chapter.json",
      "sidecar": "/Users/radinsoltani/ChapterFlow/.chapterflow/runs/stillness-is-the-key/20260611-142459/sidecars/source/ch16.source.json"
    },
    {
      "n": 17,
      "file": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/stillness-is-the-key-ch17.v21-native.chapter.json",
      "sidecar": "/Users/radinsoltani/ChapterFlow/.chapterflow/runs/stillness-is-the-key/20260611-142459/sidecars/source/ch17.source.json"
    },
    {
      "n": 18,
      "file": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/stillness-is-the-key-ch18.v21-native.chapter.json",
      "sidecar": "/Users/radinsoltani/ChapterFlow/.chapterflow/runs/stillness-is-the-key/20260611-142459/sidecars/source/ch18.source.json"
    }
  ],
  "quizCounts": {
    "1": 9,
    "2": 9,
    "3": 9,
    "4": 9,
    "5": 9,
    "6": 9,
    "7": 9,
    "8": 9,
    "9": 9,
    "10": 9,
    "11": 9,
    "12": 9,
    "13": 9,
    "14": 9,
    "15": 9,
    "16": 9,
    "17": 9,
    "18": 9,
    "19": 9,
    "20": 9,
    "21": 9,
    "22": 9,
    "23": 9,
    "24": 9,
    "25": 9,
    "26": 9,
    "27": 9,
    "28": 9,
    "29": 9,
    "30": 9,
    "31": 9,
    "32": 9,
    "33": 9,
    "34": 9
  },
  "goldFile": "/Users/radinsoltani/ChapterFlow/scripts/book/prompts/chapterflow-v21-authored/state/chapters/daring-greatly-ch01.v21-native.chapter.json",
  "rubric": {
    "quiz_key_correctness": "Cover correctIndex. From the prompt + choices + source, derive the correct answer yourself, THEN reveal the key. If you confidently land on a different index, or the explanation argues for a choice other than the keyed one → CORRUPTION (wrong key). FP-guard: a misconception keyed correct IS correct when the stem asks for it ('What is the simplistic reading to avoid?').",
    "quiz_distractor_quality": "Distractors must be realistic wrong answers a thoughtful reader could pick. DRAFT if a distractor is the correct sentence wearing a junk prefix ('Reverse/Flatten/Prefer X over'), a generated source-summary ('would be managed through'), the only 'clean' choice is the key (format-identifiable), or the answer is decided by a trailing container-noun. FP-guard: genuinely tempting near-misses are GOOD, not defects.",
    "card_learning_value": "The front must be a question; the back must ANSWER it in the card's own words and test understanding, not source-recall. DRAFT if recall-only or front is a bare label. CORRUPTION if the back doesn't answer the front, is pasted verbatim from the breakdown, or is truncated mid-word.",
    "example_coherence": "A concrete scene with a named human acting. CORRUPTION if a concept-label is the subject/object ('Cleo lifts a productive vulnerability folder'), a '<Name> studies <label>' skeleton, a fixed-timestamp concept-label HEADER repeated each chapter, or scaffold titles ('Source Moment N.1'). DRAFT if whatToDo is a proposition rather than an action, or if a source anchor appears as SET DRESSING rather than in the scene's logic — a case card pinned to a wall, a quote 'in the margin', a flyer about the source's anecdote ('On the plant wall, a case card reads Henry Ford, Model T'): anchor-as-prop reads as product placement and marks an ungrounded scene wearing a grounding costume. FP-guard: a timestamp INSIDE a coherent scene is FINE — only the label-HEADER + identical-fixed-time-every-chapter is the defect; an anchor a character genuinely reasons about or reacts to is GOOD grounding.",
    "prose_coherence": "fastRead/deepRead/fullRead read as human prose that teaches. CORRUPTION if a clause repeats ~25× (templated loop), a '<Concept> means The <concept> is…' seam, ends mid-sentence, or fastRead just restates the thesis. DRAFT if a tier is wall-to-wall ABSTRACTION — chains of abstract nouns with no person, scene, or number for paragraphs ('scarcity culture shrinks courage by teaching lack first'); the plain-language bar (2026-06-11 direction): every abstract claim is followed within two sentences by something a reader can SEE, terms-of-art are explained in everyday words on first use, and a tier opens concrete, not with a thesis. FP-guard: a consistent pedagogical opener across chapters ('The mechanism is:') is a convention when the content differs and reads as prose; precise technical terms explained plainly once are GOOD, not abstraction.",
    "memorable_line_quality": "Each memorable line is a portable aphorism. DRAFT if it is a 16-23-word explanation or an enumeration rather than a compact, complete claim.",
    "plan_actionability": "ifThenPlans context = a situational trigger; plan = an imperative If-X-then-Y using the chapter's one named reader tool. DRAFT if context is a source/proper-noun label, or plan is a pasted breakdown sentence or editor language ('use <source> as the source check', 'revisit the hard edge').",
    "factual_accuracy": "Named-framework enumerations are complete and correctly named (BRAVING = 7 items incl. 'Vault', not 6 with 'Vault' renamed 'confidentiality'). CORRUPTION on a wrong/incomplete named enumeration or a fabricated fact. Needs the source as ground truth; when absent, mark 'could not verify' (never silently pass)."
  },
  "weights": {
    "quiz_key_correctness": 18,
    "example_coherence": 16,
    "prose_coherence": 14,
    "quiz_distractor_quality": 14,
    "card_learning_value": 12,
    "plan_actionability": 12,
    "factual_accuracy": 8,
    "memorable_line_quality": 6
  },
  "corruptionAxes": [
    "quiz_key_correctness",
    "example_coherence",
    "prose_coherence",
    "factual_accuracy"
  ],
  "publishableFloor": 85,
  "axisFloor": 0.6
}

const P = CONFIG.pipelineDir
const pad = (n) => String(n).padStart(2, '0')
const CLI = `cd ${P} && npx tsx src/cli.ts`

// ── Schemas ──────────────────────────────────────────────────────────────────

const KEYS_SCHEMA = {
  type: 'object',
  required: ['chapterNumber', 'mismatches', 'allMatch'],
  properties: {
    chapterNumber: { type: 'number' },
    allMatch: { type: 'boolean' },
    mismatches: {
      type: 'array',
      items: {
        type: 'object',
        required: ['questionIndex', 'keyedIndex', 'derivedIndex', 'reasoning'],
        properties: {
          questionIndex: { type: 'number' },
          keyedIndex: { type: 'number' },
          derivedIndex: { type: 'number' },
          reasoning: { type: 'string', description: 'why you believe your derived index over the key' },
        },
      },
    },
  },
}

const LENS_SCHEMA = {
  type: 'object',
  required: ['chapterNumber', 'axes', 'notes'],
  properties: {
    chapterNumber: { type: 'number' },
    axes: {
      type: 'array',
      items: {
        type: 'object',
        required: ['axis', 'score', 'hits'],
        properties: {
          axis: { type: 'string' },
          score: { type: 'number', description: '0..1 per the rubric' },
          hits: {
            type: 'array',
            items: {
              type: 'object',
              required: ['unitId', 'quote', 'defect', 'corruption'],
              properties: {
                unitId: { type: 'string' },
                quote: { type: 'string', description: 'VERBATIM citation — cite-or-it-didnt-happen' },
                defect: { type: 'string' },
                corruption: { type: 'boolean', description: 'true only for corruption-tier defects per the rubric' },
              },
            },
          },
        },
      },
    },
    booleans: {
      type: 'object',
      properties: {
        grounded: { type: 'boolean' },
        frameworkComplete: { type: 'boolean' },
        cardsAnswerFronts: { type: 'boolean' },
        distractorsReal: { type: 'boolean' },
      },
    },
    notes: { type: 'string', description: 'one line — goes into the attestation' },
  },
}

const SWEEP_SCHEMA = {
  type: 'object',
  required: ['findings'],
  properties: {
    findings: {
      type: 'array',
      items: {
        type: 'object',
        required: ['chapterNumbers', 'kind', 'quote', 'detail'],
        properties: {
          chapterNumbers: { type: 'array', items: { type: 'number' } },
          kind: { type: 'string', enum: ['scene_skeleton', 'persona_drift', 'repeated_unit', 'location_stamping', 'other'] },
          quote: { type: 'string' },
          detail: { type: 'string' },
        },
      },
    },
  },
}

const ADJUDICATE_SCHEMA = {
  type: 'object',
  required: ['upheld', 'note'],
  properties: {
    upheld: { type: 'boolean' },
    note: { type: 'string' },
  },
}

const ATTEST_SCHEMA = {
  type: 'object',
  required: ['chapterNumber', 'attested', 'cliOutput'],
  properties: {
    chapterNumber: { type: 'number' },
    attested: { type: 'boolean' },
    cliOutput: { type: 'string', description: 'last lines of the qc-attest output (incl. any REFUSED message)' },
  },
}

// ── Phase 1+2: per-chapter pipeline (blind keys → two bar-read lenses) ───────

const LENSES = [
  {
    key: 'editorial',
    axes: ['example_coherence', 'prose_coherence', 'memorable_line_quality', 'plan_actionability'],
    brief: 'You are a skeptical senior EDITOR judging craft: do the scenes, prose tiers, memorable lines, and plan read as finished, human, non-templated writing?',
  },
  {
    key: 'pedagogy',
    axes: ['quiz_distractor_quality', 'card_learning_value', 'factual_accuracy'],
    brief: 'You are a skeptical LEARNING DESIGNER judging whether this chapter actually teaches: distractor quality, card learning value, factual/framework accuracy against the source.',
  },
]

function lensPrompt(lens, c) {
  const rubricBlock = lens.axes.map((a) => `- ${a} [weight ${CONFIG.weights[a]}]: ${CONFIG.rubric[a]}`).join('\n')
  return `${lens.brief}

Chapter: ${c.file}
Source sidecar (ground truth where present): ${c.sidecar ?? 'NONE ON DISK — mark factual checks "could not verify", never silently pass'}
${CONFIG.goldFile ? `CALIBRATION ANCHOR: first skim ${CONFIG.goldFile} — that is what an 85+/PUBLISHABLE chapter reads like. Score relative to it.` : ''}

Score ONLY these axes, each 0..1, per these exact rubric definitions (they include the false-positive guards a clean book relies on — apply them):
${rubricBlock}

Every hit needs a VERBATIM quote and unitId (e.g. "example[2].scenario", "breakdown.deepRead", "card03.back"). Mark corruption=true only for corruption-tier defects as the rubric defines them. Do NOT check quiz answer keys — that is verified mechanically elsewhere.${lens.key === 'pedagogy' ? '\nAlso return booleans: grounded (≥2 real hardSpecifics from the sidecar appear meaningfully), frameworkComplete (named multi-step tools enumerated completely/correctly), cardsAnswerFronts (every back answers its front), distractorsReal (no junk-prefix/format-identifiable distractors).' : ''}
Do not write any files. Be the reviewer the QC-SESSION prompt warns about: scene-skeleton templating and cards-not-answering-fronts are THE historically most-missed defects.`
}

phase('Blind keys')
const perChapter = await pipeline(
  CONFIG.chapters,
  (c) =>
    agent(
      `Verify this chapter's quiz answer key WITHOUT ever seeing it. HARD RULES: do NOT open ${c.file} or any chapter file; do NOT read state/qc. Work only from the commands below${c.sidecar ? ` and the source sidecar (${c.sidecar}), which contains the source facts but no answer key — reading it is allowed and encouraged` : ''}.

1. Run: ${CLI} quiz-blind ${c.file}
   It prints the questions as {questionIndex, prompt, choices[]} with the key stripped.
2. For EACH question, derive the correct choice index yourself from the prompt + choices${c.sidecar ? ' + the sidecar facts' : ''}. FP-guard: a misconception keyed correct IS correct when the stem asks for it ("What is the simplistic reading to avoid?").
3. Run: ${CLI} quiz-verify ${c.file} --answers "<qIndex>:<choiceIndex>,..." with your full derivation (every question — partial coverage fails).
4. Report quiz-verify's result: allMatch, and each MISMATCH with your reasoning. A mismatch is a CLAIM at this stage (it may be your error) — adjudication is separate; do not re-litigate here.`,
      { label: `keys:ch${c.n}`, phase: 'Blind keys', schema: KEYS_SCHEMA },
    ),
  (keys, c) =>
    parallel(
      LENSES.map((lens) => () =>
        agent(lensPrompt(lens, c), { label: `read-${lens.key}:ch${c.n}`, phase: 'Bar read', schema: LENS_SCHEMA }),
      ),
      // Keep nulls IN PLACE: a dead lens agent must be visible as a missing
      // read (DID-NOT-RUN), not silently filtered into "fewer findings".
    ).then((lenses) => ({ c, keys, lenses })),
)

// ── Phase 3: cross-chapter sweep (the defect class per-chapter reads miss) ──

phase('Sweep')
const sweep = await agent(
  `Cross-chapter templating sweep for "${CONFIG.bookId}" — read ALL of these chapter files (they are siblings of one book):
${CONFIG.chapters.map((c) => `- ${c.file}`).join('\n')}

Per-chapter reviewers structurally cannot see what repeats ACROSS chapters. You are the only stage that can. Look for exactly these classes (each finding needs a verbatim quote + every chapter number involved):
1. scene_skeleton — example scenes sharing one frame across chapters ("[Name] does X at [time] in [place]; must decide A or B"), even with different nouns. THE most-missed defect historically.
2. persona_drift — the same protagonist name used as different people across chapters/sections.
3. repeated_unit — card backs, plan shapes, quiz openers, hooks, or breakdown paragraphs that are near-identical across chapters.
4. location_stamping — one location stamped as the setting across many chapters' examples.
FP-guards: clock times INSIDE coherent, different scenes are fine; a consistent pedagogical opener is a convention when content differs; shared CONCEPT terms are the book's vocabulary, not templating. Report only what a paying reader would notice as copy-paste.`,
  { label: 'sweep', phase: 'Sweep', schema: SWEEP_SCHEMA },
)

// ── Merge: provisional verdicts (publishableBar reduction, weights/floors from CONFIG) ──

const sweepByChapter = new Map()
for (const f of (sweep?.findings ?? [])) {
  for (const n of f.chapterNumbers) {
    if (!sweepByChapter.has(n)) sweepByChapter.set(n, [])
    sweepByChapter.get(n).push(f)
  }
}

function provisionalFor(r) {
  const liveLenses = r.lenses.filter(Boolean)
  const axes = []
  for (const lens of liveLenses) for (const a of lens.axes) axes.push(a)
  const keyScore = r.keys
    ? Math.max(0, 1 - (r.keys.mismatches.length / Math.max(1, CONFIG.quizCounts[r.c.n] ?? 9)))
    : 0
  axes.push({ axis: 'quiz_key_correctness', score: keyScore, hits: [] })
  const corruptionClaims = []
  for (const m of r.keys?.mismatches ?? []) {
    corruptionClaims.push({ kind: 'key', chapter: r.c, m })
  }
  for (const lens of liveLenses) {
    for (const a of lens.axes) {
      for (const h of a.hits) {
        if (h.corruption && CONFIG.corruptionAxes.includes(a.axis)) {
          corruptionClaims.push({ kind: 'axis', chapter: r.c, axis: a.axis, hit: h })
        }
      }
    }
  }
  // Read completeness: every lens returned AND each returned every axis it
  // was assigned. A partial-axes response is a partial read — scoring only
  // what a lens chose to return is the same DID-NOT-RUN laundering as a dead
  // agent (weighted overall over 3 axes can hit 100 while 5 went unread).
  const lensesComplete =
    r.lenses.length === LENSES.length &&
    r.lenses.every((lens, i) => {
      if (!lens) return false
      const returned = new Set((lens.axes ?? []).map((a) => a.axis))
      return LENSES[i].axes.every((a) => returned.has(a))
    })
  return { axes, corruptionClaims, readComplete: Boolean(r.keys) && lensesComplete }
}

// ── Phase 4: adjudicate every corruption-class claim before it counts ────────

phase('Adjudicate')
// A dead sweep agent means the cross-chapter read DID NOT RUN — that must
// cap every chapter (the sweep is the only stage that sees cross-chapter
// templating), never read as "no findings".
const sweepRan = sweep !== null && sweep !== undefined
const adjudicated = await parallel(
  perChapter.filter(Boolean).map((r) => () => {
    const prov = provisionalFor(r)
    return parallel(
      prov.corruptionClaims.map((claim) => () =>
        agent(
          claim.kind === 'key'
            ? `Adjudicate a claimed WRONG QUIZ KEY in ${claim.chapter.file}, question index ${claim.m.questionIndex}. A blind reviewer derived choice ${claim.m.derivedIndex}; the key says ${claim.m.keyedIndex}. Their reasoning: ${JSON.stringify(claim.m.reasoning).slice(0, 400)}.
Independently: run \`${CLI} quiz-blind ${claim.chapter.file}\`, derive YOUR answer for question ${claim.m.questionIndex} from prompt+choices${claim.chapter.sidecar ? ` + the sidecar ${claim.chapter.sidecar}` : ''} WITHOUT opening the chapter file, then open the chapter file and read that question's keyed choice AND its explanation. upheld=true ONLY if the key is genuinely wrong (your derivation AND the explanation's own logic disagree with correctIndex). A defensible key with a hard distractor → upheld=false. Default to upheld=false if uncertain.`
            : `Adjudicate a claimed ${claim.axis} CORRUPTION in ${claim.chapter.file}: unit ${claim.hit.unitId}, defect "${claim.hit.defect}", quote: ${JSON.stringify(claim.hit.quote).slice(0, 300)}.
Rubric (with its FP-guards — apply them): ${CONFIG.rubric[claim.axis]}
Read the actual unit in context. upheld=true ONLY if the quote is real (verbatim or near), the defect matches the rubric's corruption tier, and no FP-guard excuses it. Default to upheld=false if uncertain.`,
          { label: `adjudicate:ch${r.c.n}`, phase: 'Adjudicate', schema: ADJUDICATE_SCHEMA },
        ).then((v) => ({
          claim,
          // A dead adjudicator must NOT exonerate the claim it was supposed
          // to verify (the one path that could attest PUBLISHABLE over an
          // unverified wrong-key claim). null verdict = UNRESOLVED.
          resolved: v !== null && v !== undefined,
          upheld: !!v?.upheld,
          note: v?.note ?? 'ADJUDICATOR DID NOT RUN — claim unresolved',
        })),
      ),
    ).then((verdicts) => ({ r, prov, verdicts: verdicts.filter(Boolean) }))
  }),
)

function finalVerdict(entry) {
  const { r, prov, verdicts } = entry
  const upheldKeys = verdicts.filter((v) => v.claim.kind === 'key' && v.resolved && v.upheld)
  const upheldAxis = verdicts.filter((v) => v.claim.kind === 'axis' && v.resolved && v.upheld)
  const unresolved = verdicts.filter((v) => !v.resolved)
  const sweepHits = sweepRan ? (sweepByChapter.get(r.c.n) ?? []) : []
  // publishableBar's hard rule: DID NOT RUN is never a pass. Missing keys
  // stage, a dead/partial lens, a dead sweep, or an unresolved corruption
  // claim all mean the chapter was not fully judged — such chapters are
  // reported NEEDS_RERUN and are NOT attested (attesting a verdict nobody
  // earned would arm the replay guard against the eventual real review).
  const incomplete = !prov.readComplete || !sweepRan || unresolved.length > 0
  // Weighted overall (mirror of publishableBar.weightedOverall, weights from CONFIG)
  let num = 0, den = 0
  for (const a of prov.axes) {
    const w = CONFIG.weights[a.axis] ?? 0
    num += w * Math.max(0, Math.min(1, a.score))
    den += w
  }
  const overall = den === 0 ? 0 : Math.round((num / den) * 100)
  // HARD RULE 1: confirmed corruption is a veto — the average never launders it.
  let verdict
  if (upheldKeys.length > 0 || upheldAxis.length > 0) verdict = 'CORRUPTION'
  // HARD RULE 2: publishable ≠ not-corrupt; sweep templating AND an incomplete
  // read both cap at REVISE (a chapter nobody fully read cannot be attested
  // PUBLISHABLE).
  else if (incomplete || overall < CONFIG.publishableFloor || prov.axes.some((a) => a.score < CONFIG.axisFloor) || sweepHits.length > 0) verdict = 'REVISE'
  else verdict = 'PUBLISHABLE'
  const liveLenses = r.lenses.filter(Boolean)
  const booleans = Object.assign({ grounded: true, frameworkComplete: true, cardsAnswerFronts: true, distractorsReal: true }, ...liveLenses.map((l) => l.booleans ?? {}))
  const dims = {
    keysCorrect: upheldKeys.length === 0,
    grounded: !!booleans.grounded,
    nonTemplated: upheldAxis.length === 0 && sweepHits.length === 0,
    frameworkComplete: !!booleans.frameworkComplete,
    cardsAnswerFronts: !!booleans.cardsAnswerFronts,
    distractorsReal: !!booleans.distractorsReal,
  }
  const noteBits = [
    incomplete
      ? `INCOMPLETE READ (${liveLenses.length}/${LENSES.length} lenses${r.keys ? '' : ', keys missing'}${sweepRan ? '' : ', sweep missing'}${unresolved.length ? `, ${unresolved.length} unresolved claim(s)` : ''}) — NOT attested; re-run qc-run`
      : `overall=${overall}`,
    upheldKeys.length ? `${upheldKeys.length} confirmed wrong key(s)` : null,
    upheldAxis.length ? `${upheldAxis.length} confirmed corruption hit(s)` : null,
    sweepHits.length ? `cross-chapter: ${sweepHits.map((f) => f.kind).join('/')}` : null,
    liveLenses.map((l) => l.notes).filter(Boolean).join(' | ').slice(0, 120) || null,
  ].filter(Boolean)
  // The note is embedded in a double-quoted shell argument by the attest
  // agent — strip the characters bash would expand or execute ($ ` " \).
  // The 4HWW run proved this live: "$500K-$1M" arrived as "00K-M".
  const note = noteBits.join('; ').replace(/[$`"\\]/g, "'").slice(0, 220)
  return { n: r.c.n, file: r.c.file, verdict, incomplete, overall: incomplete ? null : overall, dims, note, upheldKeys: upheldKeys.length }
}

const finals = adjudicated.filter(Boolean).map(finalVerdict)

// ── Phase 5: attest ──────────────────────────────────────────────────────────

phase('Attest')
const toAttest = finals.filter((f) => !f.incomplete)
const needsRerun = finals.filter((f) => f.incomplete)
log(`Verdicts: ${finals.map((f) => `ch${f.n}=${f.incomplete ? 'NEEDS_RERUN' : `${f.verdict}(${f.overall})`}`).join(' ')}`)
if (needsRerun.length > 0) {
  log(`${needsRerun.length} chapter(s) NOT attested (incomplete read) — re-run: qc-run ${CONFIG.bookId} --chapters ${needsRerun.map((f) => f.n).join(',')}`)
}
const attested = await parallel(
  toAttest.map((f) => () =>
    agent(
      `Record a QC verdict. Run EXACTLY this command and report its output verbatim:

${CLI} qc-attest ${f.file} --verdict ${f.verdict} --reviewer "${CONFIG.reviewer}" --dimensions "${Object.entries(f.dims).map(([k, v]) => `${k}=${v}`).join(',')}" --notes ${JSON.stringify(f.note)}

If it prints "QC attestation written" → attested=true. If it prints REFUSED (the self-attest guard: a PUBLISHABLE flip over an unchanged non-PUBLISHABLE verdict) → attested=false and put the COMPLETE refusal message in cliOutput; do NOT retry with --supersede — that override is the OPERATOR's call, not yours.`,
      { label: `attest:ch${f.n}`, phase: 'Attest', schema: ATTEST_SCHEMA },
    ),
  ),
)

const byVerdict = { PUBLISHABLE: [], REVISE: [], CORRUPTION: [] }
for (const f of toAttest) byVerdict[f.verdict].push(f.n)
log(`Attested ${attested.filter(Boolean).filter((a) => a.attested).length}/${toAttest.length} — PUBLISHABLE ${byVerdict.PUBLISHABLE.length}, REVISE ${byVerdict.REVISE.length}, CORRUPTION ${byVerdict.CORRUPTION.length}; NEEDS_RERUN ${needsRerun.length}`)

return {
  bookId: CONFIG.bookId,
  byVerdict,
  needsRerun: needsRerun.map((f) => ({ chapter: f.n, note: f.note })),
  perChapter: finals,
  sweepFindings: sweep?.findings ?? [],
  attested: attested.filter(Boolean).map((a) => ({ chapter: a.chapterNumber, attested: a.attested, output: a.cliOutput.slice(-600) })),
}
