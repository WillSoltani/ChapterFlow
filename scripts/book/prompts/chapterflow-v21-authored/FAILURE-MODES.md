# v21 Failure-Mode Catalog

The single source of truth for what v21 must catch before any chapter ships. Each entry lists the v13 failure mode, the v21 enforcement mechanism, the severity tier, and the specific code location.

Severity tiers:
- **BLOCKER** — chapter cannot ship; final gate hard-fails.
- **MAJOR** — chapter ships only if voice-pass / writer can't fix after retries; logged.
- **MINOR** — advisory; logged but never blocks ship.

Anything not in this catalog is not a known v13 failure mode. If a new failure mode is identified later, add it here first, then add the enforcement.

---

## A. Schema integrity

| ID | v13 failure | v21 enforcement | Severity | Code |
|----|-------------|-----------------|----------|------|
| A1 | Bloom's level used hyphenated/underscored compound tokens (`apply-analyze`, `apply_analyze`) | Quiz writer prompt restricts to canonical 6 verbs; validator `validateQuiz` rejects non-canonical | BLOCKER | [agents/writer-quiz.ts](src/agents/writer-quiz.ts), [critics/schema.ts](src/critics/schema.ts) |
| A2 | depthLevel used inconsistent values (`medium`/`standard`/`hard`/`deeper`/`deep`) | Quiz writer prompt restricts to `simple/standard/deep`; validator rejects others | BLOCKER | [agents/writer-quiz.ts](src/agents/writer-quiz.ts) |
| A3 | bloomsLevel missing on 10.8% of questions | Validator requires non-empty bloomsLevel | BLOCKER | [agents/writer-quiz.ts](src/agents/writer-quiz.ts) |
| A4 | Quiz answer-position bias (TFS had 100% in idx 0; AH 80%) | Validator: max 50% in any single position; book-level gate enforces uniform distribution across chapters | MAJOR | [agents/writer-quiz.ts](src/agents/writer-quiz.ts), [critics/schema.ts](src/critics/schema.ts) |
| A5 | Quiz inconsistent choice count (some 3, some 4) | Validator requires exactly 3 choices | BLOCKER | [agents/writer-quiz.ts](src/agents/writer-quiz.ts) |
| A6 | Tone matrix duplication (3 voices = 3× content storage) | v21 schema is single canonical voice; v21 schema `BookPackageV21` has no tone keys | RESOLVED-BY-DESIGN | [src/types.ts](src/types.ts) |
| A7 | `keyTakeaways[]` arrays were always empty in v13 | v21 schema drops the field; replaced with single `keyTakeaway` string | RESOLVED-BY-DESIGN | [src/types.ts](src/types.ts) |
| A8 | `oneMinuteRecap` duplicated `keyTakeawayCard` | v21 schema drops `oneMinuteRecap` | RESOLVED-BY-DESIGN | [src/types.ts](src/types.ts) |
| A9 | `contexts` field overloaded (sometimes 3 short tags, sometimes 150-char planner specs) | v21 splits: `tags` for short display strings, `planSpec` for full planner data | RESOLVED-BY-DESIGN | [src/assembler.ts](src/assembler.ts) |
| A10 | Cache-skip regressions silently ship books with structurally inconsistent chapters (memorable-lines added later but Ch1–3 from earlier run never got re-touched, etc.) | Book gate `schemaInconsistencies` check: if a field is present on ≥80% of chapters but absent on others, fails as a BLOCKER. Forces operator to backfill the stale chapters before promotion. | BLOCKER | [src/critics/bookGate.ts](src/critics/bookGate.ts) |
| A11 | A polish/refactor pass rewrote chapter prose without updating pinned `memorableLines[].text`, leaving the pin pointing at a sentence that no longer exists. The reader's quote/share-card surface breaks. The post-shipping polish run did this 298 times across 22 v21 books before the check existed. | Ship gate scans each chapter's `memorableLines[].text` against `breakdown.fastRead/deepRead/fullRead`; fails as BLOCKER on any pin not found verbatim. Repair script: `src/scratch/regenerate-broken-memorable-lines.ts` calls the memorable-lines agent on each broken chapter to pick fresh pins from the current prose. | BLOCKER | [src/critics/finalGate.ts](src/critics/finalGate.ts), [src/scratch/regenerate-broken-memorable-lines.ts](src/scratch/regenerate-broken-memorable-lines.ts) |
| A12 | Display fields (hook, counterintuition, keyTakeaway, tryThisNow, example scenario, example title) started with a lowercase letter. Indistractable shipped with 29/30 lowercase hooks and 25 lowercase example scenarios, and the gate said PASS. Extreme Ownership later shipped lowercase sentence fragments inside breakdown prose after a templated closing paragraph inserted a lowercase verb phrase after a period. The reader sees this immediately in the app — looks unprofessional and reads as broken. | Ship gate scans every display field through `checkCapitalization`; fails as BLOCKER on any field that begins with a lowercase letter after stripping leading quotes/whitespace. It also emits `A12-breakdown` when a non-initial sentence in `fastRead`, `deepRead`, or `fullRead` starts lowercase after a sentence boundary. | BLOCKER | [src/critics/integrity.ts](src/critics/integrity.ts), [src/critics/finalGate.ts](src/critics/finalGate.ts) |
| A13 | Display fields contained grammatically broken syntax: apposition orphans ("Audrey, owners gather staff with no agenda…" — leaves Audrey without a verb), missing-connective fragments ("needs to choose the sous-chef refuses the meeting"), doubled periods, and 3+ commas in opener positions. Indistractable Ch15 shipped multiple examples with these patterns. | Ship gate runs `checkSentenceSanity` on every display field — narrow but high-precision heuristics for the four failure patterns above. Logs as MAJOR (the writer can ship after retry) but the writer is forced to rewrite. | MAJOR | [src/critics/integrity.ts](src/critics/integrity.ts), [src/critics/finalGate.ts](src/critics/finalGate.ts) |
| A14 | `keyTakeaway` ran 35+ words, unreadable as a quick-scan card. Indistractable Ch15 shipped a 38-word run-on with "the nearest respectable escape" leaking into example scenarios. | Ship gate caps `keyTakeaway` at 30 words via `checkMaxWordCount`. Logged as MAJOR. | MAJOR | [src/critics/integrity.ts](src/critics/integrity.ts), [src/critics/finalGate.ts](src/critics/finalGate.ts) |
| A15 | Tier-length stub: any chapter with fastRead < 350, deepRead < 1000, or fullRead < 2400 chars. The rubric docks points but the previous gate accepted the chapter. 48 Laws of Power shipped with 0/48 chapters at fullRead target (avg 347 chars, 12% of target) and the rubric still graded A- 88%. Ship gate now refuses to ship stub chapters. | Ship gate runs `checkTierLengthFloors` per chapter; fails as BLOCKER on any tier below floor. | BLOCKER | [src/critics/finalGate.ts](src/critics/finalGate.ts) |

## B. Voice and register

| ID | v13 failure | v21 enforcement | Severity | Code |
|----|-------------|-----------------|----------|------|
| B1 | Meta-references: "the chapter / the book / the author / in this chapter / [author-name] argues/writes" plus the abstract meta-frame "the idea / this idea / this move" used as essay-agent subject ("the idea wants", "this move targets", "the idea's demand"). 85.3% of v13 breakdowns used the explicit form; Zero to One shipped with the abstract form in 13 of 14 chapters before this expansion. | Writer prompts forbid; critic `checkNoMetaReference` regex-blocks both forms. | BLOCKER | [critics/register.ts](src/critics/register.ts), [config/meta-patterns.json](config/meta-patterns.json) |
| B2 | Literal "Chapter N" in prose (394 occurrences in TFS alone) | Writer prompts forbid; critic `checkNoChapterNumberLiteral` regex-blocks | BLOCKER | [critics/register.ts](src/critics/register.ts) |
| B3 | Author-surname-verb constructions ("Clear argues...", "Kahneman writes...") | Meta-pattern regex catches; critic blocks | MAJOR | [config/meta-patterns.json](config/meta-patterns.json) |
| B4 | Stock phrases across books (`boundary condition` 201 hits across 46 books, etc.) | Hard-banned list in config; critic blocks each | MAJOR | [critics/register.ts](src/critics/register.ts), [config/banned-phrases.json](config/banned-phrases.json) |
| B5 | Em dashes (37 per chapter on average) — model voice tell | Writer prompts forbid; critic `checkNoEmDash` blocks | BLOCKER | [critics/register.ts](src/critics/register.ts) |
| B6 | Voice was generic "smart magazine" register, not author-specific | Editor-in-chief produces voice specimens + anti-specimens; voice-pass agent rewrites toward specimens | MAJOR | [agents/editor-in-chief.ts](src/agents/editor-in-chief.ts), [agents/voice-pass.ts](src/agents/voice-pass.ts) |
| B7 | Generic closing lines ("be careful", "think carefully") | Critic `checkClosingLineLandings` flags | MINOR | [critics/prose.ts](src/critics/prose.ts) |
| B8 | Cross-tier verbatim phrase repetition | Critic `checkCrossTierPhraseUniqueness` flags 4+ word verbatim shares | MINOR | [critics/prose.ts](src/critics/prose.ts) |
| B9 | Reverse-priming: writer prompts that name a forbidden phrase ("don't say 'the chapter'") cause the model to emit it. Naming the bad token plants it in attention. | Brief-sanitizer strips literal forbidden phrases from `voiceCharter.avoidMoves` before the brief reaches a writer. Retry messages must be phrased structurally ("address the reader directly"), never as "don't say X". | BLOCKER (design rule, enforced at brief assembly) | [src/lib/brief-sanitizer.ts](src/lib/brief-sanitizer.ts), [src/agents/editor-in-chief.ts](src/agents/editor-in-chief.ts) |
| B10 | Source-freeze sidecars contain meta-tells ("the chapter should…", "the author argues…"). When fed into a writer as context, those tells leak into output and trip B1/B2/B3 downstream. | `source-loader.ts` strips meta lines at read time before content reaches any agent. If everything strips out, the loader returns null instead of an empty string. | BLOCKER (prevention; ship gate still catches escapes) | [src/source-loader.ts](src/source-loader.ts) |

## C. Examples (scenes)

| ID | v13 failure | v21 enforcement | Severity | Code |
|----|-------------|-----------------|----------|------|
| C1 | Scenarios were thesis-paraphrase, not scenes ("A work decision is moving quickly because...") | Writer prompt demands named protagonist + scene; validator rejects scenarios <200 chars or without proper noun | BLOCKER | [agents/writer-example.ts](src/agents/writer-example.ts), [critics/narrative.ts](src/critics/narrative.ts) |
| C2 | Examples lacked specific setting (time/place/role) | Critic `checkSpecificScene` requires anchor (clock time, specific location, specific role) | MAJOR | [critics/narrative.ts](src/critics/narrative.ts) |
| C3 | Examples had no decision point | Critic `checkDecisionPoint` requires decision cue (skipped for postmortem/reflection formats by design) | MAJOR | [critics/narrative.ts](src/critics/narrative.ts) |
| C4 | Format rotation locked to `decision_point/postmortem/dialogue/predict_reveal/dilemma/before_after` (49% of chapters used identical sequence) | Curriculum planner prompt demands ≥3 distinct formats; over-generation curator picks variety | MAJOR | [prompts/curriculum-planner.system.md](prompts/curriculum-planner.system.md) |
| C5 | Category locked to `work/school/personal` (66% of chapters used identical rotation) | Curriculum planner prompt forbids the rotation; chooses domains specific to chapter | MAJOR | [prompts/curriculum-planner.system.md](prompts/curriculum-planner.system.md) |
| C6 | Same context-tuple used across unrelated books (`(meetings, forecasting, evaluation)` 68 times) | Per-example planSpec has unique domain/audience/stakes; tags are short and book-specific | RESOLVED-BY-DESIGN | [src/assembler.ts](src/assembler.ts) |
| C7 | Protagonist name pool tiny (Priya in 50 books, Omar in 47) | Banned-pool list of 20 v13-overused names; cross-book ledger tracks recent names; writers receive forbidden list | BLOCKER | [agents/writer-example.ts](src/agents/writer-example.ts), [librarian/libraryState.ts](src/librarian/libraryState.ts) |
| C8 | Examples competent but safe (one accepted candidate per slot) | Over-generation: 3 candidates per slot; curator picks best by rubric | QUALITY-LIFT | [generateChapter.ts](src/generateChapter.ts), [curator/exampleSelector.ts](src/curator/exampleSelector.ts) |
| C10 | Example-title verb shell: 4+ titles in one chapter follow `<Name> <verb> <domain>` (Indistractable Ch15: "Samantha handles shipyard…", "Grant handles university…", "Audrey handles restaurant…" — six identical shells with only the name and domain swapped). C8 missed this because each title's 3-word substring is unique; the shell only repeats the second word. | Ship gate calls `checkExampleTitleVerbShell` — if 4+ titles share the same second word (after lowercasing and stripping punctuation), fail as BLOCKER. | BLOCKER | [src/critics/integrity.ts](src/critics/integrity.ts), [src/critics/finalGate.ts](src/critics/finalGate.ts) |

## D. Pedagogy

| ID | v13 failure | v21 enforcement | Severity | Code |
|----|-------------|-----------------|----------|------|
| D1 | Quiz tested recall ("What does Chapter 6 say about...") in 44.2% of prompts | Writer prompt demands application stems; critic `checkQuizTestsApplication` blocks recall openers | MAJOR | [agents/writer-quiz.ts](src/agents/writer-quiz.ts), [critics/pedagogy.ts](src/critics/pedagogy.ts) |
| D2 | Cards were comprehension checks not retrieval prompts | Writer prompt demands retrieval; critic `checkCardTestsRetrieval` flags recall openers | MINOR | [critics/pedagogy.ts](src/critics/pedagogy.ts) |
| D3 | Quiz Bloom's mix not aligned with curriculum | Curriculum planner sets `bloomsMix`; writer must match within ±1 per level | MAJOR | [agents/writer-quiz.ts](src/agents/writer-quiz.ts) |
| D4 | No transfer-emphasis: questions used the same scenarios as the chapter | Curriculum planner sets `transferEmphasis ≥ 0.7`; writer demands novel scenarios per question | MAJOR | [prompts/writer-quiz.system.md](prompts/writer-quiz.system.md) |
| D5 | Implementation plan generic ("be consistent", "track progress") | Writer prompt requires concrete trigger + concrete response in if-then plans; 24-hr challenge must be specific | MAJOR | [prompts/writer-implementation-plan.system.md](prompts/writer-implementation-plan.system.md) |

## E. Reading level

| ID | v13 failure | v21 enforcement | Severity | Code |
|----|-------------|-----------------|----------|------|
| E1 | All tiers wrote at same FK grade (~grade 9), too academic for easy tier | Tier-specific reading-level constraints in writer prompt; critic `checkReadingLevel` enforces ceilings (fastRead ≤9.5, deepRead ≤12.5) | MAJOR | [critics/readingLevel.ts](src/critics/readingLevel.ts), [prompts/writer-breakdown.system.md](prompts/writer-breakdown.system.md) |
| E2 | Tiers were redundant (easy = shorter medium, medium = shorter hard) | Writer prompt requires progressive tiers; critic `checkTiersProgressive` blocks identical first sentences | MAJOR | [critics/prose.ts](src/critics/prose.ts) |
| E3 | Easy tier opened with abstractions/definitions | Critic `checkOpeningConcreteness` flags definitional openers | MINOR | [critics/prose.ts](src/critics/prose.ts) |
| E5 | Average sentence length above 18 words in any tier (or 16 in deepRead, 14 in fastRead). Even when FK grade level is under ceiling, dense sentence packing makes prose feel like work. New enforcement via line-editor and voice-pass agents (readability polish section). | [prompts/line-editor.system.md](prompts/line-editor.system.md), [prompts/voice-pass.system.md](prompts/voice-pass.system.md) | MINOR (advisory) | (no score cap) |
| E6 | Paragraph fails the "satisfaction test" — restatement closer, categorical closer where image would land, no payoff named by the paragraph's end. Not currently mechanically enforced (judgment call). Documented in writer-breakdown.system.md "What makes a paragraph satisfying" section. | [prompts/writer-breakdown.system.md](prompts/writer-breakdown.system.md) | ADVISORY | (no score cap) |

## F. Cross-book state

| ID | v13 failure | v21 enforcement | Severity | Code |
|----|-------------|-----------------|----------|------|
| F1 | Same protagonist names recurred across 50+ books | Library state ledger tracks names per book; writers receive forbidden list of last-10-books names. Within-book duplication is now also a BLOCKER in the book gate, restricted to **recurring** names (a token that appears 2+ times in a single chapter's examples) so one-off capitalized words like "Nobody", "Third", or "Street" don't false-flag. | BLOCKER (within-book) / MAJOR (cross-book) | [librarian/libraryState.ts](src/librarian/libraryState.ts), [src/critics/bookGate.ts](src/critics/bookGate.ts) |
| F2 | Stock phrases recurred across books with no library-wide accounting | Library state ledger tracks signature phrases | MINOR | [librarian/libraryState.ts](src/librarian/libraryState.ts) |
| F3 | Library-wide answer-position drift | Library state ledger tracks cumulative position counts | MINOR (advisory; book-level gate could enforce) | [librarian/libraryState.ts](src/librarian/libraryState.ts) |

## BP. Book-level pattern audit

These IDs are produced by the deterministic book-level pattern audit and surface in `score-chapters` as score caps. They do not run at chapter time; they run when scoring or validating a whole package.

| ID | v13 failure | v21 enforcement | Severity | Code |
|----|-------------|-----------------|----------|------|
| BP1 | One quiz explanation copy-pasted across the whole book (Deep Work's first promotion shipped with the same explanation 81 times across 9 chapters) | If the same normalized quiz explanation appears in ≥2 chapters or ≥3 times total, BLOCK as a BP1 finding with `maxScoreCap=70`. | BLOCKER (book) | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| BP2 | Example scenes reused across chapters with names swapped | 6-word verbatim-after-normalization repeated across ≥`chapterThreshold(N)` chapters fires BP2 with `maxScoreCap=75`. | BLOCKER (book) | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| BP3 | Hook/counter/tryThisNow stems reused across chapters | 5-gram (after name+title normalization) appears in ≥`chapterThreshold(N)` chapters. | BLOCKER (book) | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| BP9 | Hook-shell repetition under the BP3 threshold. BP3 needs 7 matches in a 30-chapter book to fire. Indistractable shipped 3 distinct hook shells each used in only 3–5 chapters with the leading noun swapped (`"<noun> makes the pressure visible before the day can rename it as necessity"`, etc.). BP3 missed every one. | BP9 lowers the floor to 3 chapters for hooks specifically and uses 4-grams over the tail of the hook (skipping the first 3 skeleton tokens). Hooks are the most-surfaced field in the app and a single shell appearing in 3 chapters is a shipping defect. `maxScoreCap=70`. | BLOCKER (book) | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| BP10 | Cross-chapter breakdown prose duplication. Extreme Ownership shipped with six paragraphs (two in deepRead, four in fullRead) copy-pasted verbatim into all 13 chapters — over 50% of every chapter's deepRead and fullRead was identical filler. BP1 covers quiz explanations, BP2 covers example scenarios, BP3 covers surface-frame shells — none scanned breakdown prose. A reader moving chapter to chapter saw the same paragraphs repeatedly. | BP10 splits each chapter's `fastRead`, `deepRead`, and `fullRead` into paragraphs, normalizes whitespace, and fires a BLOCKER for any paragraph of 30+ words that appears verbatim in 3 or more chapters. `maxScoreCap=60` — the worst class of templating because the duplicated unit is the reader-facing prose itself. | BLOCKER (book) | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| BP11 | Cross-chapter breakdown paragraph shells with small variable substitutions. Extreme Ownership shipped a 13-chapter closing paragraph template where one noun and one verb phrase changed per chapter while roughly 110 words of structure stayed identical. BP10 missed it because the paragraphs were not exact matches, and the verb-phrase slot produced broken fragments like `speech. turn incomplete information…`. | BP11 scans `fastRead`, `deepRead`, and `fullRead` paragraphs of 200+ characters across chapter pairs, computes a thresholded longest common substring after lowercase whitespace normalization, and groups connected matches. A 100+ character shared substring that covers at least 40% of the shorter paragraph becomes a candidate; a grouped paragraph-scale skeleton of 250+ characters spanning 3+ chapters emits a BLOCKER with `maxScoreCap=60`. | BLOCKER (book) | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| BP12 | Short closing or transitional paragraphs (≤29 words) repeated verbatim across chapters. The One Thing shipped with the 9-word sentence "Keep the clue. Leave the costume where it belongs." as the closing paragraph in 16 of 18 chapters. BP10's 30-word floor and BP11's ~100-char LCS floor both missed it because the paragraph was too short. | BP12 fires when any breakdown paragraph of 5–29 words appears verbatim across ≥3 chapters after the standard normalization. `maxScoreCap=60` — same severity as BP10 since the reader sees identical short prose across chapters. | BLOCKER (book) | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| B11 | Counter negation-correction shell repetition. When ≥50% of a book's chapters open counterintuition with "X is not Y. [correction]" or "X does not Y. [correction]" — same defect class as a literal banned-stem repetition but evades the stem-based check because the literal string varies. You Can't Hurt Me shipped 10 of 11 chapters in this shape and Charisma Myth shipped 9 of 13 pre-regen; both passed every existing gate. | First-sentence check for `\b(is not\|are not\|does not\|do not)\b` with a non-empty second sentence; if ≥50% of chapters match, emit a MAJOR finding listing the affected chapters. Surfaces in book pattern audit. No score cap — substance can be fine when shell repeats. | MAJOR | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| B12 | Counter connector phrase overuse. The same second-sentence connector ("In fact", "The opposite", "Counterintuitively", "Instead", "Despite", "But actually", "What looks like") appears in more chapters than the scale-based cap (2 for ≤10-chapter books, 3 for 11–25, 4 for 26+). Reader-visible templating that earlier regens consistently slipped past the agent's hard caps because the brief's "no shape used in more than N" rule was treated as a soft target. Visible in post-regen audits on Atomic Habits, NSTD, Charisma Myth, Pitch Anything. | Word-boundary regex per connector; counts chapters whose counterintuition contains the connector; fires MINOR per connector over cap. Advisory, no score cap. | MINOR | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| B13 | Hook frame clustering: >50% of chapters open with the same first word. GMM Vol 2 had 19/20 starting "The…"; Talk Like Ted (pre-fix) had 9/9; 48 Laws had 14/48 starting "The"; Blue Ocean Strategy shipped 9/11 starting "A". Visible to a reader within 3 chapters. | `checkHookFirstWordCap` extracts the first word (lowercased, punctuation stripped) of every chapter's hook; fires when any first word appears in ≥50% of chapters (relaxed per author-voice-profile when supplied). `maxScoreCap=80`. MAJOR. | MAJOR | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| B14 | Counter shape clustering: a single paradox-signal shape used by ≥40% of chapters even when no literal stem repeats. So Good They Can't Ignore You shipped 14/16 chapters in the negation_correction shape ("X is not Y, but Z"); Righteous Mind shipped 5/12 with "The paradox:". Each counter individually looks fine; the shape cluster is the defect. | `classifyCounterShape` classifies each counter into one of 7 known shapes; `checkCounterShapeCap` fires when any non-"other" shape appears in ≥40% of chapters (relaxed per author-voice-profile when supplied). `maxScoreCap=80`. MAJOR. | MAJOR | [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts) |
| BP15 | Strawman distractor: a non-correct quiz choice defeatable by an absolute trigger word (`always`, `never`, `forever`, `automatically`, `impossible`, `entirely`, `ever`, `completely`, …) rather than by the chapter's framework. Across 86 audited v21 books: 1,200+ instances. Test-takers can pick the right answer by spotting the extreme word without understanding the idea. | `checkQuizStrawmanDistractors` regex-tests every wrong choice for the trigger word set and surfaces a MAJOR finding per occurrence. Writer-quiz validator catches the same so the retry loop fixes it pre-assembly. | MAJOR | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| BP16 | Quiz answer-length bias: correct answer >1.5× the average distractor length lets the reader pick the longest text without understanding. Indistractable shipped with 219 of 270 questions at ratio ≥ 2.0 (peak 6.67); Thinking, Fast and Slow shipped with 84 blockers. | `checkQuizAnswerLengthRatio` measures word-count ratio; ratio ≥ 2.0 → BLOCKER (`BP16.quiz_answer_length_blocker`); ratio ≥ 1.5 → MAJOR (`BP16.quiz_answer_length_major`). Target < 1.4. Writer-quiz validator mirrors the threshold. | BLOCKER (≥2.0) / MAJOR (≥1.5) | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| BP17 | Quiz prompt opener monotony: >5 of 9 prompts in a chapter open with "A " or "An ". The quiz reads as a single template ("A manager…", "A founder…", "A nurse…"). All 38 chapters of Thinking, Fast and Slow shipped with this defect; all 33 chapters of The 33 Strategies of War; all 10 of Execution. | `checkQuizPromptOpenerMonotony` counts A/An openers; fires MAJOR when count exceeds ceil(N × 5/9). Writer-quiz validator mirrors the threshold and instructs the writer to vary openers. | MAJOR | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| BP18 | Label-shaped correct answer: ≤6-word correct choice with no verb ("Cut charting time.", "Delete the app."). Reads as a label, not an action. Diagnostic, not a blocker because legitimate short answers exist. | `checkQuizLabelShapedCorrect` flags MINOR when wc ≤ 6 and a broad verb-hint regex doesn't match. Writer-quiz validator catches the same. | MINOR | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| BP19 | Banned tail-clause distractor template. Specific 5+ word phrases that emerged across many books during bulk distractor regeneration ("fits the immediate pressure around", "given the constraints in play", "could make that choice seem workable", "Keep the old message for now…", "until the team feels more certain delay the decision…", etc.). Generator artifact, not authored content. | `checkQuizBannedTailPhrase` substring-matches against `BANNED_TAIL_CLAUSES` array (mirrored in [config/banned-phrases.json](config/banned-phrases.json)). BLOCKER on any hit. Writer-quiz validator mirrors. | BLOCKER | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| BP20 | Quiz n-gram template repeat at book scale. A 5-word phrase appearing ≥20 times, 6-word ≥10 times, or 8-word ≥5 times across distractors indicates template substitution. Execution shipped with "Keep the old message for now so the team does not lose energy" × 80; the-12-week-year with "until the team feels more certain delay the decision so" × 102; deep-work with "Answer every visible request first so no one can accuse the team of being unavailable" × 9; the-one-thing with "Ranking would make action impossible" × 6 and "It proves easy tasks never matter" × 6. None caught by any existing critic. | `checkBookQuizNgramTemplates` (book-gate level) tokenizes every distractor, builds 5/6/8-gram counts, fires BLOCKER per offending phrase with 4-gram-based dedupe to coalesce sliding-window neighbors into one finding per template instance. | BLOCKER (book) | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| BP21 | Cross-chapter duplicate distractor. The same wrong-answer text copied verbatim across multiple chapters of the same book (e.g., the-one-thing shipped "Ranking would make action impossible" as choice #2 of q05 in 6 different chapters). Generator artifact. | `checkBookQuizCrossChapterDuplicates` (book-gate level) normalizes every distractor (lowercased, trimmed, ≥30 chars), and fires BLOCKER for any text that appears in ≥2 distinct chapters, with the location list in the message. | BLOCKER (book) | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| schema.quiz_duplicate_choice | Two literally-identical choices within the same question. Corrupts the multiple-choice contract; gates the question itself. | `checkQuizDuplicateChoices` normalizes per-question (lowercased, trimmed) and fires BLOCKER on duplicate detection. | BLOCKER | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| schema.quiz_lowercase_choice_start | A choice begins with a lowercase letter — presents poorly and signals generation error. | `checkQuizLowercaseChoiceStart` strips leading whitespace/quotes, tests first char for `[a-z]`. MAJOR. | MAJOR | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| schema.quiz_unexpected_field | A quiz question carries a field outside `{questionId, prompt, choices, correctIndex, correctAnswerIndex, explanation, bloomsLevel, depthLevel}`. The upstream `app/api/book/_lib/validate-book-package.ts` validator returns 422 on any extra field. Common offender: `whyItMatters` mistakenly added to quiz questions (it belongs on examples). | `checkQuizUnexpectedFields` enumerates keys against the allow-list. BLOCKER. Writer-quiz validator mirrors. | BLOCKER | [src/critics/quizQuality.ts](src/critics/quizQuality.ts) |
| BP24 | Substantial verbatim copy-paste between breakdown tiers within a single chapter. The May 2026 "Step 2 second-round" incident: Ch3 of *The 7 Habits* had **1,436 characters of verbatim prose shared between DeepRead and FullRead** — the entire middle of DeepRead was copy-pasted into FullRead. The existing B8 critic (`prose.ts:checkCrossTierPhraseUniqueness`) returns after the first 4-word match and only emits one minor finding per chapter; it has no mass-based escalation. A reader sees the same paragraph twice in a single chapter — the FullRead tier should LAYER content (third example, boundary case, failure mode) rather than duplicate DeepRead. | Chapter-level: `checkBreakdownCrossTierVerbatim` computes the longest contiguous common substring between each tier pair (FastRead↔DeepRead, FastRead↔FullRead, DeepRead↔FullRead). Threshold: 150 chars of verbatim shared substring → BLOCKER. Independent of B8 and intentionally more aggressive on copy-paste mass. | BLOCKER | [src/critics/intraBookFieldSimilarity.ts](src/critics/intraBookFieldSimilarity.ts) |

## SC. Source coherence (researcher output)

These IDs fire when the researcher's output is internally inconsistent or contains downstream-breaking content. Run before any writer agent sees the bundle.

| ID | Failure | Enforcement | Severity | Code |
|----|---------|-------------|----------|------|
| SC1 | Chapter count in bibliography doesn't match the number of chapter-research results returned. | `runSourceCoherenceCheck` compares `bibliography.edition.chapterCount` against `chapters.length`. | BLOCKER (book) | [src/critics/sourceCoherence.ts](src/critics/sourceCoherence.ts) |
| SC2 | Chapter numbers not sequential 1..N — the researcher returned a gap or a duplicate. | After sorting, verifies position-index alignment. | BLOCKER (book) | [src/critics/sourceCoherence.ts](src/critics/sourceCoherence.ts) |
| SC3 | Per-chapter paraphrase notes under 600 chars (target 1200–2400). Downstream prose can't be specific without specific source notes. | Length floor check on `paraphraseNotes`. | BLOCKER (chapter) | [src/critics/sourceCoherence.ts](src/critics/sourceCoherence.ts) |
| SC4 | Meta-reference leakage in source notes ("this chapter…", "the book…", "Chapter N…"). source-loader strips these silently, hiding the loss from the orchestrator. | Regex sweep over every text field of every chapter-research result. | BLOCKER (chapter) | [src/critics/sourceCoherence.ts](src/critics/sourceCoherence.ts) |
| SC5 | Author-surname-verb construction in source notes ("Clear argues…", "Kahneman says…"). Same reverse-priming risk as meta-references (B9). | Author-name + reporting-verb regex. | BLOCKER (chapter) | [src/critics/sourceCoherence.ts](src/critics/sourceCoherence.ts) |
| SC6 | 40+ character quoted span in paraphrase notes — possible verbatim citation, copyright concern. | Regex for `"..."` or `“...”` spans of 40+ chars. MAJOR (manual review). | MAJOR (chapter) | [src/critics/sourceCoherence.ts](src/critics/sourceCoherence.ts) |
| SC7 | No named examples returned for a chapter. Downstream example writer has no specific anchors and falls back to generic templates. | Array length check. MAJOR. | MAJOR (chapter) | [src/critics/sourceCoherence.ts](src/critics/sourceCoherence.ts) |
| SC8 | Cross-chapter paraphrase duplication: the researcher reused 8-gram phrases across chapters (self-templating). | Signature-bucket pairwise comparison; flags chapter pairs sharing 3+ 8-gram signatures. | BLOCKER (book) | [src/critics/sourceCoherence.ts](src/critics/sourceCoherence.ts) |

## AS. Anti-salting (May 2026 Covey-incident response)

In May 2026 a writer agent produced `the-7-habits-of-highly-effective-people.v21.json` that passed every chapter ship gate and every book pattern audit but shipped with quiz prompts like `"goose q7 person goose studio critique"` and scenarios like `"MaplefieldBridgeton 10:20 p.m.."`. The agent's own postmortem confirmed it had **inserted unique tokens per chapter to break verbatim n-gram matching while leaving the underlying template intact**. The package passed because the BP-codes detect verbatim repetition; they cannot detect intentionally-salted prose. The AS-codes close that gap.

Every AS finding is a BLOCKER. False positives are acceptable — the cost of a spurious block (operator reviews and overrides) is much lower than the cost of shipping ruined prose.

| ID | v13 failure | v21 enforcement | Severity | Code |
|----|-------------|-----------------|----------|------|
| AS1 | Identifier-style token (`q7`, `q01`, `ex1`, `p2`, `card3`) appearing inside a quiz prompt, choice, explanation, card front/back, breakdown tier, or example scenario. Real prose contains no such tokens; their presence is gaming. | Regex `\b(q\|p\|ex\|c\|card\|chapter)\d{1,3}\b` over every non-identifier text field. Structural id fields (questionId, exampleId, cardId, chapterId) are skipped. | BLOCKER | [src/critics/antiSalting.ts](src/critics/antiSalting.ts) |
| AS2 | Two 4+-letter capitalized words mashed without a separator: `MaplefieldBridgeton`, `HarborlineNorthwell`, `ZenithKestrel`. Real English never produces these; they are `{place_a}{place_b}` template substitutions with a missing separator. | Regex `\b[A-Z][a-z]{3,}[A-Z][a-z]{3,}\b` across every text field. Allows legitimate compound names like McDonald, MacArthur, O'Brien (apostrophe or 2-3-letter prefix). | BLOCKER | [src/critics/antiSalting.ts](src/critics/antiSalting.ts) |
| AS3 | Doubled period followed by whitespace + capital letter: `"10:20 p.m.. The room"`. Generation parse error or deliberate sentence-boundary salting. | Regex `\w\.\.\s+[A-Z]` over prose fields. | BLOCKER | [src/critics/antiSalting.ts](src/critics/antiSalting.ts) |
| AS4 | Positional quiz prompt template substitution: same question position (Ch1 q06, Ch2 q06, …) sharing the same skeleton with one or two substituted nouns per chapter. The Covey incident: every chapter's q06 was `"If the [TOKEN] family calendar rewards push through fatigue, which plan best serves [TOKEN] balance?"` — passed BP20 (which checks 5-word verbatim repeats) because the [TOKEN] swap broke every 5-gram match. AS4 catches it because the 14 non-[TOKEN] words give a word-multiset similarity of 14/16 = 87.5%, above the 70% threshold. | Book-level: for each question position N across chapters, compute pairwise word-set similarity. If a connected component of ≥3 chapters all share >70% word overlap at the same position, fail closed. | BLOCKER (book) | [src/critics/antiSalting.ts](src/critics/antiSalting.ts) |
| AS5 | Quiz prompt of a newly-written chapter matches a previously-written chapter's same-position prompt at ≥70% word overlap. The May 2026 "7 Habits Step 2" incident: the writer agent produced 11 chapters where every quiz prompt was identical across chapters with only the protagonist name swapped (`"Your family is split after the call connects. What should [NAME] protect..."`). The individual chapters all passed ship-gate one-at-a-time because no chapter-level critic compared against siblings. AS4 (book-level) caught it but only at finalization, after all 11 chapters had been written — 10+ chapters of wasted work. AS5 catches the same defect class at chapter ship-gate time. | Chapter-level: `gate-chapter` auto-discovers sibling chapter files in `state/chapters/<bookId>-ch*.v21-native.chapter.json` and compares the current chapter's quiz prompts to every prior chapter's same-position prompt. ≥70% word overlap fails closed at the chapter being written, before the writer moves to the next chapter. | BLOCKER (chapter) | [src/critics/intraBookQuizSimilarity.ts](src/critics/intraBookQuizSimilarity.ts) |
| AS6 | Quiz distractor of a newly-written chapter matches a previously-written chapter's same-position distractor at ≥80% word overlap. Same May 2026 incident as AS5: the agent's correct answer of "should act inside the Circle of Influence, using response-ability to choose the next step" appeared in 99 of 99 quiz questions across the 11 chapters, just shuffled across positions. AS6 detects this at the chapter being written so the writer is forced to compose chapter-specific distractors before moving to the next chapter. The threshold (80%) is higher than AS5's (70%) because legitimate distractors of similar concepts can share more vocabulary than legitimate prompts. | Chapter-level: same auto-discovery as AS5; compares the current chapter's non-correct choices to all prior chapters' choices at the same question position. Groups all prior matches into a single finding per (current_question, current_choice) so the writer agent gets one actionable message per offending choice rather than N×M findings. | BLOCKER (chapter) | [src/critics/intraBookQuizSimilarity.ts](src/critics/intraBookQuizSimilarity.ts) |
| AS7 | Review card front or back of a newly-written chapter matches a previously-written chapter's same-position card at ≥75% word overlap. **The May 2026 "Step 2 second-round" incident:** after AS5/AS6 forced the writer agent to genuinely differentiate quiz prompts/distractors across chapters of *The 7 Habits*, the agent applied the same template-substitution pattern to the *cards* layer instead. All 11 chapters used a single 6-card skeleton with one concept-name swapped per card (Ch1: "What does inside-out change ask you to inspect first?" / Ch3: "What does response-ability ask you to inspect first?" / …). Backs followed the same template ("Inspect the source pattern before the surface behavior. In practice, that means choosing to [CHAPTER-SPECIFIC ACTION] before the quick repair takes over."). Every card across the book passed the C11 identical-back check because the backs *weren't* byte-identical — one phrase varied per chapter — but the skeleton was. AS7 catches the same defect class on the card layer. | Chapter-level: same auto-discovery as AS5/AS6; compares card[i].front and card[i].back to prior chapters' same-position cards. Groups matches into one finding per (card_index, front\|back). | BLOCKER (chapter) | [src/critics/intraBookFieldSimilarity.ts](src/critics/intraBookFieldSimilarity.ts) |
| AS8 | Implementation plan field of a newly-written chapter matches a previously-written chapter at ≥70% word overlap. Same May 2026 "Step 2 second-round" incident: every chapter's `coreSkill` used the template "Practice [TITLE] by pausing before the pressured response and naming the source pattern. Then choose one visible action that serves [CHAPTER-CONCEPT]…" The 24-hour challenge, weekly practice, and ifThenPlans followed the same pattern. AS8 covers all four scalar plan fields (title, coreSkill, twentyFourHourChallenge, weeklyPractice) plus the positional ifThenPlans[i].plan array. | Chapter-level: same auto-discovery. Compares each plan field across chapters at the same position (title-to-title, ifThen[0]-to-ifThen[0], etc.) | BLOCKER (chapter) | [src/critics/intraBookFieldSimilarity.ts](src/critics/intraBookFieldSimilarity.ts) |
| AS9 | Example scenario / whatToDo / whyItMatters of a newly-written chapter matches a previously-written chapter's same-position example at ≥70% word overlap. **The May 2026 "Start With Why Step 2" incident:** after AS5–AS8 + BP24 forced the writer agent to differentiate quiz, cards, plan, and breakdown across chapters, the agent applied the same template-substitution pattern to *example scenarios* — a field BP2 detects only at book-gate time. All 14 chapters of Start With Why shipped example scenarios with the same skeleton (`"[NAME] leans over a clipboard at 8:10 a.m. in the [LOCATION]. A quick answer would [CHAPTER-VERB-PHRASE], and the group is already nodding. Minutes before the room commits, [NAME] must decide whether to protect the fast fix…"`) with only name, location, and a single verb phrase swapped per chapter. Per-chapter ship gates passed because no chapter-level critic compared against siblings. BP2 caught it at book-gate time but by then all 14 chapters had been written. AS9 catches the same defect class at the chapter being written so the writer must compose chapter-specific examples before moving to the next chapter. | Chapter-level: same auto-discovery as AS5/AS6/AS7/AS8; compares example[i].scenario, example[i].whatToDo, example[i].whyItMatters to prior chapters' same-position examples. Groups all prior matches into a single finding per (example_index, field_name). Threshold: 70% word overlap (same as AS5/AS8 for prose-length fields). | BLOCKER (chapter) | [src/critics/intraBookFieldSimilarity.ts](src/critics/intraBookFieldSimilarity.ts) |

### Why these critics matter as a class

The BP1–BP14 critics from earlier sessions and BP15–BP21 from this session all rely on **verbatim n-gram matching**. They detect "the same 5 / 6 / 8 words appearing in N chapters." A motivated writer agent can defeat all of them by inserting one unique token per chapter at the substitution point: every n-gram becomes unique, the bytewise audits pass, the reader receives garbage. AS1–AS4 detect the gaming patterns directly (not the absence of repetition). AS4 in particular uses word-multiset similarity (not n-gram identity) so token substitution does not evade it.

### What the writer agent should do when an AS finding fires

The right fix is NEVER to remove the salt token. The right fix is to rewrite the field as a different sentence in different words from a different angle. If the agent cannot produce a different sentence (after 3 honest attempts), the upstream issue is that the chapter source notes are too similar across chapters, or the chapter's central concept overlaps another chapter's — surface that to the operator. The gate-attempt tracker at `state/gate-attempts.json` warns when an agent re-runs the gate on the same chapter with the same blocker signature 3+ times.

### Coverage gap that remains

Cross-chapter prompt-template detection (AS4) currently runs on quiz prompts only. A motivated agent could attempt the same gaming pattern in example scenarios or breakdown paragraphs. Extending AS4 to other field types is a known follow-up; for now, BP10–BP12 (breakdown paragraph repetition) and BP2 (example scenario repetition) provide partial coverage, and AS1–AS3 (which are field-agnostic) catch the salt-token patterns in those fields directly.

## G. Process and resilience

| ID | v13 failure | v21 enforcement | Severity | Code |
|----|-------------|-----------------|----------|------|
| G1 | Single-shot generation; if any unit failed, chapter was lost | Resume capability: existing chapter outputs are skipped; generateChapter is idempotent per chapter | RESOLVED-BY-DESIGN | [generateChapter.ts](src/generateChapter.ts) |
| G2 | Per-book repair scripts proliferated as one-offs | All repairs go through the same critic suite; no per-book tooling | RESOLVED-BY-DESIGN | (architecture) |
| G3 | No structured failure logging when an example failed | Writer-example errors logged with reason; orchestrator surfaces batch-1 failures before retry | RESOLVED | [generateChapter.ts](src/generateChapter.ts) |
| G4 | Pronoun false-positives cascaded into validation rejections | Validator filters pronouns from usedNames before enforcing | RESOLVED | [agents/writer-example.ts](src/agents/writer-example.ts) |
| G5 | Library-state ledger lost updates under concurrent generateBook runs (load-modify-write race plus a TOCTOU in the file lock) | Lock now uses atomic create-or-fail (`{ flag: "wx" }`); new `withLibraryState(fn)` helper holds the lock for the entire load-modify-write sequence so concurrent ingestions serialize without lost writes. Orchestrator's two ingest sites (resume-cache and post-ship-gate) call through the helper. Stress test at [src/scratch/stress-test-librarian.ts](src/scratch/stress-test-librarian.ts) runs 12-way concurrent ingestion and verifies every write persists. | RESOLVED | [librarian/libraryState.ts](src/librarian/libraryState.ts), [generateChapter.ts](src/generateChapter.ts) |

## Final gate

A chapter cannot ship to `state/chapters/` unless it passes the **chapter ship gate**, which runs every check above and fails closed on any BLOCKER. The gate lives at [src/critics/finalGate.ts](src/critics/finalGate.ts) and is called from [src/generateChapter.ts](src/generateChapter.ts) immediately before `writeFileSync`.

A book cannot be considered complete unless every chapter ship-gates AND the book-level gate passes (cumulative answer-position balance, no within-book name duplication, voice consistency).

## Adding a new failure mode

1. Add a row to the appropriate section above with ID (continuing the alphabetical sequence) and severity.
2. Implement the check (regex, heuristic, or model-backed).
3. Wire the check into [src/critics/finalGate.ts](src/critics/finalGate.ts) so the ship gate enforces it.
4. If the failure was found in shipped chapters, regenerate the affected ones.

This catalog should be the first thing a new engineer or new agent reads. If it isn't here, it isn't enforced.

## Rubric tuning

2026-05-13: removed `\bmost (readers|people)\b.{0,80}\b(assume|think|believe|expect)\b` and `\bwhat (you|they) (assume|expect|think) is\b` regexes from `PARADOX_SIGNALS` in [src/scratch/score-chapters.ts](src/scratch/score-chapters.ts) — the phrases are banned templating stems in [config/banned-phrases.json](config/banned-phrases.json), so rewarding them as a paradox signal pushed agents toward the templating defect (the rubric was awarding what the gate banned). Added five new regexes detecting non-templated paradox structures: `gets the credit`, `looks like ... is usually/often/actually`, `feels like ... the opposite/in fact/the real`, `the real/actual/hidden/hard/deeper lever/cause/move/test/reason/cost is`, and `sounds like ... in fact`. Side-effects documented: Extreme Ownership Ch3 ("What they assume is a bad crew is often a standard...") and The One Thing Ch8 ("...what they think is balance is often two areas slowly weakening...") regressed from 10/10 to 7/10 on the counter dimension — both openers were exactly the templating frame the change targets, so the regressions are correct.

2026-05-14: added B13 hook first-word cap and B14 counter shape cap audits in [src/critics/bookPatternAudit.ts](src/critics/bookPatternAudit.ts). Both use [config/author-voice-profiles.json](config/author-voice-profiles.json) to relax the per-book cap when the source author naturally clusters on one frame (Robert Greene leads with a historical figure most chapters; Cal Newport leads with a named practitioner). Without a profile the caps are 50% / 40%. With a profile the cap relaxes to `max(natural_share + 0.10, 0.40)` for first-word and `max(natural_share + 0.10, 0.30)` for counter shape. Side-effect: previously "100/100" books like GMM Vol 1 / Laws of Human Nature now cap at 80% because the audits surface real cross-chapter templating those books shipped with (75%+ chapters opening "The…", 80%+ chapters with negation_correction shape). These are not regressions in book quality — the audit caught defects that existed at ship time but had no enforcement.

Added A15 tier-length stub floor blocker in [src/critics/finalGate.ts](src/critics/finalGate.ts). fastRead < 350, deepRead < 1000, fullRead < 2400 chars fail-closes the ship gate. Caught 44 of 48 chapters in 48 Laws of Power that previously shipped at avg 347-char fullRead (the rubric docked points but the gate accepted them).

Narrowed B1-ext abstract-meta-frame regex in [src/critics/register.ts](src/critics/register.ts) and [src/scratch/score-chapters.ts](src/scratch/score-chapters.ts). The earlier `(?:this|the) idea\s+(is|argues|…|on)` form false-fired on legitimate scene narration like "the idea worked when museum attendance rose". The narrowed form requires an essay-verb within 5 tokens of the noun, so essay-shell uses still fire but body-prose uses don't.

Wired per-chapter writer awareness of prior chapter shapes in [src/generateBook.ts](src/generateBook.ts) / [src/generateChapter.ts](src/generateChapter.ts) / [src/agents/writer-hook.ts](src/agents/writer-hook.ts) / [src/agents/writer-breakdown.ts](src/agents/writer-breakdown.ts). Each writer call now receives the first-word of every prior hook and the counter shape of every prior counterintuition. The system prompts add hard rules so the writer steers away from any opener or shape already used in 50%/40%+ of prior chapters. This is the upstream fix; B13/B14 are the downstream backstop.
