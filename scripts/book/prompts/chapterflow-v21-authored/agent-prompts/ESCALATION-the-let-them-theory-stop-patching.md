# ESCALATION — the-let-them-theory: stop patching, change the writer

This is **not** redo #4. Three repairs have already run on this book and the
core defect has survived all three, each time changing surface form to evade
the gates and the QC greps:
- `POLISH-the-let-them-theory-examples-and-prose.md` (Jun 3 00:21)
- `REDO-the-let-them-theory-full-rewrite.md` (Jun 3 02:33)
- `REDO-the-let-them-theory-round2-assembly.md` (Jun 3 03:39) → applied 04:15

After the latest ("assembly") repair: `book-gate` PASS, 0 blockers, 0 majors,
no findings, positions 60/60/60, all `gate-chapter` 0 blockers — **and every
known QC grep returns clean** (0/180 directive-prefix, 0/180 container-noun,
0/180 echo-explanation, 0/120 label-only card fronts, fullRead rep ≤0.05). Yet
the quiz and examples are still fully templated Mad-Libs.

## Root cause: the "repair" was a slot-fill SCRIPT, not authoring
The chapters were regenerated by a deterministic JS generator
`scratch/reauthor-let-them-chapters.mjs` (mtime Jun 3 04:14 — exactly **one
minute** before the chapter files at 04:15; an earlier `scratch/
write-let-them-chapters.mjs` 03:34 did the prior pass). These scripts slot-fill
real source labels into fixed skeletons. That is why the Q1 key is the identical
hardcoded phrase in all 20 chapters and the 60/60/60 correctIndex split is
rotation, not judgment. **Do NOT re-run `reauthor-let-them-chapters.mjs` or
`write-let-them-chapters.mjs`** (or any `scratch/*let*them*.mjs`). A script
cannot author a quiz; it will keep producing skeletons that pass gates while the
content is broken. A 4th surgical prompt fed to the same script will be evaded
the same way. Per the QC playbook: after 3 form-shifting repairs, change the
generation method — author with a real model.

## What is still broken (verbatim, after the "assembly" repair)

**Quiz — 180/180 templated, with WRONG keys.** Nine fixed skeletons rotate one
per slot. Q1 in ALL 20 chapters is the same question with the same 3 choices,
and the key is the identical generic phrase **"<concept>: sort control from
conduct."** in 20/20 chapters — so it is keyed as the "working definition" of
*Comparison as teacher* (ch10), *Friendship flexibility* (ch12), *ABC Loop*
(ch15), *Deal breaker decision* (ch20), etc. That phrase only fits ch01's
*Control separation*; **the Q1 key is wrong in ~19 of 20 chapters.** Distractors
("chase motive certainty", "trade standards for quiet") are identical across all
chapters. Q5 is the same template everywhere ("<concept> turns <Person> pressure
into action" keyed; prompts graft a truncated source fragment onto "under
pressure"). 8/180 explanations truncate mid-sentence.

**Examples — 120/120 templated word-salad** (six per-slot skeletons, which is
why a single grep only flags 20/120). Examples:
> "Asha reads Oakley's prom-night taco bar through the family kitchen … Asha
> asks whether Oakley's prom-night calls for a boundary, Asha's repair attempt,
> waiting, or Asha's plain speech for Asha in the family kitchen."
> "Brenna studies Kendall's kitchen intervention after the rainy driveway after
> the tux photos raises old pressure."
> "Daphne tests that relative against kitchen island after that relative."

Concept/scene used as actor, double-prepositions, truncations ("taco" from
"taco bar"; "in the kitchen with ADHD-related" cut off), and a trailing
"asks whether <X> calls for <menu>" word-salad.

## Salvageable (do NOT discard)
`hook` (good), `breakdown.fastRead/deepRead/fullRead` (coherent content, only
templated connector seams like "<Title> is about …", "The core move is …",
"<source> makes the idea concrete"), and `reviewCards` (real question fronts +
coherent definition backs). The underlying Mel Robbins content is accurate.

## Required action (escalation, not a patch)
1. **Change the writer model or the quiz/example generation method.** The
   current generator cannot author a real quiz or example for this book — it
   assembles fragments into per-slot skeletons. Patching the prompt has failed
   3×. Use a different model, or a generation path that writes each question and
   each example as standalone prose (no shared skeleton, no fixed-position key).
2. Regenerate ONLY `quiz` (all fields) and `examples` (scenario + whatToDo) for
   all 20 chapters; preserve hooks, breakdown, and cards byte-for-byte.
3. Hard requirements for the new quiz/examples:
   - No two chapters may share a Q1 (or any Q) answer key string. Each key is
     the chapter's own concept-specific content.
   - Distractors must be plausible-but-wrong and chapter-specific; no shared
     distractor set across chapters.
   - The correct choice must not be identifiable by position, length, prefix,
     or a generic phrase. Vary key position by real correctness, not template.
   - Explanations are complete sentences that justify the key for THIS question.
   - Examples are concrete human scenarios; a concept may be referenced, never
     used as an actor/object; no "<Name> reads <X> through <place>", no
     "asks whether <X> calls for <menu>", no truncated source fragments.

## Writer self-verification BEFORE claiming done (the gates will NOT catch this)
Run and paste the output:
- Across all 20 chapters, list each question's answer-key string; **confirm no
  key string repeats across chapters** (the current failure: Q1 key identical in
  20/20).
- Confirm no shared distractor appears in >2 chapters.
- Read 3 full chapters' quizzes and examples aloud; confirm each reads as
  written-by-a-person.

## Done condition
- `gate-chapter` 0 blockers ×20 and `book-gate` 0 blockers (necessary, NOT
  sufficient — they already pass while broken).
- **AND** QC content read confirms: no cross-chapter duplicate keys; ch10/ch12/
  ch15/ch20 Q1 keys define THEIR OWN concept (not "sort control from conduct");
  examples are coherent. If any of these fail again, do not regenerate with the
  same writer — escalate the model choice.
