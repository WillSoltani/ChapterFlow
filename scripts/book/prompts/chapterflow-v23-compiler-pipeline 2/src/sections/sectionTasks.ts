import { existsSync, readFileSync } from "fs";

import { expectedSourceChapters } from "../qc/sourceV2Gate.js";
import { normSlug } from "../lib/chapterPaths.js";
import {
  blueprintPath,
  readJsonFile,
  sectionPath,
  sectionTaskPath,
  writeTextFile,
  sourcePacketPath,
  type CompilerStoreRoots,
} from "../artifacts/artifactStore.js";
import { SECTION_KINDS, type ChapterBlueprintV1, type SectionKind, type SourcePacketV1 } from "../artifacts/artifactTypes.js";

export type SectionTask = {
  bookId: string;
  chapterNumber: number;
  chapterId: string;
  kind: SectionKind;
  taskPath: string;
  outputPath: string;
  exists: boolean;
};

const ROLE_NAME: Record<SectionKind, string> = {
  "summary-pack": "SummaryPack writer",
  "example-pack": "ExamplePack writer",
  "learning-pack": "LearningPack writer",
  "action-pack": "ActionPack writer",
};

function sectionContract(kind: SectionKind): string {
  switch (kind) {
    case "summary-pack":
      return `Write ONLY the hook, tiered summaries, keyTakeaway, and optional tryThisNow. Do not write examples, quiz, review cards, or implementationPlan. Keep keyTakeaway at 30 words or fewer, always. Use reservedVariety.hookShape as the hook's assigned opening move. In a parallel batch, no three hooks in five may start with the same first word; do not default to location-stamp openers like "At", "In", or "On". Keep counterintuition and takeaway openers syntactically simple: no run-on list openers with three commas in the first 80 characters. Keep fastRead at grade 7-8 plain language and at least 350 characters, deepRead at grade 9-11 and at least 1000 characters, and fullRead at grade 10-12 with at least 2400 characters; use short sentences and plain verbs. Seed at least three standalone memorable-line candidates in the breakdown: 6-16 words, portable outside the paragraph, not a category list, not an "if not/if so" fragment, not a question, and not an explanatory checklist. For breakdown.sourceAnchorIds, prefer testable_fact/framework anchors; only cite a namedExample anchor in a breakdown tier if the tier and any likely short memorable line from that tier include at least two of that anchor's hardSpecifics verbatim. Do not paste source-note sentences or long framework/list runs verbatim; keep required hardSpecific phrases short, then paraphrase the surrounding claim in this section's own voice. Never echo a famous hardSpecific as a reusable five-word tag across tiers or chapters; if a case detail such as "red phone by the pool" must appear, use it in one teaching unit and paraphrase the mechanism elsewhere. Never expose audit labels like "Fact 2" or "Fact five", "Source 3" or "Source six", or source-note numbering in ANY digit or spelled-out form in reader prose; name the evidence itself. Avoid jammed CamelCase source labels unless the exact brand is allowlisted; use a spaced natural name or descriptive phrase. Never paste a source anchor's label verbatim into reader text: anchor labels use an internal "Entity / descriptor" bookkeeping form (for example "Disney parks / evening spectacular", "Southwest Airlines / playful safety routines", or "John Deere / first-day peak"), so name the case in natural prose ("Disney's nightly fireworks finale") and never carry a label's " / " separator into a sentence. Do not anonymize a real, nameable public case into a periphrastic tell such as "a Moline-based equipment company" or "a U.K.-founded sandwich chain"; name it plainly or describe it in the chapter's own words. AS10 compares fastRead, deepRead, and fullRead across the whole book, so never repeat the book's framework list as a stock sentence in every tier or every chapter; when shared vocabulary is necessary, use it once and surround it with chapter-specific source facts, named cases, and verbs. Do not stamp framework triads such as "transition, milestone, or pit" across chapters; translate them into chapter-specific thresholds, earned markers, low points, repairs, or relationship turns. Do not turn source-grounding rules into reader-facing prose: avoid "at least 3 named cases", "concrete settings give memory a handle", "named people, places, dates, or numbers", and "claims checkable" unless you are briefly explaining provenance, not teaching the chapter. Do not use "attention, meaning, or memory" or variants such as "attention, meaning, memory" and "attention, meaning, or shared..." as generic proof loops; name the chapter-specific observable proof instead. Do not reuse stock fullRead paragraphs or connective scaffolds from sibling chapters. Write each tier with a chapter-specific skeleton: open from this chapter's core move, rotate through this chapter's named cases/frameworks, use unique transition verbs, and avoid reusable five-word connective runs such as "the practical question is therefore", "the hard edge is", "the useful answer is", "targets are transitions, milestones, and", "at least 3 named cases", "red phone by the pool", "the stake-fit rule because a", "attention, meaning, or memory", or any repeated list like "elevation, insight, pride, and connection". In fullRead, every body paragraph after the opener should contain a unique named case label or hardSpecific from this source packet, and the sentence around it must teach the chapter's move rather than the source-gathering rule. VOICE — narrate the real cases as LIVED MOMENTS, not abstract summaries: this genre teaches through concrete stories, so build deepRead and fullRead AROUND this chapter's real named cases. Open a case with one specific sensory moment drawn ONLY from its hardSpecifics (a named person, place, object, or number that is actually in the source), let the reader briefly FEEL the moment, THEN name the principle it proves. As a STYLE model only: prefer "The nurse taped a bright cartoon over the ceiling light so the boy staring up during the scan had something to find, and he stopped crying" over "Environments can be redesigned to reduce patient distress." Invent nothing beyond this chapter's own source hardSpecifics — the sample scene is only a voice model, so never import its nurse/boy/scan or any other book's cast, and if you have only a bare fact, state it plainly rather than embroidering it. Output SummaryPackV1 JSON only.`;
    case "example-pack":
      return `Write ONLY the chapter's example pack. Produce exactly the six example slots in the blueprint; the final v21 gate requires six. Use exampleId values exactly "ex01" through "ex06" in slot order, or chapter-scoped ids like "ch02-ex01-short-slug"; never include the bookId in exampleId. Every example must be a concrete human scene with a named person living a DEFINING MOMENT — which may be a decision or repair, but may equally be a reaction to a surprise, a ritual or milestone, a consequence unfolding from earlier, a first-time experience, a recognition, or a small kindness. Do NOT default all six to "a person weighs a choice"; let the dealt sceneFrame/requiredBeat set the KIND of moment for each slot. Use only the blueprint's allowed fictional names, and use a DIFFERENT dealt name as the protagonist of each of the six examples (never reuse one name as both a cited character and a later protagonist); never use source-figure names as invented actors. Each example slot has a dealt sceneFrame and requiredBeat; obey both so same-position examples across parallel chapters do not share one dramatic transaction. Each example must cite a namedExample/example anchor and include at least two of that anchor's hardSpecifics verbatim in the scenario, whatToDo, or whyItMatters. The whyItMatters sentence must explain the same cited sourceFactIds and the decision shown in the scene; do not rotate to a neighboring fact just to mention the named case. If the named case is only supporting evidence, state the source fact's decision logic first and use the case as a boundary or example. Source facts/cases must drive the scene logic, never appear as props, labels, wall cards, desk objects, or title subjects. Never write source-label bookkeeping sentences like "<Case label> includes/contributes/supplies/points to..." or "<hardSpecific> is evidence for fit"; instead, show the protagonist using the mechanism in a concrete local decision, then mention hard specifics only as evidence inside that decision. Never paste a source anchor's label verbatim: anchor labels use an internal "Entity / descriptor" bookkeeping form (for example "Disney parks / evening spectacular" or "Southwest Airlines / playful safety routines"), so name the case in natural prose and never carry a label's " / " separator into a sentence; and do not anonymize a real, nameable public case into a periphrastic tell such as "a Moline-based equipment company" or "a U.K.-founded sandwich chain". Vary openings, venues, protagonists, outcomes, and title grammar across the six; do not give every title the same "<Name> decision ..." shape. The whatToDo line must add a new reader instruction, test, scorecard, refusal rule, or observation that is not already narrated in the scenario; never paraphrase the scenario's verbs, props, or sequence as the advice. Do not let one slot across the book become "improve the whole process versus one loaded/focused point"; only use that broad-polish tradeoff when the dealt sceneFrame requires it, and otherwise center consequences, failed tests, stakeholder conflict, measurement surprise, deletion, or a boundary case. Never use "one focused intervention" as a reader-facing abstraction; name the concrete chapter-specific repair, threshold, handoff, test, or comparison instead. Do not let any same-position slot across a book become "pleasant average versus memorable peak/low/ending"; when a source fact mentions peak-end memory, turn it into the dealt sceneFrame's concrete action (failed trial, interview, deletion, owner handoff, or field test) instead of another average-smoothing tradeoff. No exact five-word phrase may appear across three or more example scenarios, whatToDo lines, or whyItMatters lines, even if it is a source/legal label and even if only two words carry content; use different anchors, paraphrase the setup, or move the source detail into one scene instead of stamping it across the slate. Avoid known BP13 connective/prop stamps such as "transition, milestone, or pit, then", "a transition, milestone, or pit", "the stake-fit rule because a", "red phone by the pool", "attention, meaning, or memory", and "tradeoff memo" unless this exact chapter owns that source detail as its one teaching unit. AS9 compares examples across prior chapters, so do not use a finite cycle of reusable scene frames that repeats in later chapters; each chapter-slot needs a distinct role, timing, scene engine, turning point, and advice shape. Never use stock next-step phrases like "so the next action is", "the next action is to", or "the next action is" in examples; name the action directly in fresh words. Do not write jammed CamelCase proper nouns such as BrokerCheck in reader-facing prose; if a source name is CamelCase, use a descriptive phrase or a spaced, natural form that the gate accepts. Do not reuse stock connective phrases from sibling chapters; phrases like "The social pressure is mild" and formulaic closers like "[Name] decides after X, not before" become book-wide patterns when parallel writers repeat them. Do not make pending/until/only-if evidence gates the default ending, and do not repeatedly close with "partial answer/result/outcome, then next action/review/later evidence." Do not make the default scene pressure a tactile mundane action while another person waits, asks, or presses for an answer; vary point of view, timing, stakes, and scene mechanics. Across a batch, vary outcomes with decisive rejection, bounded approval, chosen comparison winner, post-decision audit, owner action, or changed sizing. Do not cycle generic action containers across chapters, especially tradeoff memo, prospectus packet, broker statement, portfolio policy file, bond quote sheet, allocation worksheet, and research queue. If you use a document, memo, note, or audit, make it one chapter-specific detail, not the scene engine for the whole pack. Do not reuse the document-plus-old-default/shortcut-plus-repair frame, and do not make the book-wide default scene engine "old shortcut/default/test fails, checklist/source fact interrupts, decision becomes pending/rejected/review." Change the opening action, pressure, turning point, and outcome logic across chapters. Prefer varied scene engines: a sound process pressure-tested before commitment, two plausible choices compared, successful execution with friction, a consequence after a past decision, a boundary case, or a changed amount/timing/commitment after new evidence. SCENE-ENGINE DIVERSITY IS REQUIRED: across the six examples use SIX DIFFERENT scene engines — do NOT write six variations of one person weighing two options. At least THREE of the six must be a NON-deliberation engine, i.e. not "someone decides between choices": a surprise or delight that lands on someone; a habit or ritual observed in mid-action; a consequence unfolding from an earlier choice; a boundary case where the move backfires or must be withheld; a first-time or milestone experience; or a public recognition or reveal. Vary WHAT KIND of moment it is and WHO experiences it, not merely the names, venues, and props. Two examples whose only difference is the swapped name/object are a repeat, not a variation. Do not rely on generic action containers or overused default venues such as budget apps, shared spreadsheets, calendar reminders, service counters, notebook margins, team chats, kitchen tables, conference rooms, or break rooms unless the source case specifically requires that workflow; if you use one, make it chapter-specific and do not repeat it as the default scene anchor. Include controlled friction or recovery without turning the ending into a repeated partial-answer/next-action template; avoid six instant wins. Do not open with stock phrases like "[Name] is on a phone call" or "[Name] is at the front desk"; make the first action specific to this chapter's decision. Never use synthetic scene scaffolds such as "beside the slate cabinet", "the page toward", coined compound tokens like "slatefilter"/"riskcard", or any "stays closed" closure. Do not write summaries, quiz, cards, or implementation. Output ExamplePackV1 JSON only.`;
    case "learning-pack":
      return `Write ONLY the quiz and review cards. Each quiz question MUST include questionId, prompt, exactly 3 choices, correctIndex, explanation, bloomsLevel, and depthLevel. Use bloomsLevel values remember|understand|apply|analyze|evaluate|create; use depthLevel from blueprint.sections.quiz[].depthLevel for the matching slot. correctIndex MUST match blueprint.sections.quiz[].correctIndex exactly. Quiz sourceAnchorIds/keyEvidenceAnchorIds must use anchors whose supportsClaimTypes include quiz_prompt, quiz_explanation, and quiz_key_evidence; concrete namedExample anchors may be used when the source packet lists those claim types. For each quiz slot, obey its dealt promptShape, answerStyle, distractorTrap, and caseCueIds; those fields exist to keep same-position questions from matching sibling chapters. Use caseCueIds as scenario/detail cues and as sourceAnchorIds/keyEvidenceAnchorIds when their anchors support quiz claims; otherwise pair them with the requiredFactIds. Never expose audit labels like "Fact 2" or "Fact five", "Source 3" or "Source six", or source-note numbering in ANY digit or spelled-out form in quiz explanations, prompts, choices, or cards; name the evidence itself. Avoid jammed CamelCase source labels unless the exact brand is allowlisted; use a spaced natural name or descriptive phrase. Never paste a source anchor's label verbatim into reader text: anchor labels use an internal "Entity / descriptor" bookkeeping form (for example "Disney parks / evening spectacular", "Southwest Airlines / playful safety routines", or "John Deere / first-day peak"), so name the case in natural prose ("Disney's nightly fireworks finale") and never carry a label's " / " separator into a sentence. Do not anonymize a real, nameable public case into a periphrastic tell such as "a Moline-based equipment company" or "a U.K.-founded sandwich chain"; name it plainly or describe it in the chapter's own words. AS5 compares q01-to-q01, q02-to-q02, etc. across the whole book, so a same skeleton with swapped nouns will block; choose a different scenario, question opening, decision pressure, and evidence source for every slot. Keep quiz prompts LEAN and scenario-driven. Do NOT front-load named-case labels: a prompt that stuffs several source proper nouns ("a Los Angeles hotel", "a Houston public charter network", "free popsicles on a silver tray") reads as a matching exercise and telegraphs the key. Name AT MOST ONE case in a prompt, and only when the question truly hinges on it; otherwise pose a fresh concrete mini-scenario that tests the MECHANISM. Do NOT repeat a verbatim case-identifying phrase (e.g. "free popsicles on a silver tray", "students announce college choices", "red phone by the pool") across multiple quiz stems or choices — reference each named case in at most one question. Distractors must discriminate on the mechanism, not on recognizing which source detail was named. AS6 compares correct answers and distractors across chapters, so every correct answer must name this chapter's requiredFactIds mechanism/case and a concrete action or diagnosis in fresh words. Never use reusable correct answers like "change attention, meaning, or memory", "use elevation, insight, pride, and connection", "keep the choice flexible", or other abstract book-level slogans. Review cards must use review_card-capable anchors, including concrete namedExample anchors when available. For each review card, obey frontShape, retrievalTarget, backShape, and caseCueIds; include a hardSpecific or case label from the cued named case in the front/back text whenever caseCueIds is present. Never make a review card retrieve source-grounding requirements such as "at least 3 named cases", "concrete settings give memory a handle", "named people, places, dates, or numbers", "claims checkable", or QC/source-anchor discipline; those are provenance supports, not reader learning goals. Wrong choices must be plausible, not strawman absolutes: avoid always, never, automatically, impossible, guaranteed, entirely, ever, forever, completely, wholly, absolutely, under no circumstances, and in all cases in distractors. For named-case questions, wrong choices must usually stay inside the same named case or mechanism; do not make a distractor obviously wrong by importing a different source case's proper noun, prop, or artifact. Never append mechanical proof tails to choices: banned examples include "under the stated evidence test" and "after checking the concrete source condition"; each wrong choice must read as a natural chapter-specific misconception, not a clean sentence plus a generic evidence/source suffix. Keep the correct answer similar in length to the distractors; it must stay below 1.4x their average word count and at or below 1.5x their average character count. Distractors should have enough scenario-specific substance that the correct answer is not usually the longest. Vary quiz prompt grammar, choice grammar, explanations, card fronts, and card backs; do not reuse "What check does..." card stems or "The source fact says..." explanation stems across a chapter. Review card fronts/backs are checked across the whole book, so every card must follow its dealt frontShape, retrievalTarget, and backShape and include a chapter-specific noun, case, mechanism, or contrast from its requiredFactIds instead of a reusable abstract shell. Do not use generic cross-book stems like "What should you inspect", "What check does", "Why does this matter", "What is the key move", "When should the reader", or "How can you apply"; ask a fact-shaped recall question in varied forms such as comparison, boundary, failure mode, trigger, diagnostic sign, or named-case lesson. Card backs must answer in a different sentence shape from neighboring cards and must not start with the same explanatory frame across cards. Output LearningPackV1 JSON only.`;
    case "action-pack":
      return `Write ONLY tryThisNow and implementationPlan. Actions must be concrete, low-friction, and provable. All tryThisNow and implementationPlan provenance must use anchors whose supportsClaimTypes include implementation_guidance; concrete namedExample anchors may be used when the source packet lists that claim type. Never expose audit labels like "Fact 2" or "Fact five", "Source 3" or "Source six", or source-note numbering in ANY digit or spelled-out form in reader-facing action prose; name the evidence itself. Avoid jammed CamelCase source labels unless the exact brand is allowlisted; use a spaced natural name or descriptive phrase. Never paste a source anchor's label verbatim into reader text: anchor labels use an internal "Entity / descriptor" bookkeeping form (for example "Disney parks / evening spectacular", "Southwest Airlines / playful safety routines", or "John Deere / first-day peak"), so name the case in natural prose ("Disney's nightly fireworks finale") and never carry a label's " / " separator into a sentence. Do not anonymize a real, nameable public case into a periphrastic tell such as "a Moline-based equipment company" or "a U.K.-founded sandwich chain"; name it plainly or describe it in the chapter's own words. Start tryThisNow with a chapter-specific concrete trigger; do not reuse book-wide opener shells such as "Open the next stock idea", "Before the next stock decision", or "Each Friday". implementationPlan.coreSkill must be built around action.practiceForm and action.practiceConstraint, and must end with a chapter-specific practice sentence, not a reusable closer such as "Use a written checkpoint so the decision can be repeated under pressure." AS8 compares implementationPlan fields across chapters, so each ifThen plan must follow a different dealt action.ifThenPlanShapes[] entry and include this chapter's requiredFactIds, source mechanism, action.practiceForm, and action.practiceConstraint. Do not make ifThen plans repeat the stock "social pressure pushes/favors/praises X, then pause for evidence first/before approving/copying it" shell; use chapter-specific triggers and actions such as owner handoff, deletion, field test, refusal rule, measurement swap, rehearsal, or post-moment review. coreSkill and twentyFourHourChallenge must not both use the same classify-opportunity, choose-lever, predict-change sequence. twentyFourHourChallenge must use the dealt action.practiceForm as its exercise form; vary its opener and cadence across chapters, not another repeated "Before tomorrow ends" / "Within the next day" shell. Do not repeatedly ask readers to classify a transition/milestone/pit, choose an elevation/insight/pride/connection lever, and predict an attention/meaning/memory shift unless that exact practiceForm calls for classification. Do not use "attention, meaning, or memory" or variants such as "attention, meaning, memory" and "attention, meaning, or shared..." as generic proof loops; name the chapter-specific observable proof, behavior, receipt, or recovery signal instead. Do not stamp the exact phrase "transition, milestone, or pit" across tryThisNow or if-then plans; use chapter-specific triggers such as first-day entry, public achievement, recovery point, stretch point, recognition receipt, shared-purpose turn, or relationship repair. Each ifThenPlans[].context must be a situational trigger phrase, not a bare venue, source label, or stage direction: use forms like "Before buying a familiar security" or "When a proposed holding lacks a price check". Do not ask readers to compare a live decision to a full source-case phrase; lead with the local rule, then use one concrete source detail only when it clarifies the rule. Do not make the book-wide default practice "create/open a template, row, gate, blank, or checkpoint and keep the idea pending until every blank is filled"; use varied behaviors such as a two-option comparison, a price cap, a rejection rule, a delegation boundary, a one-minute audit, a cadence change, or a written owner question when the source supports it. Do not paste source-note sentences or long framework/list runs into tryThisNow, coreSkill, challenges, weeklyPractice, or if-then plans; translate the source into a reader action and vary any checklist wording. Output ActionPackV1 JSON only.`;
  }
}

function sectionSchemaHint(kind: SectionKind): string {
  switch (kind) {
    case "summary-pack": return `{"schemaVersion":"section-artifact-v1","artifactType":"summary-pack","chapterId":"...","hook":{...HookOutput},"breakdown":{...BreakdownOutput},"keyTakeaway":"...","keyTakeawaySourceAnchorIds":["..."],"tryThisNow":"...","tryThisNowSourceAnchorIds":["..."],"sourceFactIds":["..."]}`;
    case "example-pack": return `{"schemaVersion":"section-artifact-v1","artifactType":"example-pack","chapterId":"...","examples":[{"exampleId":"ex01","title":"...","scenario":"...","whatToDo":"...","whyItMatters":"...","sourceAnchorIds":["..."],"sourceFactIds":["..."],"namedCaseIds":["..."]}]}`;
    case "learning-pack": return `{"schemaVersion":"section-artifact-v1","artifactType":"learning-pack","chapterId":"...","quiz":{"passingScorePercent":70,"questions":[{"questionId":"q01","sourceAnchorIds":["..."],"keyEvidenceAnchorIds":["..."],"prompt":"...","choices":["...","...","..."],"correctIndex":0,"explanation":"...","bloomsLevel":"apply","depthLevel":"standard"}]},"cards":{"cards":[...]}}`;
    case "action-pack": return `{"schemaVersion":"section-artifact-v1","artifactType":"action-pack","chapterId":"...","tryThisNow":"...","tryThisNowSourceAnchorIds":["..."],"implementationPlan":{...ImplementationPlanOutput}}`;
  }
}

export function buildSectionTaskMarkdown(args: { bookId: string; kind: SectionKind; blueprint: ChapterBlueprintV1; sourcePacket: SourcePacketV1; outputPath: string }): string {
  const { bookId, kind, blueprint, sourcePacket, outputPath } = args;
  const sectionInput = kind === "summary-pack"
    ? { hook: blueprint.sections.hook, summaries: blueprint.sections.summaries, coreMove: blueprint.coreMove, reservedVariety: blueprint.reservedVariety }
    : kind === "example-pack"
      ? { examples: blueprint.sections.examples, reservedVariety: blueprint.reservedVariety, forbiddenLeakage: blueprint.constraints.forbiddenLeakage }
      : kind === "learning-pack"
        ? { quiz: blueprint.sections.quiz, cards: blueprint.sections.cards, answerIndexPattern: blueprint.reservedVariety.answerIndexPattern }
        : { action: blueprint.sections.action, reservedVariety: blueprint.reservedVariety };
  return `ROLE\nYou are the ${ROLE_NAME[kind]} for ChapterFlow v23. You have one bounded artifact to produce.\n\nINPUTS\n- bookId: ${bookId}\n- chapterId: ${blueprint.chapterId}\n- chapterNumber: ${blueprint.chapterNumber}\n- outputPath: ${outputPath}\n\nTASK\n${sectionContract(kind)}\n\nDO NOT\n- Do not edit any file except ${outputPath}.\n- Do not weaken schemas, gates, source sidecars, QC artifacts, or other chapters.\n- Do not introduce new real-world entities, numbers, dates, places, participants, studies, institutions, or outcomes unless they appear in the source packet below.\n- Do not mention "this chapter", "the book", or "the author" in reader-facing content.\n- Never use hard-banned register phrases or opener shells such as "The trap is to", "The trap is not", "The mistake is to", "The paradox is that", "Most readers assume", or "Most people think".\n- Avoid soft-banned house tics: "rather than", "That matters because", "turns out to be", and "treats it as". Use plain alternatives unless the phrase is truly necessary.\n- Do not change the final ChapterV21 schema; this is an intermediate artifact only.\n\nOUTPUT SCHEMA HINT\n\`\`\`json\n${sectionSchemaHint(kind)}\n\`\`\`\n\nSECTION-SPECIFIC BLUEPRINT\n\`\`\`json\n${JSON.stringify(sectionInput, null, 2)}\n\`\`\`\n\nFULL CHAPTER BLUEPRINT\n\`\`\`json\n${JSON.stringify(blueprint, null, 2)}\n\`\`\`\n\nSOURCE PACKET — ONLY allowed facts/cases/numbers/entities\n\`\`\`json\n${JSON.stringify(sourcePacket, null, 2)}\n\`\`\`\n\nVALIDATION\nAfter writing ${outputPath}, run:\n\n  npx tsx src/cli.ts validate-sections ${bookId} --chapters ${blueprint.chapterNumber} --section ${kind}\n\nStop only when validation passes for this section or the validator gives a precise blocker you cannot resolve without more source.\n`;
}

export function dealSectionTasks(bookId: string, roots: CompilerStoreRoots = {}): SectionTask[] {
  const normalized = normSlug(bookId);
  const tasks: SectionTask[] = [];
  for (const chapterNumber of expectedSourceChapters(normalized, { stateRoot: roots.stateRoot })) {
    const blueprint = readJsonFile<ChapterBlueprintV1>(blueprintPath(normalized, chapterNumber, roots));
    const sourcePacket = readJsonFile<SourcePacketV1>(sourcePacketPath(normalized, chapterNumber, roots));
    for (const kind of SECTION_KINDS) {
      const outputPath = sectionPath(normalized, chapterNumber, kind, roots);
      const taskPath = sectionTaskPath(normalized, chapterNumber, kind, roots);
      writeTextFile(taskPath, buildSectionTaskMarkdown({ bookId: normalized, kind, blueprint, sourcePacket, outputPath }));
      tasks.push({ bookId: normalized, chapterNumber, chapterId: blueprint.chapterId, kind, taskPath, outputPath, exists: existsSync(outputPath) });
    }
  }
  return tasks;
}

export function sectionTasks(bookId: string, roots: CompilerStoreRoots = {}): SectionTask[] {
  const normalized = normSlug(bookId);
  const tasks: SectionTask[] = [];
  for (const chapterNumber of expectedSourceChapters(normalized, { stateRoot: roots.stateRoot })) {
    let chapterId = `${normalized}-ch${String(chapterNumber).padStart(2, "0")}`;
    try { chapterId = readJsonFile<ChapterBlueprintV1>(blueprintPath(normalized, chapterNumber, roots)).chapterId; } catch { /* keep derived */ }
    for (const kind of SECTION_KINDS) {
      const taskPath = sectionTaskPath(normalized, chapterNumber, kind, roots);
      const outputPath = sectionPath(normalized, chapterNumber, kind, roots);
      tasks.push({ bookId: normalized, chapterNumber, chapterId, kind, taskPath, outputPath, exists: existsSync(outputPath) });
    }
  }
  return tasks;
}

export function missingSectionTasks(bookId: string, roots: CompilerStoreRoots = {}): SectionTask[] {
  return sectionTasks(bookId, roots).filter((t) => !t.exists || !existsSync(t.taskPath));
}

export function readSectionTask(task: SectionTask): string {
  return readFileSync(task.taskPath, "utf8");
}
