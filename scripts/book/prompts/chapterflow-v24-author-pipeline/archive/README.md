# Archive — completed one-time documents

Files here did their job and are kept for history only. **Nothing in this
directory is a live instruction — never paste these into a session.**

- `MIGRATION-*`, `BOOK-MIGRATION-PROMPT` — the v1→v21 migration campaign (completed).
- `AGENT-BRIEFING`, `OPERATOR-PROMPT` — the pre-Codex "inline operator" mode,
  superseded by the 3-step flow (it predates the author≠reviewer trust split).
- `QC-PATTERNS`, `QC-everything-is-fcked` — early QC notes, superseded by
  QC-PLAYBOOK + QC-SESSION-PROMPT.
- `CODEX-BRIEF-*` — executed build briefs (palettes, charters, dealing layers,
  stillness remediation). The standing anti-gaming ADDENDUM lives on in
  agent-prompts/CODEX-BRIEF-CATALOG-REFRESH.md, which stays active (Part 4
  is still pending for legacy books).
- `REDO-START-WITH-WHY-*`, `POLISH-PROMPT` — completed repair/polish rounds.

The living documents are: README.md, PIPELINE-HANDOFF.md, MASTER-PLAN.md,
FAILURE-MODES.md at the pipeline root, and everything in agent-prompts/.
