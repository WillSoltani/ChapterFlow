{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "forward-repair-operation-receipt.schema.json",
  "title": "Forward typed-repair operation receipt",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "operation", "chapterId", "artifactPath", "artifactSha256"],
  "properties": {
    "schema": { "type": "string", "enum": ["forward-repair-operation-receipt-v1"] },
    "operation": { "type": "string", "enum": ["typed-repair"] },
    "chapterId": { "type": "string" },
    "artifactPath": { "type": "string", "enum": ["patch.json"] },
    "artifactSha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
  }
}
