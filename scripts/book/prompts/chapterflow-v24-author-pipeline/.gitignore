# Pre-QC variety-scout reads (P08). Ephemeral operator/P10 comparison artifacts written by the
# autopilot; they are NOT QC evidence and must never be committed into the tracked state corpus.
state/qc-preflight/
