{
  "$schema": "./rubric-thresholds.schema.json",
  "schemaVersion": "rubric-thresholds-v1",
  "_comment": "Thresholds for the deterministic rubric pre-flight (P04). FAIL-capable metrics: fleschEase (breakdown), tellRateMax, transferMin, memorableCleanMin. fkGrade is ADVISORY (warn-only): review calibration on the real catalog showed the top books read easier than the band (atomic-habits breakdown FK ≈ 4.2 → an FK-min gate would fail 20/20 of its chapters), so the ease band is the enforceable readability ruler and FK is descriptive. House-tic and nominalization are DIAGNOSTIC (warn-only). Rates here are FRACTIONS in [0,1] (tellRate/transferRatio), not score.py's 0..100 percentages. Do NOT tighten a fail band or flip a warn-only metric to fail without a zero-false-positive calibration against book-packages/*.v21.json.",
  "fleschEase": { "min": 72, "max": 84, "warnTolerance": 4 },
  "fkGrade": { "min": 7, "max": 8, "warnTolerance": 1 },
  "tellRateMax": 0.20,
  "transferMin": 0.70,
  "memorableCleanMin": 2,
  "houseTicDensityWarnMax": 1,
  "nominalizationRateWarnMax": 8,
  "_cardQualityComment": "W2 (plan §WS5) deterministic per-chapter CARD-QUALITY gates, calibrated against the top-5 owner-scored packages (all pass) and the-power-of-moments v24 (echo flags 4 known lifts; shortest length-tell FAILs at 51%). echoKeyThreshold=5 is the zero-FP tier (the >=4 tier admits canonical-principle-key FPs); echoDistractorCeiling=4 requires all distractors below a 4-token echo. lengthTellShortestMax=4 and lengthTellLongestMax=9 are per-chapter caps out of 9 questions; the shortest cap is the active gate (top-5 peak at 4/9, POM v24 runs 5-8/9), the longest cap is calibrated to pass the top-5 (whose keys are the uniquely-longest choice up to 9/9 by long-standing house norm) while keeping the symmetric mechanism in place. See docs/v24/w2-card-preflight-calibration.md.",
  "cardQuality": {
    "echoKeyThreshold": 5,
    "echoDistractorCeiling": 4,
    "lengthTellShortestMax": 4,
    "lengthTellLongestMax": 9
  }
}
