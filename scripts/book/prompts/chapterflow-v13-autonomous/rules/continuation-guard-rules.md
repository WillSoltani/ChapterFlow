Continuation guard rules

After Chapter 1 automatic gate clears:
- continue only in waves
- every active chapter must still go through the full chapter loop
- no book-specific content generator scripts
- no "one controlled pass" synthesis
- no package-level prose generation
- no bypassing the prose loop because early chapters already passed

Stop immediately if logs or plans contain:
- "bulk generator"
- "synthesize the rest"
- "emit the remaining chapters in one pass"
- "generate to the validator"
- "preserve chapters 1 and 2, synthesize the rest"
