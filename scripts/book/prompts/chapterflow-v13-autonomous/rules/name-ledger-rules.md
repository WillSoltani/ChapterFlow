Name rules

- use assigned names only
- no within-chapter name reuse across examples unless intentional callback is explicitly documented
- by default, do not reuse a character name across the book
- continuity state must track `withinChapterNames` and `nameUsage`
- validator fails on accidental within-chapter name reuse
